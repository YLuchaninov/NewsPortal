# Work

Этот файл хранит только live execution state. Он не является backlog, blueprint или историей.

## Свежесть live state

- Последняя проверка этого файла по worktree reality: 2026-05-07
- Последняя проверка blockers/dependencies: 2026-05-07
- Следующая revalidation для blocked items: n/a

## Текущий режим

- Workflow mode: normal
- Разрешенные workflow modes: setup | normal | repair
- Work route: capability
- Разрешенные work routes: bootstrap | micro-patch | capability | bugfix | sweep | audit | docs-operator | delivery
- Route phase: capability-local-runtime-cutover-proofed
- Route-specific next step: optional full-product matrix or production rollout planning only; production deploy remains outside this capability and requires separate operator approval.
- Route-specific proof: local runtime `0052` migration read-back, v3 API/admin/MCP compose proof, targeted Python/API/worker proof, SDK/MCP/admin cutover unit proof, v3 live/yield discovery harness proof, migration smoke, TS/Python lint, package typechecks and whitespace check.
- Planning required by route: yes
- Planning source: tool-native
- Plan/spec status: accepted-for-this-item
- Audit overlay: none
- Разрешенные audit overlay values: none | requested | active-read-only | approved-for-apply
- Фокус аудита: n/a
- Почему сейчас: user requested implementation of the resilient/adversarial discovery rebuild with durable documentation, MCP and UI follow-through, old discovery cleanup, and provider expansion support.
- Active item id: NEWSPORTAL-RESILIENT-DISCOVERY-CAPABILITY-1
- Active item status: proofed-local-runtime-cutover
- Item status: proofed-local-runtime-cutover
- Risk: high
- Approval required: yes for applying destructive schema migration or removing old runtime surfaces from a live database; source-level stage 1 writes are approved by the user's implementation request.
- Approval reason: capability includes a destructive discovery schema cutover, API/MCP/UI replacement, LLM/search/social provider behavior, and broad operator surface changes.
- Operator approval recorded: 2026-05-07 user request `PLEASE IMPLEMENT THIS PLAN: Plan: Завершить Resilient Discovery V3 Capability` explicitly includes the destructive runtime cutover plan and approval-gated migration/legacy cleanup requirements. Approved actions for this stage: apply not-yet-applied `0052_resilient_discovery_rebuild.sql` to the local runtime DB after migration smoke and schema_migrations check, then disable/delete legacy discovery runtime source paths after DB read-back proof. Production deploy remains out of scope.
- Runtime cutover proof recorded: 2026-05-07 `0052_resilient_discovery_rebuild.sql` was not applied before gate, `pnpm test:migrations:smoke` passed, then `pnpm db:migrate` applied `0052` to local runtime DB. Read-back verified old mission/candidate/recall tables are gone, v3 tables/views exist, retained ingestion tables remained present, and `discovery_legacy_archive_batches` holds legacy archive batch rows. Runtime proof then created/promoted a v3 RSS endpoint through the registrar, emitted `source.channel.sync.requested`, created/evaluated a Source Evidence Contract from probation to active, and verified partial/full coverage contribution config in `source_channels.config_json.discovery`.

## Проверки закрытия route

- `.aidp/os.yaml` initialization flag: true
- `.aidp/os.yaml` placeholder flag: false
- Setup route: закрыт 2026-04-24
- Repair route: закрыт 2026-04-25 after live-state/docs cleanup repair
- Current lifecycle mode: `normal`
- Current work route: capability
- Last completed work route: `sweep` for `NEWSPORTAL-NON-NATIVE-ADMIN-CONFIRMATION-SWEEP-1`.
- Normal mode note: `normal` не является work route; next work must select an explicit route.

## Item state machine

- Разрешенные item statuses: planned | ready | active | blocked | done | cancelled | superseded | archived
- `done` разрешен только после route-specific proof and close gate.
- `archived` разрешен только после sync в `.aidp/history.md`.
- `superseded` требует named replacing item.
- `blocked` требует blocker and next unblock condition.

## Текущая память

- NewsPortal — pnpm polyglot monorepo with Astro web/admin, FastAPI API, Node fetchers/relay/MCP, Python workers/ML/indexer, PostgreSQL, Redis/BullMQ and Docker Compose local baseline.
- PostgreSQL is durable business truth; Redis/BullMQ, HNSW, snapshots, queues and cache are derived/runtime state.
- Canonical AIDP runtime truth lives in `.aidp/*`; root/tool router files must remain thin.
- Product/reference docs remain under `docs/product`; runtime-agent contracts live under `.aidp/contracts/*`.
- Stateful proof must follow `.aidp/contracts/test-access-and-fixtures.md`.

## Активное execution state

### Primary active item

- ID: NEWSPORTAL-DISCOVERY-V3-INTELLIGENCE-COMPLETION-1
- Parent capability: Discovery acquisition.
- Почему это primary active work: complete the remaining resilient discovery v3 intelligence layer after the local `0052` runtime cutover: LLM decision logging/templates, deterministic-fallback LLM gateway, autopilot/config simplification, bounded adversarial runtime integration, search fanout/source-directory/source expansion gaps, and API/MCP/admin read/write surfaces.

### Secondary active item

- ID: none
- Почему существует: n/a
- Разрешенные overlap paths: n/a
- Условие выхода к одному primary item: n/a

### Active discovery v3 intelligence completion

- ID: NEWSPORTAL-DISCOVERY-V3-INTELLIGENCE-COMPLETION-1
- Parent capability: Discovery acquisition.
- Lifecycle mode: normal.
- Work route: capability.
- Scope: additive `0053_discovery_v3_intelligence_layer.sql`, v3 LLM gateway/schemas/tasks/autopilot worker modules, runtime wiring for Explorer -> Constructive Skeptic -> Repairer -> Verification Skeptic -> deterministic Referee, bounded provider fanout/source-directory/source-expansion completion, API/SDK/MCP/admin surfaces for create-simple/config/explain/diagnose/LLM decisions/source expand/replace, and focused proof.
- Out of scope: editing already-applied `0052`, production/shared DB migration or deploy, unsafe social scraping, website auto-promotion by default, deleting runtime evidence rows, or git commits.
- Accepted plan/spec: user-provided `Discovery V3 Completion Audit And Remaining Implementation Plan` from 2026-05-07.
- Blueprint context checked: PostgreSQL source-of-truth, Discovery acquisition, Operator/admin control plane, Public API vs admin/operator API, Source model vs provider adapters, LLM/search/social provider risk boundaries, MCP strict typed facade.
- Safety gates: all new schema work is additive in `0053`; LLM output remains proposal-only; deterministic probes/contracts/provider policy/thresholds remain final authority; social providers remain official-API-only and `monitor_only`/`needs_config`; replay eval proof is required before claiming threshold/prompt/policy improvement.
- Required proof: `pnpm test:migrations:smoke`, targeted Python unit discovery intelligence tests, targeted TS MCP/admin/API tests if touched, `pnpm lint:py`, `pnpm lint:ts`, `pnpm typecheck`, `git diff --check`, and bounded/fixture compose proof where feasible.
- Proof passed: source-level intelligence completion proof yes. Added additive `0053_discovery_v3_intelligence_layer.sql`; discovery LLM decision/template tables; v3 LLM gateway/schemas/tasks/autopilot modules; search fanout adapter; source-expansion/replacement planner; bounded source-directory live extraction; orchestrator wiring for graph expansion, Constructive Skeptic -> Repairer -> Verification Skeptic -> Referee, debate persistence and bounded generations; API/SDK/MCP/admin surfaces for create-simple, autopilot profiles, config simplify, coverage explain, run diagnose, endpoint explain, LLM decisions and source expand/replace. Verification: `PYTHONPATH=. python -m unittest tests.unit.python.test_discovery_v3_policy tests.unit.python.test_discovery_v3_stability tests.unit.python.test_api_discovery_v3_cutover tests.unit.python.test_discovery_v3_intelligence` passed 43/43; `pnpm unit_tests:ts -- mcp-control-plane` passed 340/340; `pnpm test:migrations:smoke`, `pnpm lint:py`, `pnpm lint:ts`, `pnpm typecheck`, and `git diff --check` passed. Admin/package typecheck retains pre-existing Astro hints but 0 errors.
- Residual proof not run in this source-level pass: heavy compose UI/MCP/admin nonregression suites and live external provider calls; production/shared migration/deploy remains out of scope.

### Active resilient discovery capability

- ID: NEWSPORTAL-RESILIENT-DISCOVERY-CAPABILITY-1
- Parent capability: Discovery acquisition.
- Scope stage 1: `.aidp/work.md`, `.aidp/contracts/discovery-agent.md`, `.aidp/contracts/independent-recall-discovery.md`, `docs/product/operator/discovery/resilient-discovery.md`, `database/migrations/0052_resilient_discovery_rebuild.sql`, `services/workers/app/discovery_v3_settings.py`, `services/workers/app/discovery_v3_provider_capabilities.py`, `services/workers/app/discovery_v3_endpoint_classification.py`, `services/workers/app/discovery_v3_scoring.py`, `services/workers/app/discovery_v3_actions.py`, focused unit tests, and proof.
- Out of scope stage 1: applying migration to the live local DB, deleting old API/MCP/admin/worker runtime code, replacing admin UI, changing fetcher network behavior, adding live social providers, production deploy, or disabling existing `source_channels`.
- Accepted plan/spec: tool-native plan from 2026-05-07 accepted for this item; implementation proceeds in stages, beginning with canonical docs plus pure policy/runtime skeleton.
- Blueprint context checked: `.aidp/blueprint.md` PostgreSQL source-of-truth, Discovery acquisition, Operator/admin control plane, Source model vs provider adapters, Product docs vs AIDP runtime; `.aidp/contracts/discovery-agent.md`, `.aidp/contracts/independent-recall-discovery.md`, `.aidp/contracts/mcp-control-plane.md`, `.aidp/contracts/feed-ingress-adapters.md`, `.aidp/contracts/runtime-migrations-and-derived-state.md`.
- Required proof stage 1: targeted Python unit proof for new pure discovery modules; `pnpm lint:py` or focused Python syntax proof if lint is blocked by broader repo state; `git diff --check --` touched paths; migration source inspection. Migration smoke is required before any stage can claim DB cutover complete.
- Proof passed: yes; `PYTHONPATH=. python -m unittest tests.unit.python.test_discovery_v3_policy` passed 8/8; `pnpm lint:py` passed; `git diff --check -- .aidp/work.md .aidp/contracts/discovery-agent.md .aidp/contracts/independent-recall-discovery.md docs/product/README.md docs/product/operator/discovery/resilient-discovery.md database/migrations/0052_resilient_discovery_rebuild.sql services/workers/app/discovery_v3_settings.py services/workers/app/discovery_v3_provider_capabilities.py services/workers/app/discovery_v3_endpoint_classification.py services/workers/app/discovery_v3_scoring.py services/workers/app/discovery_v3_actions.py tests/unit/python/test_discovery_v3_policy.py` passed.
- Cleanup status: no runtime state cleanup or destructive DB migration is planned in stage 1.

### Active resilient discovery stage 2 stability guards

- ID: NEWSPORTAL-RESILIENT-DISCOVERY-STABILITY-GUARDS-STAGE-2
- Parent capability: Discovery acquisition.
- Scope stage 2: amend not-yet-applied `database/migrations/0052_resilient_discovery_rebuild.sql`, update discovery AIDP/product docs, extend pure v3 worker modules for Constructive Skeptic, source evidence contracts/probation, negative evidence, claims/control comparisons, provider health/circuit breakers, source identity, replay eval, diversity budgets and blast-radius limits, plus focused unit proof.
- Out of scope stage 2: applying migration to live DB, deleting old API/MCP/admin/worker runtime code, implementing full API/MCP/admin cutover, adding live social provider calls, production deploy, or changing existing source-channel ingestion runtime behavior.
- Accepted plan/spec: tool-native follow-up plan from 2026-05-07 accepted for this stage.
- Blueprint context checked: same as stage 1; this stage adds explicit poisoning/blast-radius controls before promotion/runtime integration.
- Required proof stage 2: targeted Python unit proof for new pure discovery guard modules; `pnpm lint:py`; `git diff --check --` touched paths. Migration smoke remains required before any DB cutover claim.
- Proof passed: yes for source-level guard stage; `PYTHONPATH=. python -m unittest tests.unit.python.test_discovery_v3_policy tests.unit.python.test_discovery_v3_stability` passed 21/21; `pnpm lint:py` passed; `git diff --check -- .aidp/work.md .aidp/contracts/discovery-agent.md .aidp/contracts/independent-recall-discovery.md docs/product/README.md docs/product/operator/discovery/resilient-discovery.md database/migrations/0052_resilient_discovery_rebuild.sql services/workers/app/discovery_v3_actions.py services/workers/app/discovery_v3_adversarial.py services/workers/app/discovery_v3_claims.py services/workers/app/discovery_v3_contracts.py services/workers/app/discovery_v3_endpoint_classification.py services/workers/app/discovery_v3_eval.py services/workers/app/discovery_v3_identity.py services/workers/app/discovery_v3_negative_evidence.py services/workers/app/discovery_v3_provider_capabilities.py services/workers/app/discovery_v3_provider_health.py services/workers/app/discovery_v3_referee.py services/workers/app/discovery_v3_repair.py services/workers/app/discovery_v3_scoring.py services/workers/app/discovery_v3_settings.py services/workers/app/discovery_v3_skeptic.py tests/unit/python/test_discovery_v3_policy.py tests/unit/python/test_discovery_v3_stability.py` passed.
- Cleanup status: no runtime state cleanup or destructive DB migration planned in stage 2.

### Active resilient discovery stage 3 runtime cutover

- ID: NEWSPORTAL-RESILIENT-DISCOVERY-RUNTIME-CUTOVER-STAGE-3
- Parent capability: Discovery acquisition.
- Scope stage 3: apply approved local runtime `0052`, prove v3 runtime DB, enable bounded v3 provider execution path, disable old mission/recall orchestrator task runtime, remove reachable legacy admin discovery pages, and prove promotion/probation/contract behavior.
- Out of scope stage 3: production deployment, live social scraping/provider access, website auto-promotion, destructive cleanup outside the approved local discovery cutover, broad removal of historical migration files, or deleting unrelated old docs/components not reachable from runtime routes.
- Accepted plan/spec: user-provided full resilient discovery cutover plan plus follow-up capability-completion plan from 2026-05-07.
- Blueprint context checked: PostgreSQL source-of-truth, Runtime-поверхности, PostgreSQL vs derived state, Source model vs provider adapters, Public API vs admin/operator API, Discovery acquisition, Operator/admin control plane.
- Proof passed: local-runtime cutover proof yes. `pnpm test:migrations:smoke` passed before runtime migration; runtime DB check showed `0052` absent, then `pnpm db:migrate` applied `0052`; runtime read-back showed old checked discovery tables gone, v3 relations/constraints present, retained ingestion tables untouched, and `discovery_legacy_archive_batches` populated. Added bounded live execution in `services/workers/app/discovery_v3_execution.py`, v3 result persistence/provider-health repair handling in `services/workers/app/discovery_v3_orchestrator.py` and `services/workers/app/discovery_v3_repository.py`, v3-only orchestrator task names, and legacy API worker boundary stubs. Runtime smoke created a v3 target/run, persisted an endpoint from fixtures, promoted the RSS endpoint through `PostgresSourceRegistrarAdapter`, emitted `source.channel.sync.requested`, created a probation contract with coverage `0.25`/downstream `0.3`, then contract evaluation promoted it to active coverage/downstream `1.0`. API route inspection showed only v3 `/maintenance/discovery/*` routes and no mission/recall/candidate/class/profile routes. Deleted reachable old admin discovery subpages under `apps/admin/src/pages/discovery/*`, deleted legacy admin discovery components/view-model payload helpers, replaced admin/operator tests with v3 coverage, retired legacy API modules behind explicit removed stubs, replaced the worker discovery-enabled smoke with v3 adapter proof, and replaced the old live mission/recall examples harness with a v3 read-surface/yield proof harness that preserves evidence artifacts without touching old tables.
- Verification passed: `PYTHONPATH=. python -m unittest discover -s tests/unit/python -p 'test_*.py'` passed 324/324; targeted v3 Python discovery suites passed 57/57; `pnpm unit_tests:ts -- mcp-control-plane discovery-admin admin-operator-surfaces mcp-http-doc-parity mcp-http-live-diagnostics` passed 340/340; `pnpm test:discovery:admin:compose` passed; `node infra/scripts/test-live-discovery-examples.mjs` passed and wrote `/tmp/newsportal-live-discovery-examples-68009e85.json`; `pnpm test:discovery:yield:compose` passed and wrote `/tmp/newsportal-live-discovery-yield-proof-6bdc6d5b.json`; `DISCOVERY_ENABLED=1 PYTHONPATH=. python -m infra.scripts.workers.smoke discovery-enabled` passed; `pnpm test:migrations:smoke`, `pnpm lint:py`, `pnpm lint:ts`, `pnpm typecheck`, and `git diff --check` passed. `pnpm typecheck` retained pre-existing Astro hints but 0 errors.
- Cleanup status: local runtime proof intentionally left one `codex-runtime-smoke` source channel/target/run/contract row as read-back evidence for the approved local cutover; no production state was touched.

### Active MCP template/token hygiene bugfix

- ID: NEWSPORTAL-MCP-TEMPLATE-TOKEN-HYGIENE-BUGFIX-1
- Parent capability: MCP strict typed control-plane facade and ongoing operating intelligence.
- Scope: `.aidp/work.md`, `packages/control-plane/src/templates.ts`, `packages/control-plane/src/mcp-tokens.ts`, `services/mcp/src/tools/admin-tools.ts`, `services/mcp/src/resources.ts`, `tests/unit/ts/mcp-control-plane.test.ts`, targeted proof, and local DB expired test-token cleanup.
- Out of scope: adding per-interest `promptTemplateId` linkage, schema migrations, production deploy, changing MCP token auth expiry semantics, deleting non-expired tokens, or changing read-only id prefix behavior.
- Accepted fix: hydrate existing interest/LLM template rows before update so omitted optional fields preserve current DB values; expose token `effectiveStatus`, `activeUsable`, and `expiredActive` in MCP token inventory/summary surfaces; delete local expired test tokens after verification.
- Blueprint context checked: `.aidp/blueprint.md` PostgreSQL source-of-truth, Operator/admin control plane, Public API vs admin/operator API; `.aidp/contracts/mcp-control-plane.md` token inventory and strict typed facade.
- Required proof: targeted TS unit proof for template update preservation and token effective counts; `pnpm --filter @newsportal/control-plane typecheck`; `pnpm --filter @newsportal/mcp typecheck`; `pnpm lint:ts`; `git diff --check --` touched paths; runtime MCP rebuild and health check; DB before/after cleanup count.
- Proof passed: yes; `pnpm unit_tests:ts -- mcp-control-plane` passed 352/352; `pnpm --filter @newsportal/control-plane typecheck`, `pnpm --filter @newsportal/mcp typecheck`, `pnpm lint:ts`, and `git diff --check -- .aidp/work.md packages/control-plane/src/templates.ts packages/control-plane/src/mcp-tokens.ts services/mcp/src/tools/admin-tools.ts services/mcp/src/resources.ts services/mcp/src/tools/shared.ts services/mcp/src/tools/sequences-tools.ts services/mcp/src/tools/discovery/write-tools.ts services/mcp/src/tools/channels-tools.ts services/mcp/src/tools/templates-tools.ts tests/unit/ts/mcp-control-plane.test.ts` passed. DB cleanup verification shows no remaining expired active MCP tokens (`active=56`, `revoked=11`, `expired_count=0`); deletion query removed 0 additional rows because the expired local test rows were already gone. MCP container was rebuilt with `docker compose --env-file .env.dev -f infra/docker/compose.yml -f infra/docker/compose.dev.yml up --build -d mcp`; `/health` returned `{"service":"mcp","status":"ok"}`, `docker-mcp-1` is healthy, and recent `mcp_request_log` check showed 0 errors in the last 5 minutes.
- Cleanup status: expired active local MCP test-token cleanup verified complete; no non-expired tokens were deleted.

### Active MCP sequence id guard bugfix

- ID: NEWSPORTAL-MCP-SEQUENCE-ID-GUARD-BUGFIX-1
- Parent capability: MCP strict typed control-plane facade and ongoing operating intelligence.
- Scope: `.aidp/work.md`, `services/mcp/src/tools/shared.ts`, `services/mcp/src/tools/sequences-tools.ts`, `tests/unit/ts/mcp-control-plane.test.ts`, and targeted proof.
- Out of scope: changing sequence backend semantics, accepting ID prefixes for mutating tools, schema migrations, production deploy, or deleting runtime sequence rows/runs.
- Accepted fix: add a shared full-UUID reader for mutating MCP id arguments and use it for sequence update/run/retry/cancel/archive paths; read-only sequence tools keep existing alias/prefix behavior.
- Blueprint context checked: `.aidp/blueprint.md` Operator/admin control plane, PostgreSQL source-of-truth, API/control-plane boundaries; `.aidp/contracts/mcp-control-plane.md` strict typed facade and sequence cleanup/reindex guidance.
- Required proof: targeted MCP unit proof that invalid sequence write ids fail with MCP `-32602` before backend fetch; `pnpm --filter @newsportal/mcp typecheck`; `pnpm lint:ts`; `git diff --check --` touched paths; runtime MCP rebuild and health check.
- Proof passed: yes; follow-up similarity sweep found adjacent malformed-UUID risk in discovery write ids, discovery id-bearing payloads, template update/destructive ids, MCP token lifecycle ids, and channel delete. Added shared optional/required UUID readers and applied full-UUID MCP boundary validation while preserving read-only alias/prefix behavior and string-key `discovery.classes.*` handling. `pnpm unit_tests:ts -- mcp-control-plane` passed 350/350 including sequence and adjacent malformed-id regression coverage; `pnpm --filter @newsportal/mcp typecheck`, `pnpm lint:ts`, and `git diff --check -- .aidp/work.md services/mcp/src/tools/shared.ts services/mcp/src/tools/sequences-tools.ts services/mcp/src/tools/discovery/write-tools.ts services/mcp/src/tools/admin-tools.ts services/mcp/src/tools/channels-tools.ts services/mcp/src/tools/templates-tools.ts tests/unit/ts/mcp-control-plane.test.ts` passed. Final `rg` found no remaining `readRequiredString(...Id)` in MCP tools except the private `resolveChannelId` helper, which immediately validates full UUID/unique prefix. MCP container was rebuilt with `docker compose --env-file .env.dev -f infra/docker/compose.yml -f infra/docker/compose.dev.yml up --build -d mcp`; `/health` returned ok, `docker-mcp-1` is healthy, and a fresh 5-minute `mcp_request_log` check showed 0 new errors.
- Cleanup status: compose stack remains running for product testing; no destructive cleanup planned.

### Согласованность worktree

- Existing dirty worktree contains prior AIDP/admin/control-plane changes; do not revert unrelated changes.
- Scope warning: do not run broad `git clean -fdX`; ignored `.env.*`, `.idea`, `node_modules`, `dist`, `.astro`, `data/models`, `data/snapshots` and other runtime/build artifacts may be locally useful and must only be removed by explicit targeted request.
- No commit/stage/branch changes were requested or performed for the latest sweep/stages.

### Active MCP recall evidence update guard bugfix

- ID: NEWSPORTAL-MCP-RECALL-EVIDENCE-UPDATE-GUARD-BUGFIX-1
- Parent capability: MCP strict typed control-plane facade and ongoing operating intelligence.
- Scope: `.aidp/work.md`, `packages/contracts/src/mcp-schemas.ts`, `services/mcp/src/tools/discovery/write-tools.ts`, `tests/unit/ts/mcp-control-plane.test.ts`, and targeted proof.
- Out of scope: backend/admin recall candidate edit semantics, live network feed probing, schema migrations, production deploy, or deleting the already-promoted product-test channel/candidate.
- Accepted fix: remove `evaluationJson` from MCP recall candidate create/update payload shapes and tool guidance, so MCP clients cannot forge promotion evidence through either candidate creation or later review-state updates; keep review status/reason fields writable and keep promotion evidence checks on acquisition-owned stored candidate evidence.
- Blueprint context checked: `.aidp/blueprint.md` Operator/admin control plane, Discovery acquisition, PostgreSQL source-of-truth; `.aidp/contracts/mcp-control-plane.md` recall promotion evidence guard.
- Required proof: targeted MCP unit proof that `discovery.recall_candidates.create/update` with `evaluationJson` fails at MCP schema boundary before backend fetch; `pnpm --filter @newsportal/contracts typecheck`; `pnpm --filter @newsportal/mcp typecheck`; `pnpm lint:ts`; `node --check infra/scripts/lib/mcp-http-scenarios.mjs`; `git diff --check --` touched paths; runtime MCP rebuild and health check.
- Proof passed: yes; live `mcp_request_log` audit showed most errors were expected MCP guards, but found a real bypass where a client used `discovery.recall_candidates.update` to rewrite `evaluationJson.validFeed/discoveredFeedUrls` after an RSS promotion denial, then promoted successfully. Follow-up logic audit found the same trust edge on MCP candidate create, so `evaluationJson` is now removed from both MCP recall candidate create and update schemas. Updated tool guidance and MCP HTTP scenarios so acquisition/probe evidence cannot be forged through MCP candidate writes; explicit source rows should use channel/bulk onboarding and website candidate shell promotion needs `overrideReason`. `pnpm unit_tests:ts -- mcp-control-plane` passed 348/348; `pnpm --filter @newsportal/contracts typecheck`, `pnpm --filter @newsportal/mcp typecheck`, `pnpm lint:ts`, `node --check infra/scripts/lib/mcp-http-scenarios.mjs`, and `git diff --check -- .aidp/work.md packages/contracts/src/mcp-schemas.ts services/mcp/src/tools/discovery/write-tools.ts tests/unit/ts/mcp-control-plane.test.ts infra/scripts/lib/mcp-http-scenarios.mjs` passed; MCP container was rebuilt with `docker compose --env-file .env.dev -f infra/docker/compose.yml -f infra/docker/compose.dev.yml up --build -d mcp`; `/health` returned ok and `docker-mcp-1` is healthy.
- Cleanup status: compose stack remains running for product testing after the MCP rebuild; no destructive cleanup was run.

### Active MCP automation-first guidance micro-patch

- ID: NEWSPORTAL-MCP-AUTOMATION-FIRST-GUIDANCE-MICROPATCH-1
- Parent capability: MCP strict typed control-plane facade and ongoing operating intelligence.
- Scope: `.aidp/work.md`, `.aidp/contracts/mcp-control-plane.md`, `services/mcp/src/context.ts`, `services/mcp/src/resources.ts`, `services/mcp/src/prompts.ts`, `services/mcp/src/operating-intelligence.ts`, and targeted checks.
- Out of scope: changing backend discovery auto-promotion semantics, bypassing recall promotion evidence guards, making destructive operations automatic, schema migrations, or production deploy.
- Accepted approach: make server instructions, discovery resources, and relevant prompts say that NewsPortal is guarded-automation-first; MCP clients should plan automatic setup/run/verify/tuning when profiles/policies/evidence make it safe, and treat manual approval as explicit operator intent or fallback for missing policy/evidence, destructive actions, unsafe promotions, or unclear decisions.
- Blueprint context checked: `.aidp/blueprint.md` Operator/admin control plane and discovery safe-by-default invariants; `.aidp/contracts/mcp-control-plane.md` tool/resource contract and report verification guidance.
- Required proof: `pnpm --filter @newsportal/mcp typecheck`; `pnpm lint:ts`; `git diff --check --` touched paths; runtime MCP rebuild and health check.
- Proof passed: yes; MCP server instructions now state the default planning posture is guarded automation, not manual review. Discovery scenario resources and prompts now tell clients to use/create profiles and pass `profileId` for graph and recall missions when the operator did not request manual approval, with manual-review-only described as fallback for missing policy/evidence/ambiguity. The MCP control-plane contract records automation-first planning as part of initialize guidance. `pnpm --filter @newsportal/mcp typecheck`, `pnpm lint:ts`, and `git diff --check -- .aidp/work.md .aidp/contracts/mcp-control-plane.md services/mcp/src/context.ts services/mcp/src/resources.ts services/mcp/src/prompts.ts services/mcp/src/operating-intelligence.ts` passed; MCP container was rebuilt with `docker compose --env-file .env.dev -f infra/docker/compose.yml -f infra/docker/compose.dev.yml up --build -d mcp`; `/health` returned ok and `docker-mcp-1` is healthy.
- Cleanup status: compose stack remains running for product testing after the MCP rebuild; no destructive cleanup was run.

### Active recall no-profile guidance bugfix

- ID: NEWSPORTAL-MCP-RECALL-NO-PROFILE-GUIDANCE-BUGFIX-1
- Parent capability: MCP strict typed control-plane facade and ongoing operating intelligence.
- Scope: `.aidp/work.md`, `services/mcp/src/tools/discovery/write-tools.ts`, `services/mcp/src/tools.ts`, `services/mcp/src/context.ts`, `services/mcp/src/resources.ts`, `services/mcp/src/operating-intelligence.ts`, and targeted MCP control-plane tests.
- Out of scope: forcing every recall mission to use a profile, changing backend recall scoring/promotion semantics, schema migrations, production deploy, deleting runtime candidates, or enabling unsafe auto-promotion by default.
- Accepted plan: keep profile-less recall missions allowed, but make MCP mutation responses and `operator.report.verify discovery_run` explicitly warn that recall missions without `profileId`/applied policy are manual-review-only and should not be reported as recallPolicy/minPromotionScore configured; include recall candidate status counts so pending recall review is visible.
- Blueprint context checked: `.aidp/blueprint.md` Operator/admin control plane, Discovery acquisition, PostgreSQL source-of-truth; `.aidp/contracts/mcp-control-plane.md` report verification and recall candidate promotion guards.
- Required proof: targeted MCP unit proof for recall mission create/acquire guidance and discovery report warnings; `pnpm --filter @newsportal/mcp typecheck`; `pnpm lint:ts`; `git diff --check --` touched paths; runtime MCP rebuild and health check.
- Proof passed: yes; second-pass similarity audit found the recall lane had the same profile/policy ambiguity as graph missions. `discovery.recall_missions.create/update/acquire` now warn when no `profileId`/applied recall policy exists and point clients to read-back. `operator.report.verify reportKind=discovery_run` now includes recall mission policy flags, recall candidate status counts, pending recall warnings, and avoids pulling unrelated latest profiles/missions/recall missions when specific entity filters are supplied. Updated MCP instructions/resources/nextReadBack to cover graph and recall missions. `pnpm unit_tests:ts -- mcp-control-plane` passed 347/347; `pnpm --filter @newsportal/mcp typecheck`, `pnpm lint:ts`, and `git diff --check -- .aidp/work.md services/mcp/src/tools/discovery/write-tools.ts services/mcp/src/tools.ts services/mcp/src/context.ts services/mcp/src/resources.ts services/mcp/src/operating-intelligence.ts tests/unit/ts/mcp-control-plane.test.ts` passed; MCP container was rebuilt with `docker compose --env-file .env.dev -f infra/docker/compose.yml -f infra/docker/compose.dev.yml up --build -d mcp`; `/health` returned ok and `docker-mcp-1` is healthy.
- Cleanup status: compose stack remains running for product testing after the MCP rebuild; no destructive cleanup was run.

### Active discovery no-profile guidance bugfix

- ID: NEWSPORTAL-MCP-DISCOVERY-NO-PROFILE-GUIDANCE-BUGFIX-1
- Parent capability: MCP strict typed control-plane facade and ongoing operating intelligence.
- Scope: `.aidp/work.md`, `services/mcp/src/tools/discovery/write-tools.ts`, `services/mcp/src/tools.ts`, `services/mcp/src/context.ts`, `services/mcp/src/resources.ts`, `services/mcp/src/operating-intelligence.ts`, and targeted MCP control-plane tests.
- Out of scope: forcing every discovery mission to use a profile, changing backend discovery ranking/promotion semantics, schema migrations, production deploy, reviewing/deleting current runtime candidates, or enabling unsafe auto-promotion by default.
- Accepted plan: keep profile-less manual discovery allowed, but make MCP mutation responses and report verification explicitly warn that missions without `profileId`/applied policy are manual-review-only and should not be reported as threshold-configured/auto-promote capable; add pending-candidate warnings to `operator.report.verify discovery_run`.
- Blueprint context checked: `.aidp/blueprint.md` Operator/admin control plane, Discovery acquisition, PostgreSQL source-of-truth; `.aidp/contracts/mcp-control-plane.md` report verification and read-after-write guidance.
- Required proof: targeted MCP unit proof for no-profile discovery mission response and `operator.report.verify` warnings; `pnpm --filter @newsportal/mcp typecheck`; `pnpm lint:ts`; `git diff --check --` touched paths; runtime MCP rebuild and health check.
- Proof passed: yes; MCP now keeps profile-less manual discovery allowed but returns explicit `mcpWarnings`/read-back guidance for `discovery.missions.create/run` when no `profileId`/applied policy exists, and `operator.report.verify reportKind=discovery_run` warns when missions are manual-review-only or still have pending candidates. Updated discovery instructions/resources/read-back note to tell clients to pass `payload.profileId` when auto-promotion/profile thresholds are expected. `pnpm unit_tests:ts -- mcp-control-plane` passed 346/346; `pnpm --filter @newsportal/mcp typecheck`, `pnpm lint:ts`, and `git diff --check -- .aidp/work.md services/mcp/src/tools/discovery/write-tools.ts services/mcp/src/tools.ts services/mcp/src/context.ts services/mcp/src/resources.ts services/mcp/src/operating-intelligence.ts tests/unit/ts/mcp-control-plane.test.ts` passed; MCP container was rebuilt with `docker compose --env-file .env.dev -f infra/docker/compose.yml -f infra/docker/compose.dev.yml up --build -d mcp`; `/health` returned ok and `docker-mcp-1` is healthy.
- Cleanup status: compose stack remains running for product testing after the MCP rebuild; no destructive cleanup was run.

### Active read-id alias/prefix sweep

- ID: NEWSPORTAL-MCP-READ-ID-ALIAS-PREFIX-SWEEP-BUGFIX-2
- Parent capability: MCP strict typed control-plane facade and ongoing operating intelligence.
- Scope: `.aidp/work.md`, `services/mcp/src/tools/discovery/read-tools.ts`, `services/mcp/src/tools/sequences-tools.ts`, `services/mcp/src/tools/content-tools.ts`, and targeted MCP control-plane tests.
- Out of scope: accepting missing IDs as guessed latest entities, broad write/destructive ID prefix handling, schema migrations, production deploy, deleting runtime rows, or changing backend/API persistence semantics.
- Accepted plan: scan MCP read tools that still require one canonical id field; for read-only tools, accept common report aliases such as `id`/`entityId` and unique UUID prefixes where the source table is known; keep missing ID errors explicit and keep mutating/destructive tools strict except for already-intentional narrow helper tools.
- Blueprint context checked: `.aidp/blueprint.md` PostgreSQL source-of-truth, Public API vs admin/operator API, Operator/admin control plane; `.aidp/contracts/mcp-control-plane.md` tool/resource contract and read-after-write/report verification guidance.
- Required proof: targeted MCP unit proof for discovery/sequence/content read aliases and prefixes; `pnpm --filter @newsportal/mcp typecheck`; `pnpm lint:ts`; `git diff --check --` touched paths; runtime MCP rebuild and health check.
- Proof passed: yes; scan found read-only discovery profile/class/candidate/recall/source/score reads, sequence/sequence-run reads, article/content-item/web-resource reads with the same canonical-id-only risk. Added shared MCP alias + unique UUID-prefix resolver for read-back tools, kept missing-id errors explicit, and left mutating/destructive tools strict. `pnpm unit_tests:ts -- mcp-control-plane` passed 345/345; `pnpm --filter @newsportal/mcp typecheck`, `pnpm lint:ts`, and `git diff --check -- .aidp/work.md services/mcp/src/tools/shared.ts services/mcp/src/tools/discovery/read-tools.ts services/mcp/src/tools/sequences-tools.ts services/mcp/src/tools/content-tools.ts tests/unit/ts/mcp-control-plane.test.ts` passed; MCP container was rebuilt with `docker compose --env-file .env.dev -f infra/docker/compose.yml -f infra/docker/compose.dev.yml up --build -d mcp`; `/health` returned ok and `docker-mcp-1` is healthy.
- Cleanup status: compose stack remains running for product testing after the MCP rebuild; no destructive cleanup was run.

### Active stage scope

- Current MCP client compatibility bugfix scope: `.aidp/work.md`, `packages/control-plane/src/channels.ts`, `services/mcp/src/tools/channels-tools.ts`, `services/mcp/src/tools/templates-tools.ts`, `services/mcp/src/tools/discovery/read-tools.ts`, `services/mcp/src/operating-intelligence.ts`, `infra/scripts/lib/mcp-http-testkit.mjs`, and targeted MCP control-plane/scenario tests.
- Current MCP client compatibility bugfix out of scope: changing discovery search/ranking, channel provider persistence semantics beyond a safe activation toggle, schema migrations, production deploy, deleting product-test runtime rows, or broad admin UI changes.
- Current MCP client compatibility bugfix accepted plan: use `mcp_request_log` and API logs to classify the three reported failures; add alias-tolerant but still strict entity id parsing for system interest reads; add a safe channel activation/deactivation path or clearer partial-update guard so clients do not need to guess full provider payloads; harden discovery candidate listing against backend 500 from malformed/truncated filters; scan nearby MCP read/write tools for the same alias/partial-payload pattern; prove invalid-path behavior returns MCP `-32602` or guarded structured errors rather than backend 500/422.
- Current MCP client compatibility bugfix blueprint context checked: `.aidp/blueprint.md` Operator/admin control plane, PostgreSQL source-of-truth, Public API vs admin/operator API, Source model vs provider adapters, Discovery acquisition; `.aidp/contracts/mcp-control-plane.md`.
- Current MCP client compatibility bugfix required proof: DB/request-log reproduction evidence; targeted MCP unit proof for system interest aliases, channel partial activation/deactivation guidance, and discovery candidates invalid filter handling; `pnpm --filter @newsportal/mcp typecheck`; `pnpm --filter @newsportal/control-plane typecheck` if touched; `pnpm lint:ts`; `git diff --check --` touched paths; runtime MCP rebuild or explicit no-rebuild reason.
- Current MCP client compatibility bugfix proof passed: yes; request-log/API-log read-back showed the reported `discovery.candidates.list` 500s were caused by short mission UUID prefixes (`f3dbf7b8`, `909f0ed3`, `aaaf6dc4`) reaching the API UUID cast; `channels.update` failures were partial `channelId/isActive` payload guesses; `system_interests.read` failure was an `entityId` vs `interestTemplateId` alias mismatch. Implemented unique UUID-prefix resolution for discovery mission filters and channel read/activation, alias-tolerant system interest/LLM template read-back IDs, dedicated `channels.set_active`, channel-update hinting, and a small MCP HTTP diagnostics guard for invalid request URLs. `pnpm unit_tests:ts -- mcp-control-plane` passed 342/342; `pnpm --filter @newsportal/mcp typecheck`, `pnpm --filter @newsportal/control-plane typecheck`, `node --check infra/scripts/lib/mcp-http-testkit.mjs`, `pnpm lint:ts`, and `git diff --check --` touched paths passed; MCP container was rebuilt; MCP/API health checks passed; targeted compose `pnpm test:mcp:compose -- --scenario=template-interest-channel-flows --scenario=discovery-operator-flows --skip-build` passed with artifact `/tmp/newsportal-mcp-http-deterministic-2b65a0f2-48f5-4bdc-adfe-7f9889a3aa96.json`; recent MCP request-log read-back showed covered template/channel/discovery flows succeeded, with only expected guarded bulk-plan stale/confirm failures and no backend 500/422-like errors in the targeted run.
- Current MCP client compatibility bugfix proof residual: full default `pnpm test:mcp:compose -- --skip-build` still fails in the pre-existing `protocol-discovery` SSE diagnostic path while parsing an empty `text/event-stream` body as JSON; targeted scenarios covering this fix passed, and the remaining SSE harness issue is parked outside this bugfix.
- Current MCP client compatibility bugfix cleanup status: compose stack remains running for product testing after rebuilding MCP; no destructive cleanup was run.

- Current MCP discovery run read-back guard scope: `.aidp/work.md`, `packages/contracts/src/mcp-schemas.ts`, `services/mcp/src/context.ts`, `services/mcp/src/resources.ts`, `services/mcp/src/operating-intelligence.ts`, `services/mcp/src/tools.ts`, `services/mcp/src/tools/discovery/write-tools.ts`, `services/workers/app/task_engine/executor.py`, `services/workers/app/discovery_execution_runtime.py`, and targeted MCP/Python unit proof.
- Current MCP discovery run read-back guard out of scope: changing discovery search/ranking algorithms, schema migrations, production deploy, deleting the product-test missions/candidates, or changing backend/admin discovery semantics.
- Current MCP discovery run read-back guard accepted plan: verify the reported local discovery run through DB-backed evidence; add MCP-only confirmation for interactive missions with `maxHypotheses > 5`; keep backend payload clean by stripping MCP-only confirmation before SDK calls; enrich discovery run report verification with sequence run/task, hypothesis status, candidate status, and warnings; clarify discovery run instructions/resources/nextReadBack so clients report queued/running/completed truthfully; ensure worker cancellation/timeouts mark the active child run and hypothesis failed instead of leaving stale running state.
- Current MCP discovery run read-back guard blueprint context checked: `.aidp/blueprint.md` Operator/admin control plane, Discovery acquisition, PostgreSQL source-of-truth, Async pipeline routing risk; `.aidp/contracts/discovery-agent.md`; `.aidp/contracts/mcp-control-plane.md`.
- Current MCP discovery run read-back guard required proof: targeted MCP unit proof for wide-run guard and discovery report read-back; targeted Python unit proof for task-engine cancellation and discovery hypothesis cancellation; `pnpm --filter @newsportal/contracts typecheck`; `pnpm --filter @newsportal/mcp typecheck`; `pnpm lint:ts`; DB read-back summarizing the reported local run state; runtime MCP/worker rebuild; targeted MCP compose proof; `git diff --check --` touched paths.
- Current MCP discovery run read-back guard proof passed: yes; DB read-back showed the reported OpenCode run first failed one malformed `discovery.profiles.create` at MCP `-32602`, then created profile `outsourcing_demand_discovery`, three 15-hypothesis missions, and three parent API sequence runs that later failed on `execute_hypotheses` timeout with noisy/low-yield child results; local stale child runs/hypotheses left by the old cancellation behavior were marked `failed` with a repair note and read-back confirmed `stale_running_children=0`; `pnpm unit_tests:ts -- mcp-control-plane` passed 339/339 including wide-run confirmation and async read-back hints; `PYTHONPATH=. python -m unittest tests.unit.python.test_task_engine tests.unit.python.test_discovery_orchestrator` passed 32 tests including cancellation status repair; `pnpm --filter @newsportal/contracts typecheck`, `pnpm --filter @newsportal/mcp typecheck`, `pnpm lint:ts`, `pnpm lint:py`, and `git diff --check --` touched paths passed; runtime `worker` and `mcp` containers were rebuilt/recreated; MCP/API health checks passed; targeted `pnpm test:mcp:compose -- --scenario=discovery-operator-flows --skip-build` passed with artifact `/tmp/newsportal-mcp-http-deterministic-325c83b6-ac51-4c52-b5b5-6bf0e9ae948a.json`.
- Current MCP discovery run read-back guard cleanup status: compose stack remains running for product testing; stale local runtime rows from the pre-fix discovery run were marked failed rather than deleted; no destructive cleanup was run.

- Current MCP list-field normalization sweep scope: `.aidp/work.md`, `packages/contracts/src/mcp-schemas.ts`, `services/mcp/src/context.ts`, `services/mcp/src/tools.ts`, `services/mcp/src/tools/shared.ts`, `services/mcp/src/tools/discovery/write-tools.ts`, `services/mcp/src/tools/sequences-tools.ts`, `infra/scripts/lib/mcp-http-scenarios.mjs`, and `tests/unit/ts/mcp-control-plane.test.ts`.
- Current MCP list-field normalization sweep out of scope: loosening object payload envelopes, accepting wrong aliases, schema migrations, production deploy, changing discovery/selection semantics, or silently accepting unsupported enum values.
- Current MCP list-field normalization sweep accepted plan: audit list-like MCP write fields; let schemas accept JSON arrays, newline strings, or comma-separated strings for bounded list-like inputs; normalize those fields at MCP handler edge before SDK/backend calls; keep enum fields strict with MCP `-32602`; prove through targeted unit tests and live MCP compose.
- Current MCP list-field normalization sweep blueprint context checked: `.aidp/blueprint.md` Operator/admin control plane and PostgreSQL source-of-truth boundaries; `.aidp/contracts/mcp-control-plane.md` strict typed facade and backend-422 prevention contract.
- Current MCP list-field normalization sweep required proof: `pnpm --filter @newsportal/contracts typecheck`; `pnpm --filter @newsportal/mcp typecheck`; `pnpm unit_tests:ts -- mcp-control-plane`; `pnpm lint:ts`; `node --check infra/scripts/lib/mcp-http-scenarios.mjs`; `git diff --check --` touched paths; runtime MCP rebuild; targeted `pnpm test:mcp:compose -- --scenario=discovery-operator-flows --skip-build`.
- Current MCP list-field normalization sweep proof passed: yes; contracts and MCP typechecks passed; `pnpm unit_tests:ts -- mcp-control-plane` passed 338/338 and now covers comma/newline list normalization plus invalid enum `-32602` before backend fetch; `pnpm lint:ts` and scenario syntax check passed; `docker compose --env-file .env.dev -f infra/docker/compose.yml -f infra/docker/compose.dev.yml up --build -d mcp` rebuilt the MCP runtime; targeted MCP compose passed with artifact `/tmp/newsportal-mcp-http-deterministic-df9fb693-4efe-4c2e-9c47-afdb8fbdd899.json`; recent MCP request log showed discovery write/read flow succeeded with 0 failures and `backend_422_like_errors=0`.
- Current MCP list-field normalization sweep cleanup status: compose stack remains running for product testing; rebuild recreated `api` and `mcp` due compose dependencies; no destructive cleanup was run.

- Current MCP system-interest list-field bugfix scope: `.aidp/work.md`, `packages/contracts/src/mcp-schemas.ts`, `apps/admin/src/lib/server/admin-templates.ts`, `services/mcp/src/context.ts`, `services/mcp/src/resources.ts`, `services/mcp/src/tools/templates-tools.ts`, `tests/unit/ts/admin-template-sync.test.ts`, and local dev DB read-back/repair for the malformed `AI implementation failures and adoption barriers` test interest.
- Current MCP system-interest list-field bugfix out of scope: schema migrations, production deploy, deleting/recreating the interest, changing final selection semantics, changing LLM review policy, or destructive runtime cleanup.
- Current MCP system-interest list-field bugfix accepted plan: reproduce from MCP error/DB read-back; make MCP schemas accept system-interest string arrays as well as newline strings; make admin/control-plane parsing split comma-separated token/kind fields and candidate cues where appropriate; add targeted regression proof; repair the existing local malformed interest/profile/criterion JSON arrays; restart/prove runtime MCP/admin code if needed.
- Current MCP system-interest list-field bugfix blueprint context checked: `.aidp/blueprint.md` Operator/admin control plane, System selection vs personalization, PostgreSQL source-of-truth vs derived state; `.aidp/contracts/mcp-control-plane.md` strict typed facade and read-after-write/report verification contract.
- Current MCP system-interest list-field bugfix required proof: targeted TS unit parser proof; `pnpm --filter @newsportal/contracts typecheck`; `pnpm --filter @newsportal/mcp typecheck`; `pnpm --filter @newsportal/admin typecheck`; `pnpm lint:ts`; DB read-back showing split `allowed_content_kinds`, `must_not_have_terms`, profile cues, and compiled criterion; compose/restart proof if live containers must pick up code.
- Current MCP system-interest list-field bugfix proof passed: yes; DB read-back confirmed the created `AI implementation failures and adoption barriers` interest, its selection profile, criterion, and compiled criterion now use split arrays for `allowed_content_kinds`, `must_not_have_terms`, and candidate cues; `pnpm --filter @newsportal/contracts typecheck`, `pnpm --filter @newsportal/mcp typecheck`, `pnpm --filter @newsportal/admin typecheck`, `pnpm unit_tests:ts -- admin-template-sync` (337/337), `pnpm lint:ts`, `node --check infra/scripts/lib/mcp-http-scenarios.mjs`, and `git diff --check -- .aidp/work.md packages/contracts/src/mcp-schemas.ts apps/admin/src/lib/server/admin-templates.ts services/mcp/src/context.ts services/mcp/src/resources.ts services/mcp/src/tools/templates-tools.ts tests/unit/ts/admin-template-sync.test.ts apps/admin/src/pages/bff/admin/channels/bulk/shared.ts tests/unit/ts/admin-bulk-channel-import.test.ts` passed; rebuilt runtime containers with `docker compose --env-file .env.dev -f infra/docker/compose.yml -f infra/docker/compose.dev.yml up --build -d admin mcp`; health checks for admin and MCP passed; `pnpm test:mcp:compose -- --scenario=template-interest-channel-flows --skip-build` passed with artifact `/tmp/newsportal-mcp-http-deterministic-9b91e559-1776-4163-99bb-b6c4763219e0.json`; recent MCP request log proved `system_interests.create/update/read/list/archive/delete` all succeeded and `backend_422_like_errors=0` after the compose run.
- Current MCP system-interest list-field bugfix cleanup status: local dev DB test interest was repaired in place; existing backfill job `076145b9-7ce6-40e9-a0e8-d796a026b197` remains running and reached `5500/10970` at last read-back; compose stack remains running for product testing.

- Current discovery page bugfix scope: `.aidp/work.md`, `services/relay/src/migrations.ts`, local replay of existing `database/migrations/0042_discovery_core_schema_repair_replay.sql`, and admin/API runtime proof for `/discovery`.
- Current discovery page bugfix out of scope: discovery policy/yield tuning, schema/migrations, auth/session policy, source promotion semantics, MCP token behavior, production deploy, destructive runtime cleanup, or broad refactors.
- Current discovery page bugfix accepted plan: reproduce the `/discovery` render failure through the local admin runtime; confirm the API/schema drift behind `/maintenance/discovery/summary`; prevent schema-scoped migration smoke from falling through to public tables; replay the existing idempotent discovery schema repair migration locally; then prove the route renders and static checks pass.
- Current discovery page bugfix blueprint context checked: `.aidp/blueprint.md` UI/BFF boundary, Public API vs admin/operator API, Discovery capability, Auth/session boundary; `.aidp/contracts/discovery-agent.md` admin/operator proof expectations; `.aidp/contracts/auth-session-boundary.md` mutating admin BFF guard context.
- Current discovery page bugfix required proof: failing/reproducing evidence for `/discovery`; local schema repair proof; proof that migration smoke no longer mutates public schema; passing route/browser or curl proof after repair; `pnpm test:migrations:smoke`; `pnpm lint:ts`; `pnpm typecheck`; `git diff --check -- .aidp/work.md services/relay/src/migrations.ts`.
- Current discovery page bugfix proof passed: yes; admin/API logs showed `/maintenance/discovery/summary` failed with `psycopg.errors.UndefinedTable: relation "discovery_hypothesis_classes" does not exist`; `schema_migrations` listed all migrations as applied while `to_regclass` showed missing discovery core tables; local replay of `database/migrations/0042_discovery_core_schema_repair_replay.sql` initially restored public discovery tables, then `pnpm test:migrations:smoke` reproduced the table removal because schema-scoped migrations used `search_path = temp_schema, public`; `services/relay/src/migrations.ts` was patched to use only the requested temp schema for schema-scoped runs; `pnpm test:migrations:smoke` passed after the patch; replaying `0042_discovery_core_schema_repair_replay.sql` restored `discovery_hypothesis_classes`, `discovery_source_profiles`, `discovery_source_interest_scores`, `discovery_portfolio_snapshots`, and `discovery_feedback_events` in public; `curl -sS -i http://127.0.0.1:8000/maintenance/discovery/summary` returned 200; a temporary allowlist admin sign-in rendered `http://127.0.0.1:4322/discovery` with `Adaptive Discovery Agent`, `Dual-path discovery control plane`, `Dashboard`, `Profiles`, `Missions`, and `Recall`; `pnpm lint:ts`, `pnpm typecheck`, and `git diff --check -- .aidp/work.md services/relay/src/migrations.ts` passed. `pnpm typecheck` retained existing Astro hints but 0 errors.
- Current discovery page bugfix cleanup status: no destructive cleanup run; temporary proof admin user was deleted by the proof script; existing local compose stack remains running.

- Current bugfix scope: `.aidp/work.md`, `apps/admin/src/pages/channels/import.astro`, `apps/admin/src/components/BulkChannelImport.tsx`, `apps/admin/src/pages/bff/admin/channels/bulk/shared.ts`, `tests/unit/ts/admin-bulk-channel-import.test.ts`.
- Current bugfix out of scope: auth/session policy changes, admin role/allowlist changes, APP_SECRET/token signing semantics, schema, migrations, deploy/production settings, MCP bearer-token issuance or storage.
- Current bugfix accepted plan: reproduce by code inspection; the bulk import island runs preflight through `fetch` and final import through `form.submit()`, which bypasses the AdminShell submit listener that injects `adminActionToken`; pass route-scoped action tokens from the server-rendered page into the island, include the import token as a hidden field for form submission, attach the preflight token to JSON preflight fetches, and preserve admin action token fields through the bulk import custom payload reader before the shared guard validates them.
- Current bugfix blueprint context checked: `.aidp/blueprint.md` UI/BFF boundary, Public API vs admin/operator API, Auth/session boundary; `.aidp/contracts/auth-session-boundary.md` mutating admin BFF action-token expectations.
- Current bugfix required proof: targeted TS unit proof for bulk import/action-token rendering and payload-reader guard contract; `pnpm lint:ts`; `pnpm typecheck`; `git diff --check -- .aidp/work.md apps/admin/src/pages/channels/import.astro apps/admin/src/components/BulkChannelImport.tsx apps/admin/src/pages/bff/admin/channels/bulk/shared.ts tests/unit/ts/admin-bulk-channel-import.test.ts`.
- Current bugfix proof passed: yes; `pnpm unit_tests:ts -- admin-bulk-channel-import`, `pnpm lint:ts`, `pnpm typecheck`, and `git diff --check -- .aidp/work.md apps/admin/src/pages/channels/import.astro apps/admin/src/components/BulkChannelImport.tsx apps/admin/src/pages/bff/admin/channels/bulk/shared.ts tests/unit/ts/admin-bulk-channel-import.test.ts` passed. `pnpm typecheck` retained existing Astro hints but 0 errors.
- Current hardening scope: `.aidp/work.md`, `apps/admin/src/components/admin-form-submit.ts`, `apps/admin/src/components/AdminConfirmAction.tsx`, `apps/admin/src/components/AdminConfirmSubmitButton.tsx`, `tests/unit/ts/admin-action-kit.test.ts`.
- Current hardening accepted plan: replace duplicate `requestSubmit()`/native `form.submit()` fallback logic with a shared helper; when `requestSubmit()` exists, preserve current behavior; when only native `form.submit()` remains, dispatch a cancelable bubbling `submit` event first so the `AdminShell` submit listener can inject `adminActionToken`.
- Current hardening required proof: targeted TS unit proof for fallback event dispatch behavior; `pnpm lint:ts`; `pnpm typecheck`; `git diff --check -- .aidp/work.md apps/admin/src/components/admin-form-submit.ts apps/admin/src/components/AdminConfirmAction.tsx apps/admin/src/components/AdminConfirmSubmitButton.tsx tests/unit/ts/admin-action-kit.test.ts`.
- Current hardening proof passed: yes; `pnpm unit_tests:ts -- admin-action-kit`, `pnpm lint:ts`, `pnpm typecheck`, and `git diff --check -- .aidp/work.md apps/admin/src/components/admin-form-submit.ts apps/admin/src/components/AdminConfirmAction.tsx apps/admin/src/components/AdminConfirmSubmitButton.tsx tests/unit/ts/admin-action-kit.test.ts` passed. `pnpm typecheck` retained existing Astro hints but 0 errors.

- Current MCP reindex hardening scope: `.aidp/work.md`, `.aidp/contracts/mcp-control-plane.md`, `services/mcp/src/context.ts`, `services/mcp/src/resources.ts`, `services/mcp/src/tools.ts`, `services/mcp/src/tools/sequences-tools.ts`, `services/mcp/src/operating-intelligence.ts`, `tests/unit/ts/mcp-control-plane.test.ts`, `infra/scripts/lib/mcp-http-scenarios.mjs`.
- Current MCP reindex hardening out of scope: changing worker selection semantics, schema/migrations, production reindex execution, admin UI semantics unless needed for MCP proof.
- Current MCP reindex hardening accepted plan: use the user-provided MCP Reindex Intent Hardening plan; restrict reindex MCP schema/handler to supported index/jobKind enums; make `backfill` defaults match admin UI selection replay; clarify tool descriptions/resources/instructions so historical/current-interest selection replay maps to `maintenance.reindex.request jobKind=backfill` and content-analysis backfill is not treated as selection replay; add structured read-back hints/warnings; detect stale selected/pass_through rows with no system-criterion `interest_filter_results`; cover invalid arguments/defaults/descriptions and scenario static behavior with targeted tests.
- Current MCP reindex hardening blueprint context checked: `.aidp/blueprint.md` Runtime-поверхности, Operator/admin control plane, System selection vs personalization, PostgreSQL vs derived state; `.aidp/contracts/mcp-control-plane.md` Tool/resource contract and proof expectations.
- Current MCP reindex hardening required proof: `pnpm unit_tests:ts -- mcp-control-plane`; `node --check infra/scripts/lib/mcp-http-scenarios.mjs`; `pnpm lint:ts`; `pnpm typecheck`; `git diff --check -- .aidp/work.md .aidp/contracts/mcp-control-plane.md services/mcp/src/context.ts services/mcp/src/resources.ts services/mcp/src/tools.ts services/mcp/src/tools/sequences-tools.ts services/mcp/src/operating-intelligence.ts tests/unit/ts/mcp-control-plane.test.ts infra/scripts/lib/mcp-http-scenarios.mjs`.
- Current MCP reindex hardening proof passed: yes; `pnpm unit_tests:ts -- mcp-control-plane` passed 331/331 TS tests including invalid reindex enum rejection, backfill default option storage, metadata disambiguation, and content-analysis warning coverage; `node --check infra/scripts/lib/mcp-http-scenarios.mjs` passed; `pnpm lint:ts` passed; `pnpm typecheck` passed with existing Astro hints and 0 errors; `pnpm test:mcp:compose` passed with artifact `/tmp/newsportal-mcp-http-deterministic-2fdf5fce-9440-4d2c-a5cf-57973111b373.json`; `git diff --check -- .aidp/work.md .aidp/contracts/mcp-control-plane.md services/mcp/src/context.ts services/mcp/src/resources.ts services/mcp/src/tools.ts services/mcp/src/tools/sequences-tools.ts services/mcp/src/operating-intelligence.ts tests/unit/ts/mcp-control-plane.test.ts infra/scripts/lib/mcp-http-scenarios.mjs` passed.
- Current MCP reindex hardening proof note: the first compose attempt intentionally exposed that a full historical backfill over the already-populated local DB (`4609` articles) can exceed the sequence task timeout; the compose scenario now proves HTTP backfill lifecycle with a single existing `docId`, while unit coverage separately proves omitted `backfill` defaults.
- Current MCP reindex hardening cleanup status: compose stack was already running and remains running after `pnpm test:mcp:compose`; the failed wide backfill attempt left a local runtime `reindex_jobs` row in `running` state from the proof attempt, but no repository source cleanup is required.

- Current reindex cancellation scope: `.aidp/work.md`, `database/migrations/*`, `packages/control-plane/src/*`, `apps/admin/src/pages/reindex.astro`, `apps/admin/src/components/LiveReindexJobsSection.tsx`, `apps/admin/src/lib/live-updates.ts`, `apps/admin/src/lib/server/live-updates.ts`, `apps/admin/src/pages/bff/admin/reindex.ts`, `services/api/app/content_analysis_backfill.py`, `services/mcp/src/tools/sequences-tools.ts`, `services/workers/app/reindex_processor.py`, `services/workers/app/reindex_backfill.py`, `services/workers/app/reindex_backfill_runtime.py`, targeted TS/Python tests and relevant proof harnesses if needed.
- Current reindex cancellation out of scope: production deployment, hard-killing BullMQ/worker processes, broad queue redesign, changing selection semantics beyond replay cancellation, notification policy, auth/session policy, or unrelated MCP/admin changes.
- Current reindex cancellation accepted plan: implement same-lane `cancellation_key` supersession for new reindex/content-analysis requests, add manual admin Cancel for queued/running jobs, add `cancel_requested`/`cancelled` reindex statuses with cooperative worker exits between batches, repair stale `running` rows when sequence evidence already failed, and expose truthful cancelling/cancelled state in admin live jobs.
- Current reindex cancellation blueprint context checked: `.aidp/blueprint.md` Runtime-поверхности, Операционная схема, PostgreSQL vs derived state, UI/BFF boundary, Public API vs admin/operator API, Relay vs workers, Async pipeline routing risk; `.aidp/engineering.md` async/state/database discipline; `.aidp/verification.md` migration, relay/queue, worker, admin UI proof expectations.
- Current reindex cancellation required proof: targeted TS unit proof for control-plane/admin/MCP cancellation semantics and UI cancellability; targeted Python unit proof for API/worker cancellation and timeout handling; `pnpm test:migrations:smoke` if feasible; `pnpm lint:ts`; `pnpm typecheck`; relevant compose proof if runtime remains available; `git diff --check --` for touched paths.
- Current reindex cancellation proof passed: yes; targeted TS proof passed with `node --import tsx --test tests/unit/ts/reindex-control-plane.test.ts tests/unit/ts/admin-live-updates.test.ts tests/unit/ts/admin-reindex-live-updates.test.ts tests/unit/ts/mcp-control-plane.test.ts`; targeted Python proof passed with `PYTHONPATH=. python -m unittest tests.unit.python.test_reindex_backfill_progress tests.unit.python.test_api_discovery_management tests.unit.python.test_interest_auto_repair`; `pnpm lint:ts`, `pnpm lint:py`, `pnpm typecheck`, `pnpm test:migrations:smoke`, and `git diff --check --` for touched paths passed. `pnpm typecheck` retained existing Astro hints but 0 errors.
- Current reindex cancellation cleanup status: no new cleanup obligation; existing local compose stack and stale runtime rows were pre-existing audit evidence, and no destructive runtime cleanup was run.

- In scope: `.aidp/work.md`, `.aidp/contracts/mcp-control-plane.md`, `docs/product/operator/mcp/README.md`, `packages/control-plane/src/index.ts`, `packages/control-plane/src/channel-bulk-onboarding.ts`, `apps/admin/src/pages/bff/admin/channels/bulk/shared.ts`, `services/mcp/src/context.ts`, `services/mcp/src/resources.ts`, `services/mcp/src/prompts.ts`, `services/mcp/src/tools/channels-tools.ts`, `infra/scripts/lib/mcp-http-scenarios.mjs`, and targeted bulk-channel proof files if existing harnesses require them.
- Out of scope: token issuance UI changes, auth/session policy changes, schema, migrations, deploy/production settings, and storing real MCP token secrets in repository files.
- Allowed paths: `.aidp/work.md`, `.aidp/contracts/mcp-control-plane.md`, `docs/product/operator/mcp/README.md`, `packages/control-plane/src/index.ts`, `packages/control-plane/src/channel-bulk-onboarding.ts`, `apps/admin/src/pages/bff/admin/channels/bulk/shared.ts`, `services/mcp/src/context.ts`, `services/mcp/src/resources.ts`, `services/mcp/src/prompts.ts`, `services/mcp/src/tools/channels-tools.ts`, `infra/scripts/lib/mcp-http-scenarios.mjs`, and targeted bulk-channel proof files if existing harnesses require them.
- Accepted plan summary: stage 14 implements the user-provided MCP Bulk Source Onboarding plan: move admin bulk-channel planning/execution into the shared control-plane package, keep the admin import flow on that shared contract, add MCP `channels.bulk_onboard.plan/apply/verify`, classify per-source create/update/duplicate/mismatch/override states, require stale-plan and overwrite confirmation guards, and prove mixed-source onboarding through MCP compose.
- Blueprint context checked: `.aidp/blueprint.md` Runtime-поверхности, Operator/admin control plane capability, Runtime boundary, Product docs vs AIDP runtime; `.aidp/contracts/mcp-control-plane.md` Transport/Auth proof expectations.
- Required proof: `pnpm --filter @newsportal/control-plane typecheck`; `pnpm --filter @newsportal/admin typecheck`; `pnpm --filter @newsportal/mcp typecheck`; `node --check infra/scripts/lib/mcp-http-scenarios.mjs`; `pnpm lint:ts`; `pnpm test:mcp:compose`; `git diff --check --` for touched paths.
- Proof passed: yes; `pnpm --filter @newsportal/control-plane typecheck`, `pnpm --filter @newsportal/admin typecheck`, `pnpm --filter @newsportal/mcp typecheck`, `node --check infra/scripts/lib/mcp-http-scenarios.mjs`, `pnpm lint:ts`, `pnpm test:mcp:compose`, and `git diff --check -- .aidp/work.md .aidp/contracts/mcp-control-plane.md docs/product/operator/mcp/README.md packages/control-plane/src/index.ts packages/control-plane/src/channel-bulk-onboarding.ts apps/admin/src/pages/bff/admin/channels/bulk/shared.ts services/mcp/src/context.ts services/mcp/src/resources.ts services/mcp/src/tools/channels-tools.ts infra/scripts/lib/mcp-http-scenarios.mjs` passed. MCP compose artifact: `/tmp/newsportal-mcp-http-deterministic-83bdc9ef-2e08-4cca-9f01-2337bbf8271a.json`; markdown artifact: `/tmp/newsportal-mcp-http-deterministic-83bdc9ef-2e08-4cca-9f01-2337bbf8271a.md`.
- Admin sign-in form-only proof completed: `rg -n "Admin operations|Manage sources, templates, and moderation without losing your place|Dedicated CRUD flows|Safer operations|Context preserved" apps/admin/src/pages/sign-in.astro` returned no matches; `pnpm lint:ts` passed; `pnpm typecheck` passed with existing Astro hints and 0 errors; `git diff --check -- apps/admin/src/pages/sign-in.astro .aidp/work.md` passed.
- Web card-grid page-size proof completed: `rg -n "DEFAULT_PAGE_SIZE|WEB_CARD_GRID_PAGE_SIZE|pageSize:" apps/web/src/pages/index.astro apps/web/src/pages/matches.astro apps/web/src/pages/following.astro apps/web/src/lib/view-helpers.ts` confirmed the three 3-column card-grid pages use `WEB_CARD_GRID_PAGE_SIZE`; `pnpm lint:ts` passed; `pnpm typecheck` passed with existing Astro hints and 0 errors.
- Non-native admin confirmation proof completed: `rg -n "window\\.confirm|window\\.alert|window\\.prompt|\\bconfirm\\(|\\balert\\(|\\bprompt\\(" apps packages services infra` returned no real app/runtime matches; broader scan only finds intentional test XSS payload strings plus the newly documented AIDP rule. `pnpm lint:ts`, `pnpm typecheck`, and `git diff --check --` passed.
- MCP token admin UX proof completed: `pnpm unit_tests:ts -- mcp-control-plane` passed 323/323 TS tests including revoked-only delete guard and audit row coverage; `pnpm lint:ts` passed; `pnpm typecheck` passed with existing Astro hints and 0 errors.
- Runtime availability proof completed: `pnpm dev:mvp:internal:no-build` completed against the existing local compose stack; `curl -sS http://127.0.0.1:4322/api/health` returned admin `status:"ok"`; `curl -sS -I http://127.0.0.1:4322/automation/mcp` returned `302` to `/sign-in?next=%2Fautomation%2Fmcp` as expected without an admin session.
- Cleanup status: compose stack is intentionally left running so the admin change can be tried locally; no destructive cleanup was run.
- Product total-live proof completed: `pnpm test:product:total-live:compose -- --skip-diagnostics --skip-stack-build` run id `38128383` passed with `runtimeVerdict=pass`, `finalVerdict=pass`; artifact `/tmp/newsportal-product-total-live-38128383.json` and `.md`; strict nested mega-flow artifact `/tmp/newsportal-product-mega-flow-695b3c2b.json` passed with `runtimeVerdict=pass`, `yieldVerdict=pass`, `finalVerdict=pass`.
- Product total-live coverage evidence: strict A/B/C live selected product proof passed; providers/channel auth/website-admin/automation-admin/MCP/Web viewport/Web UI audit/relay phase 3/4/5/ingest/normalize-dedup/interest compile/criterion compile/cluster-match-notify/embed/reindex-backfill/LLM-budget-stop required commands passed; RSS and Website provider evidence passed; API and Email IMAP deterministic fixtures passed and external live lanes were recorded as `not_applicable_with_reason` because no real external target is available.
- Product total-live live-selected evidence: Example A selected `Boulevard: Staff Product Designer, Platform` from `We Work Remotely — Programming`; Example B selected `How GitHub uses eBPF to improve deployment safety` from `GitHub Blog`; Example C selected `Why Tokyo is the most important tech destination of 2026` from `TechCrunch — Startups`; all had `finalDecision=selected` and `matchedFilterCount=1`.
- Product total-live fixes applied during proof: UI button audit seed now retries Postgres deadlock/serialization conflicts, waits for worker-stable article state before deterministic selection seeding, re-marks the admin moderation fixture as a recent failure immediately before the admin route check, and viewport proof waits longer with diagnostics for criteria processing; reindex-backfill smoke now isolates criterion scope during the reindex replay so cardinality proof remains deterministic in a live populated DB.
- Product total-live proof not run: full diagnostic-mode `pnpm test:product:total-live:compose` was not run after the required-live pass; diagnostic lanes remain separately classifiable by the harness and can be run when the long live-internet matrix is desired.
- Product mega-flow proof completed: latest strict `pnpm test:product:mega-flow:compose` run id `093f6007` passed with `runtimeVerdict=pass`, `yieldVerdict=pass`, `finalVerdict=pass`; artifact `/tmp/newsportal-product-mega-flow-093f6007.json` and `.md`; child live discovery artifact `/tmp/newsportal-product-mega-flow-discovery-6cbd0bec.json` passed for Examples A/B/C; child MCP artifact `/tmp/newsportal-mcp-http-deterministic-215cb065-494e-452d-b2e7-b268a50e102d.json` passed.
- Product mega-flow live-selected evidence: Example A selected live current-window article `Show HN: AI voice screens for hiring managers to save time` from `Hacker News — Ask HN: Who is hiring?`; Example B selected live article `How GitHub uses eBPF to improve deployment safety` from `GitHub Blog`; Example C selected live article `Why Tokyo is the most important tech destination of 2026` from `TechCrunch — Startups`. All three had `finalDecision=selected` and `matchedFilterCount=1`.
- Product mega-flow coverage evidence: A/B/C admin-managed profile truth passed; live graph+recall discovery passed for all three; RSS, Website, API and Email IMAP provider evidence passed; deterministic rejected/filter/duplicate buckets passed; strict live-selected article evidence passed; sequence resource/article/enrichment/digest/cancel/fail/retry buckets passed; Web/Admin/MCP surfaces passed.
- Separate required discovery proofs completed: `pnpm test:discovery:examples:compose` run id `84c5ef4c` passed with `runtimeVerdict=pass`, `yieldVerdict=pass`, `finalVerdict=pass`; `pnpm test:discovery:yield:compose` run id `5f14543e` passed with 3/3 passing runs for Examples A, B and C.
- Static/local proof completed: `node --check infra/scripts/test-product-mega-flow.mjs`, `node --check infra/scripts/lib/product-mega-flow-proof.mjs`, `pnpm unit_tests:ts` (318/318), targeted `PYTHONPATH=. python -m unittest tests.unit.python.test_final_selection tests.unit.python.test_selection_profiles` (22/22), `pnpm lint:ts`, and `pnpm typecheck` passed; `typecheck` retained existing Astro hints but 0 errors.
- Runtime proof completed: `pnpm test:discovery:mega:compose` run id `b57b55db` wrote `/tmp/newsportal-live-discovery-domain-matrix-b57b55db.json` and `.md`; all 27 child runs completed; `runtimeVerdict=pass`, `yieldVerdict=weak`, `finalVerdict=yield_weak`.
- Runtime proof residuals: pass 3/3 for `weworkremotely.com`, `infoq.com`, `github.blog`, `sam.gov`; weak 0/3 for `hnrss.org` with `target_domain_generation_problem`; weak 0/3 for `remoteok.com`, `blog.cloudflare.com`, `ted.europa.eu`, `contractsfinder.service.gov.uk` with `target_domain_review_policy_problem`.
- Full complex proof run: `pnpm test:discovery:admin:compose` passed; `pnpm test:discovery:examples:compose` run id `1eb94981` passed; `pnpm test:discovery:mega:compose` run id `5f4ce70f` completed all 27 child runs with `runtimeVerdict=pass`, `yieldVerdict=weak`, `finalVerdict=yield_weak`; `pnpm test:discovery:yield:compose` run id `c82c5137` passed 3/3 example repeats; `pnpm test:mcp:compose` passed with artifact `/tmp/newsportal-mcp-http-deterministic-d335de54-4f3e-45c2-a716-5fdc2a5bf2f6.json`; `pnpm test:channel-auth:compose` initially exposed localhost crawl-policy cache contamination, then passed after cleanup and a pre-clean test isolation patch; `pnpm test:providers:compose`, `pnpm test:web:viewports`, and `pnpm test:website:compose` passed.
- Cleanup status: compose stack stopped with `pnpm dev:mvp:internal:down`; final `docker ps --format '{{.Names}}'` was empty; current proof artifacts include `/tmp/newsportal-product-total-live-38128383.*`, `/tmp/newsportal-product-mega-flow-695b3c2b.*`, `/tmp/newsportal-product-mega-flow-discovery-ad8f7e3c.*`, `/tmp/newsportal-product-mega-flow-093f6007.*`, `/tmp/newsportal-product-mega-flow-discovery-6cbd0bec.*`, `/tmp/newsportal-live-discovery-examples-84c5ef4c.*`, `/tmp/newsportal-live-discovery-yield-proof-5f14543e.*`, and `/tmp/newsportal-mcp-http-deterministic-215cb065-494e-452d-b2e7-b268a50e102d.*`.

### Recent completed work

- Current resilient discovery v3 live calibration stage scope: `.aidp/work.md`, `package.json`, `infra/scripts/test-live-discovery-calibration.mjs`, `infra/scripts/lib/discovery-live-calibration.mjs`, and targeted TS unit tests for calibration policies.
- Current resilient discovery v3 live calibration stage out of scope: social/API/email ingress, production/shared DB execution, paid provider use without configured keys, automatic threshold/prompt/policy mutation, and git commit/stage/push.
- Current resilient discovery v3 live calibration accepted plan: add a separate bounded live web/RSS/Atom/website calibration contour with diagnostic, strict acceptance and soak commands; create run-scoped v3 targets/runs, execute them through `discovery.v3.run`, read back coverage/hypotheses/endpoints/contracts/claims/negative-evidence/provider-health/LLM decisions, emit JSON/Markdown artifacts, create replay-fixture pointers, and classify tuning recommendations without applying them.
- Current resilient discovery v3 live calibration blueprint context checked: `.aidp/blueprint.md` Discovery acquisition, PostgreSQL source-of-truth, Runtime surfaces, Task engine/worker boundary; `.aidp/contracts/discovery-agent.md`; `.aidp/contracts/test-access-and-fixtures.md`; `.aidp/verification.md` discovery live/external-provider proof notes.
- Current resilient discovery v3 live calibration required proof: targeted TS unit proof for case validation, verdict policy, tuning recommendation mapping and replay fixture preservation; `node --check` for new harness scripts; `pnpm lint:ts`; `pnpm typecheck`; `git diff --check`; run live compose calibration/acceptance if local provider/network conditions allow, otherwise record explicit residual.
- Current resilient discovery v3 live calibration proof passed: yes for source-level harness and a bounded live guard smoke; `node --check infra/scripts/lib/discovery-live-calibration.mjs`, `node --check infra/scripts/test-live-discovery-calibration.mjs`, `pnpm unit_tests:ts -- discovery-live-calibration` (full TS suite 345/345), `pnpm lint:ts`, `pnpm check:env-sync`, `pnpm typecheck`, `pnpm lint:py`, `pnpm test:migrations:smoke`, and `git diff --check` passed. A first live smoke exposed that sequence runs can terminate as `completed`, so the harness terminal status set was corrected; rerun with `DISCOVERY_ENABLED=1 DISCOVERY_V3_LIVE_EXECUTION=1 DISCOVERY_SEARCH_PROVIDERS=ddgs node infra/scripts/test-live-discovery-calibration.mjs --mode=calibration --cases=negative_evidence_provider_health` passed 3/3 guard iterations with runtime/quality/final verdict `pass`, artifact `/tmp/discovery-live-calibration-085e31ea.json` and `.md`, replay suite `98ca7ed5-4b58-49ee-a922-66df82dbd232`, eval run `08908764-e5e8-4952-afd0-7ddd8800f89f`.
- Current resilient discovery v3 live calibration residual gates: the full `pnpm test:discovery:live-calibration:compose`, strict `pnpm test:discovery:live-acceptance:compose`, and manual/nightly `pnpm test:discovery:live-soak:compose` commands were added but not run end-to-end in this pass because they intentionally execute multiple real web/RSS/Atom/website flows; use the guard-smoke artifact above as the route proof and run the full/strict suites for product polishing before threshold/prompt/policy tuning.

- Current resilient discovery v3 cutover source scope: `database/migrations/0052_resilient_discovery_rebuild.sql`, `.aidp/contracts/discovery-agent.md`, `.aidp/contracts/independent-recall-discovery.md`, `docs/product/operator/discovery/resilient-discovery.md`, `docs/product/README.md`, `infra/scripts/relay/test-migrations.ts`, `services/workers/app/discovery_v3_*.py`, `services/api/app/discovery_v3_api.py`, `services/api/app/routes/discovery_routes.py`, `packages/sdk/src/index.ts`, `services/mcp/src/tools/discovery/v3-tools.ts`, `services/mcp/src/tools/discovery/index.ts`, `services/mcp/src/resources.ts`, `services/mcp/src/prompts.ts`, `apps/admin/src/pages/discovery.astro`, and targeted Python/API/admin/MCP/migration tests.
- Current resilient discovery v3 cutover out of scope: applying the destructive `0052` migration to a shared/runtime database, deleting old worker runtime modules, deleting old MCP/admin source files that are no longer exported, production deploy, real social-provider scraping, or disabling old runtime data without explicit operator approval.
- Current resilient discovery v3 cutover accepted plan: land the source-level v3 domain model and guards before runtime deletion: chunked legacy archive migration, provider registry/health, bounded Constructive Skeptic loop, source evidence contracts/probation, negative evidence, hidden-signal claims and controls, eval replay tables/policies, v3 repository/graph/coverage/hypothesis/orchestrator skeleton, v3 API routes, v3 MCP tools/resources/prompts, SDK v3 methods, and a v3 admin operator workspace showing why-found/why-not-promoted/missing-evidence, contracts, claims, cooldowns and provider health.
- Current resilient discovery v3 cutover proof passed: yes for source-level gates; `PYTHONPATH=. python -m unittest tests.unit.python.test_discovery_v3_policy tests.unit.python.test_discovery_v3_stability tests.unit.python.test_api_discovery_v3_cutover` passed 24/24; `pnpm test:migrations:smoke` passed and verified v3 discovery tables/constraints plus retained ingestion tables; `pnpm --filter @newsportal/sdk typecheck`, `pnpm --filter @newsportal/mcp typecheck`, `pnpm --filter @newsportal/admin typecheck`, `pnpm lint:py`, `pnpm lint:ts`, and `git diff --check` passed.
- Current resilient discovery v3 cutover continuation proof passed: yes; legacy SDK discovery mission/class/profile/recall/candidate methods were removed in favor of v3 target/run/endpoint/contract/claim/provider-health/eval methods; old MCP discovery read/write tool exports were retired and v3 tool/resource/prompt expectations now drive tests; old admin discovery BFF POST now requires the signed discovery action token and returns `410 Gone` instead of proxying old mission/recall routes; `operator.report.verify` accepts v3 `targetIds` and reports queued/running runs, manual-review endpoints and probation contracts. Proof passed with `pnpm unit_tests:ts -- discovery-admin mcp-control-plane mcp-http-doc-parity mcp-http-live-diagnostics` (full TS suite 352/352), `PYTHONPATH=. python -m unittest tests.unit.python.test_discovery_v3_policy tests.unit.python.test_discovery_v3_stability tests.unit.python.test_api_discovery_v3_cutover` (24/24), `pnpm --filter @newsportal/sdk typecheck`, `pnpm --filter @newsportal/mcp typecheck`, `pnpm --filter @newsportal/admin typecheck`, `pnpm lint:ts`, `pnpm lint:py`, `pnpm test:migrations:smoke`, and `git diff --check`.
- Current resilient discovery v3 promotion integration proof passed: yes; v3 endpoint promotion now delegates through the endpoint-aware `PostgresSourceRegistrarAdapter` instead of API-local raw SQL, and the registrar creates `source_channels`, `source_channel_runtime_state`, `outbox_events`, `discovery_source_contracts`, endpoint registration state and `discovery_actions` in one promotion path. Endpoint promotion config starts with `trustStage=probation`, `coverageContribution=0.25`, `downstreamWeight=0.3`, operator config, tags and the Source Evidence Contract; contract evaluation also syncs trust/evidence contract state back into `source_channels.config_json.discovery`. Proof passed with `PYTHONPATH=. python -m unittest tests.unit.python.test_discovery_fetchers_website_probe tests.unit.python.test_discovery_v3_policy tests.unit.python.test_discovery_v3_stability tests.unit.python.test_api_discovery_v3_cutover` (29/29), `pnpm lint:py`, `pnpm test:migrations:smoke`, and `git diff --check`.
- Current resilient discovery v3 coverage/runtime-guard proof passed: yes; deterministic coverage now dedupes source identity before counting strong sources, stores duplicate counts, uses `coverageContribution` for partial probation coverage, and keeps probation sources out of strong coverage. API refresh now uses the same coverage engine instead of a separate row-count implementation. The v3 orchestrator now filters planned hypotheses through negative-evidence cooldowns and provider-health circuit breakers before inserting them, degrading provider budgets instead of treating provider failures as hypothesis failures. Proof passed with `PYTHONPATH=. python -m unittest tests.unit.python.test_api_discovery_v3_cutover tests.unit.python.test_discovery_v3_stability tests.unit.python.test_discovery_v3_policy tests.unit.python.test_discovery_fetchers_website_probe` (31/31), `pnpm lint:py`, and `git diff --check`.
- Current resilient discovery v3 hidden-claim follow-up proof passed: yes; confirmed hidden-signal claims with control comparison now generate only direct `web_search` follow-up hypotheses, while hidden claims without control comparison generate no direct follow-up. The v3 orchestrator loads confirmed claims for the target and appends claim-backed direct-source hypotheses before runtime guard filtering, preserving the rule that hidden/social evidence cannot directly promote `source_channels`. Proof passed with `PYTHONPATH=. python -m unittest tests.unit.python.test_discovery_v3_stability tests.unit.python.test_api_discovery_v3_cutover tests.unit.python.test_discovery_v3_policy` (28/28) and `pnpm lint:py`.
- Current resilient discovery v3 replay-eval proof passed: yes; fixture replay evaluation now measures source precision/recall/noise/reject recall/cost plus hidden-claim precision/recall, and `POST /maintenance/discovery/eval-suites/{eval_suite_id}/run` records a completed `discovery_eval_runs` row from stored eval case fixtures instead of a placeholder queued row. Proof passed with `PYTHONPATH=. python -m unittest tests.unit.python.test_discovery_v3_stability tests.unit.python.test_api_discovery_v3_cutover tests.unit.python.test_discovery_v3_policy tests.unit.python.test_discovery_fetchers_website_probe` (34/34), `pnpm lint:py`, and `git diff --check`.
- Current resilient discovery v3 self-healing/task-plugin/execution-bridge proof passed: yes; added bounded self-healing diagnosis/repair rows so provider/probe failures, coverage stagnation, endpoint-sweep misses, hidden-signal noise, duplicate pressure and stale sources map to queued `discovery_repairs` without treating provider failures as hypothesis failures. Added v3 task-engine plugins for URL/domain clustering, source-directory extraction with outbound-domain follow-up hypotheses, endpoint sweep with localized patterns, endpoint scoring and action decision, registered alongside existing plugins for non-destructive source-level cutover. Added a network-free fixture execution bridge that turns referee-accepted hypotheses into clustered domains, source-directory follow-ups, endpoint candidates, scored actions and negative evidence for no-result branches; the orchestrator now persists fixture bridge endpoints/edges/negative evidence when run fixtures are supplied, otherwise it explicitly leaves live provider execution disabled until operator approval. Proof passed with `PYTHONPATH=. python -m unittest tests.unit.python.test_task_engine_discovery_plugins tests.unit.python.test_task_engine_pipeline_plugins tests.unit.python.test_api_sequence_agent tests.unit.python.test_discovery_v3_stability tests.unit.python.test_api_discovery_v3_cutover tests.unit.python.test_discovery_v3_policy tests.unit.python.test_discovery_fetchers_website_probe` (73/73), `pnpm lint:py`, `pnpm test:migrations:smoke`, and `git diff --check`.
- Current resilient discovery v3 MCP/SDK scenario cutover proof passed: yes; extended SDK and MCP v3 coverage for endpoint/domain/action/contract/claim/negative-evidence/provider-health/identity/eval reads and writes, replaced MCP HTTP discovery operator scenarios and doc-parity expectations with v3 targets/runs/endpoints/contracts/claims/provider-health/eval flows, and removed legacy discovery mission/profile/class/recall/candidate tool references from the exercised MCP/SDK scenario surfaces. Proof passed with `pnpm unit_tests:ts -- mcp-control-plane mcp-http-doc-parity mcp-http-live-diagnostics` (352/352), `pnpm --filter @newsportal/sdk typecheck`, `pnpm --filter @newsportal/mcp typecheck`, `pnpm lint:ts`, `node --check infra/scripts/lib/mcp-http-scenarios.mjs`, `node --check infra/scripts/lib/mcp-http-doc-parity.mjs`, `PYTHONPATH=. python -m unittest tests.unit.python.test_task_engine_discovery_plugins tests.unit.python.test_task_engine_pipeline_plugins tests.unit.python.test_api_sequence_agent tests.unit.python.test_discovery_v3_stability tests.unit.python.test_api_discovery_v3_cutover tests.unit.python.test_discovery_v3_policy tests.unit.python.test_discovery_fetchers_website_probe` (73/73), `pnpm lint:py`, `pnpm test:migrations:smoke`, `git diff --check`, and `rg -n "discovery\\.(profiles|classes|missions|recall|candidates|source_profiles|source_interest_scores|feedback|costs)" infra/scripts/lib/mcp-http-scenarios.mjs infra/scripts/lib/mcp-http-doc-parity.mjs services/mcp/src/tools/discovery packages/sdk/src/index.ts` returning no matches.
- Current resilient discovery v3 MCP prompt/resource cleanup proof passed: yes; MCP session/yield/tuning/review prompts now describe v3 targets, coverage, endpoints, contracts, claims, provider health, negative evidence, eval replay and probation instead of old mission/recall/profile planning, and operator resources now route discovery onboarding through target coverage plus endpoint contract review. `operator.report.verify` schema no longer advertises old discovery `profileIds`/`missionIds`/`recallMissionIds`; discovery health guidance now reports rejected/duplicate endpoints instead of recall candidates. Proof passed with `pnpm unit_tests:ts -- mcp-control-plane mcp-http-doc-parity mcp-http-live-diagnostics` (352/352), `pnpm --filter @newsportal/mcp typecheck`, `pnpm lint:ts`, `node --check infra/scripts/lib/mcp-http-scenarios.mjs`, `node --check infra/scripts/lib/mcp-http-doc-parity.mjs`, `git diff --check`, and `rg -n "discovery\\.mission|discovery\\.recall|discovery\\.candidate|discovery\\.profile|profiles, missions|recall missions|recall candidates|mission/candidate|profileId|missionId|recallMissionId" services/mcp/src/prompts.ts services/mcp/src/resources.ts services/mcp/src/context.ts services/mcp/src/tools.ts tests/unit/ts/mcp-control-plane.test.ts` returning no matches.
- Current resilient discovery v3 API dependency decoupling proof passed: yes; FastAPI route registration already uses the v3 discovery router, and `services/api/app/route_deps.py` no longer requires legacy discovery mission/recall/candidate/profile/class functions to construct the API app context. Old Python legacy modules are still present for the explicit approval-gated cleanup phase, but they are no longer part of the route dependency registry. Proof passed with `PYTHONPATH=. python -m unittest tests.unit.python.test_api_discovery_v3_cutover tests.unit.python.test_api_sequence_agent` (13/13), `pnpm lint:py`, and `rg -n "discovery_(mission|recall|candidate|profile|class|source|portfolio|feedback|cost)" services/api/app/route_deps.py services/api/app/routes/discovery_routes.py` returning no matches.
- Current resilient discovery v3 local runtime cutover proof passed: yes; after the recorded operator approval, local `schema_migrations` was checked, `pnpm test:migrations:smoke` passed, `pnpm db:migrate` applied `0052_resilient_discovery_rebuild.sql`, and DB read-back verified old mission/candidate/recall tables gone, retained ingestion tables present, v3 tables/views present, and `discovery_legacy_archive_batches` populated for legacy archive batches. Runtime proof promoted a v3 RSS endpoint through `PostgresSourceRegistrarAdapter`, created `source_channels`, `source_channel_runtime_state`, `outbox_events`, `discovery_source_contracts`, endpoint registration state and `discovery_actions`, then contract evaluation moved trust from `probation/0.25/0.3` to `active/1.0/1.0`. Bounded live execution is now available behind explicit run summary/env enablement; provider failures update `discovery_provider_health` and repairs, not hypothesis failure evidence. Old admin mission/recall/candidate/profile/source pages were removed, the admin compose acceptance was rewritten for v3 targets/endpoints/contracts/claims/negative-evidence/provider-health/eval surfaces, and `pnpm test:discovery:admin:compose` passed. A v3 domain list runtime bug found by MCP compose was fixed by filtering `discovery_domain_inventory` with `first_seen_target_id`; regression test added. Final proof passed with `PYTHONPATH=. python -m unittest tests.unit.python.test_api_discovery_v3_cutover`, `pnpm lint:py`, `pnpm lint:ts`, `pnpm typecheck`, `node --check infra/scripts/test-discovery-admin-flow.mjs`, `pnpm test:discovery:admin:compose`, `pnpm test:mcp:compose -- --scenario=discovery-operator-flows` (artifact `/tmp/newsportal-mcp-http-deterministic-6698b273-37d8-4357-8c8a-8599d1402f06.*`), and `git diff --check`.
- Current resilient discovery v3 approval-gated cleanup proof passed: yes; after the local runtime cutover proof, the remaining active legacy discovery runtime references were removed from FastAPI registration and status constants, old mission/recall/candidate/profile/class/source-quality API helper modules were deleted, unused legacy worker planning/execution/evaluation/recall/candidate/source-quality/policy/cost modules were deleted, old unit tests tied to removed legacy modules were retired, and compose/MCP/product discovery proof harnesses were moved to v3 targets/runs/endpoints/contracts/claims/provider-health/eval read-backs. `services/api/app/main.py` now registers only the v3 discovery route family; `services/api/app/route_deps.py` remains decoupled from legacy discovery functions; MCP discovery HTTP scenarios no longer exercise old mission/recall/candidate tools; live discovery/domain/yield/product proof formatters now report v3 endpoints instead of legacy candidates. Proof passed with `python -m ruff check services/api/app/main.py services/api/app/status_constants.py`, `PYTHONPATH=. python -c "import services.api.app.main as m; print('ok', len(m.app.routes))"`, `pnpm lint:py`, `pnpm lint:ts`, `pnpm typecheck`, `PYTHONPATH=. python -m unittest discover -s tests/unit/python -p 'test_*.py'` (319/319), `pnpm unit_tests:ts -- mcp-control-plane discovery-admin admin-operator-surfaces mcp-http-doc-parity mcp-http-live-diagnostics` (340/340), `pnpm test:migrations:smoke`, `DISCOVERY_ENABLED=1 PYTHONPATH=. python -m infra.scripts.workers.smoke discovery-enabled`, `pnpm test:discovery:admin:compose`, `pnpm test:discovery:examples:compose` (artifact `/tmp/newsportal-live-discovery-examples-bb566d77.json` after UUID fix also used by nonregression), `pnpm test:discovery:yield:compose` (artifact `/tmp/newsportal-live-discovery-yield-proof-33d895b7.json`), `pnpm test:mcp:http:discovery` (artifact `/tmp/newsportal-mcp-http-deterministic-2677c8d6-16dd-4cc7-a7be-7b145ca5c657.json`), `pnpm test:discovery:domains:compose` (artifact `/tmp/newsportal-live-discovery-domain-matrix-2ee67265.json`), `pnpm test:discovery:mega:compose` (artifact `/tmp/newsportal-live-discovery-domain-matrix-8fa52eae.json`), `pnpm test:discovery:nonregression:compose` (artifact `/tmp/newsportal-discovery-nonregression-1b21b83e.json`), `git diff --check`, and repository scans showing no active `discovery_missions`/`discovery_candidates`/`discovery_recall_*` table references under `services`, `tests`, `apps`, `infra/scripts`, or `packages`; only negative assertions remain for removed legacy BFF exports.
- Current resilient discovery v3 cutover residual gates: production deployment, shared/prod database migration, and any physical removal of now-unreferenced legacy Python helper files outside the v3 route/plugin/export path remain separate approval-gated cleanup/deploy tasks. Social providers remain `monitor_only`/`needs_config`; website auto-promotion remains disabled by default.
- Current resilient discovery v3 intelligence completion proof passed: yes; added additive `0053_discovery_v3_intelligence_layer.sql`, discovery LLM task templates/decision logging, schema-validating cached/fallback LLM gateway, autopilot profiles/config simplification, runtime LLM graph expansion, bounded Explorer -> Constructive Skeptic -> Repairer -> Verification Skeptic -> Referee integration in the orchestrator, multi-generation discovery loop, search fanout adapter/provider votes, bounded source-directory extraction, source expansion/replacement planning, API/SDK/MCP/admin surfaces for simple target creation, explain/diagnose, LLM decisions and source expand/replace. Proof passed with `PYTHONPATH=. python -m unittest tests.unit.python.test_discovery_v3_intelligence`, `PYTHONPATH=. python -m unittest tests.unit.python.test_discovery_v3_policy tests.unit.python.test_discovery_v3_stability tests.unit.python.test_api_discovery_v3_cutover tests.unit.python.test_discovery_v3_intelligence` (43/43), `pnpm --filter @newsportal/sdk typecheck`, `pnpm --filter @newsportal/mcp typecheck`, `pnpm --filter @newsportal/admin typecheck`, `pnpm typecheck`, `pnpm test:migrations:smoke`, `pnpm lint:py`, `pnpm lint:ts`, `pnpm unit_tests:ts -- mcp-control-plane` (340/340), `pnpm test:discovery:admin:compose`, `pnpm test:mcp:http:discovery` (artifact `/tmp/newsportal-mcp-http-deterministic-97cfd164-fdf4-45c0-a64b-c2b976da887e.json`), `pnpm test:discovery:nonregression:compose` (artifacts `/tmp/newsportal-live-discovery-examples-38be8843.json` and `/tmp/newsportal-discovery-nonregression-14b76295.json`), and `git diff --check`.
- Last completed item this session: `NEWSPORTAL-REINDEX-SUPERSESSION-CANCEL-CAPABILITY-1`.
- Outcome: added reindex job supersession keys, cooperative queued/running cancellation, stale-running migration repair, worker batch-boundary cancellation handling, admin BFF Cancel action, live cancelling counts, and Recent Jobs Cancel controls; targeted proof, lint, typecheck, and migration smoke passed.
- Cleanup status: no destructive cleanup was run; existing local runtime rows/compose state were treated as pre-existing audit evidence.
- Last archived item: `NEWSPORTAL-RELEASE-READY-COMPLIANCE-CAPABILITY-STAGE-32`.
- Outcome: added the repo-owned non-deploy `pnpm release:verify` gate, production image content smoke and release verification wiring, then repaired deterministic MVP proof seed state so the full release gate completes against consistent canonical document state.
- Cleanup status: `pnpm release:verify` ran product-local cleanup/down; final `docker ps --format '{{.Names}}'` was empty; no commit/stage/branch action was performed.

### Known next candidates

- Current resilient discovery v3 extended live-polish stage:
  - ID: NEWSPORTAL-DISCOVERY-V3-EXTENDED-LIVE-POLISH-STAGE-1
  - Item status: active.
  - Lifecycle mode: normal.
  - Work route: capability.
  - Route phase: implementation/proof.
  - Parent capability: Resilient Discovery V3 capability completion and live calibration.
  - Planning/spec source/status: external-spec accepted-for-this-item from the user-provided Discovery V3 Extended Live Examples Plan.
  - Scope: add a separate extended live-polish case set for security advisories, primary/open data, report/research libraries, localized PL/DE procurement, regulatory/policy watches, and vendor ecosystem expansion; add root scripts; extend role/query/classification support only where needed for those live examples; add targeted TS/Python proof and bounded live diagnostic/acceptance evidence when feasible.
  - Out of scope: production/shared DB migration, paid provider live usage, social/API/email live ingress, website auto-promotion, automatic threshold/policy edits without replay evidence, git commits/staging.
  - Blueprint context checked: Discovery live search/LLM budget, Source model vs provider adapters, PostgreSQL source-of-truth, Worker/selection/discovery work, and test/runtime boundaries.
  - Risk: medium; external live web/RSS/website nondeterminism is bounded by repeat gates and report-only tuning artifacts.
  - Approval required: no for local source/test harness edits and bounded local compose proof; yes for production/shared DB changes, paid providers, deployment, destructive cleanup, or social/API/email live ingress.
  - Required proof: `pnpm unit_tests:ts -- discovery-live-calibration`; targeted Python discovery tests for endpoint classification/hypothesis role support; `pnpm lint:py`; `pnpm lint:ts` or targeted JS syntax/lint if full lint is too broad; `git diff --check`; at least targeted extended live diagnostic proof if local compose/network permits.
  - Proof passed: yes for the extended live-polish source/harness changes and bounded live diagnostic proof. Added extended case packs and scripts for security advisories, primary/open data, report/research libraries, localized PL/DE procurement, regulatory/policy watches, and vendor ecosystem expansion; added role-specific query/classification support for regulatory policy and localized procurement; made live reports read all endpoint pages and include endpoint examples with why-found/why-not-promoted/missing-evidence; added settled-state rereads for large endpoint sets; fixed seed role selection to respect explicit `sourceRoleTargets`. Static/unit proof passed with `node --check infra/scripts/lib/discovery-live-calibration.mjs`, `node --check infra/scripts/test-live-discovery-calibration.mjs`, `PYTHONPATH=. python -m unittest tests.unit.python.test_discovery_v3_policy tests.unit.python.test_discovery_v3_stability` (36/36), `pnpm unit_tests:ts -- discovery-live-calibration` (348/348), `pnpm lint:py`, `pnpm lint:ts`, and `git diff --check`.
  - Live proof passed: yes; targeted PL/DE procurement diagnostic passed with artifact `/tmp/discovery-live-calibration-68a8ecb6.*`; targeted regulatory policy diagnostic passed after endpoint pagination/stabilization with artifact `/tmp/discovery-live-calibration-f3c49024.*`; targeted report/research diagnostic passed after explicit role-target seed selection with artifact `/tmp/discovery-live-calibration-52ce10e8.*`. Final full extended diagnostic `pnpm test:discovery:live-polish:compose -- --repeat=1` passed with artifact `/tmp/discovery-live-calibration-7a9df566.json` and `.md`, runtime/quality/final verdict `pass`, all six extended cases passing 1/1, replay suite `59dfd7b1-14f8-4ef4-ab0b-1d4a33d9fac6`, replay eval run `03fcc548-4a76-4940-a386-804a39f6fe8a`.
  - Extended acceptance proof passed: yes; the full readiness sweep passed targeted Python discovery suites (`tests.unit.python.test_discovery_v3_policy`, `test_discovery_v3_stability`, `test_api_discovery_v3_cutover`, `test_discovery_v3_intelligence`, `test_task_engine_discovery_plugins`, `test_task_engine_pipeline_plugins`, 75/75), `pnpm unit_tests:ts -- discovery-live-calibration mcp-control-plane discovery-admin mcp-http-doc-parity mcp-http-live-diagnostics` (348/348), `pnpm test:migrations:smoke`, `pnpm lint:py`, `pnpm lint:ts`, `pnpm typecheck`, and `git diff --check`. Core strict live acceptance `pnpm test:discovery:live-acceptance:compose` passed with artifact `/tmp/discovery-live-calibration-6b339a65.*`, runtime/quality/final verdict `pass`, all six core cases 3/3, replay suite `803edc0f-a56c-4507-a8a7-a0728f03582d`, replay eval run `e5bb730e-3f24-4cf9-a78e-4bb548770b96`. Extended strict live acceptance `pnpm test:discovery:live-polish-acceptance:compose` passed with artifact `/tmp/discovery-live-calibration-7e2e7ae9.*`, runtime/quality/final verdict `pass`, all six extended cases 3/3, replay suite `865bc6ec-d33e-4d26-98c5-aae5f9b43913`, replay eval run `01e3704c-42a7-4195-bc28-9a1e95ad794a`.
  - Extended soak proof passed: yes; `pnpm test:discovery:live-polish-soak:compose` passed with artifact `/tmp/discovery-live-calibration-118b9fe4.*`, runtime/quality/final verdict `pass`, all six extended cases 5/5, replay suite `892a353a-9370-4ff4-b01b-9274a20a7075`, replay eval run `22837663-9eb3-4f56-91e1-2bac9acbcb42`. Root-cause buckets remained evidence-only tuning signals: `provider_health_event` 30 and `coverage_not_improving` 5 for localized PL/DE procurement, consistent with review-only website endpoints and circuit-breaker telemetry rather than hypothesis failure.
  - Admin/MCP/nonregression proof passed: yes; `pnpm test:discovery:admin:compose` passed v3 operator workspace acceptance for targets/endpoints/contracts/claims/negative-evidence/provider-health/eval guard actions; `pnpm test:mcp:http:discovery` passed with artifact `/tmp/newsportal-mcp-http-deterministic-9aeb9e3e-4ad1-4530-9704-adb2f2270f6c.*`; `pnpm test:discovery:nonregression:compose` passed with artifacts `/tmp/newsportal-discovery-nonregression-51122f2d.*` and `/tmp/newsportal-live-discovery-examples-e4a603e6.*`.
  - Residual gates: no local readiness gate from the requested sweep remains failing. Production/shared DB migration, paid provider live usage, social/API/email live ingress, production deploy, destructive cleanup, and any policy/threshold/prompt tuning remain separate approval-gated work. Provider-health events remain a tuning/observability track; website auto-promotion remains disabled, and website endpoints stay review/manual-review only.
  - Cleanup status: local compose stack may remain running from the proof commands; no destructive cleanup, git stage, commit, or push was performed.
- Optional next candidate: narrow profile/policy tuning for consistently weak matrix domains, then rerun `pnpm test:discovery:mega:compose`.
- Current resilient discovery v3 live calibration proof passed: yes for runtime/live web-RSS-Atom-website execution; full calibration command `pnpm test:discovery:live-calibration:compose` completed with artifact `/tmp/discovery-live-calibration-00e72023.json` and `/tmp/discovery-live-calibration-00e72023.md`, runtime verdict `pass`, final verdict `pass`, replay suite `2992e13d-0a52-4713-bd34-134ea3f35da4`, replay eval run `06bea808-4cce-48fb-8b3b-0d452a472972`. The full 6-flow pass showed `bootstrap_rss_atom` 3/3, `existing_source_expansion` 3/3, `replacement_discovery` 3/3, `source_directory_extraction` 3/3, and `negative_evidence_provider_health` 3/3; `gap_procurement_web` remained quality-weak at 0/3 with root causes `provider_health_event`, `review_policy_problem`, and `coverage_not_improving`. Earlier live diagnostic failures in this route exposed and were fixed for UUID JSON serialization in coverage snapshots, invalid `source_expansion` endpoint signal mode, unbounded source expand/replace run payloads, provider-health FK leakage through synthetic `fetchers`, missing endpoint novelty causing duplicate rejection, missing seed URL/domain simple-target support, RSS probe evidence mapping for `is_valid_rss`/`sample_entries`, and RSS auto-promotion/contract creation. Targeted post-fix calibration artifacts `/tmp/discovery-live-calibration-3108b7cc.*` and `/tmp/discovery-live-calibration-73ca4b6f.*` showed bootstrap RSS/Atom passing 3/3 with probation/partial coverage while procurement remained the tuning target. Proof also passed with `PYTHONPATH=. python -m unittest tests.unit.python.test_discovery_v3_stability tests.unit.python.test_discovery_v3_policy tests.unit.python.test_api_discovery_v3_cutover tests.unit.python.test_task_engine_discovery_plugins` (52/52), `pnpm unit_tests:ts -- discovery-live-calibration` (345/345), `pnpm lint:py`, and `git diff --check`.
- Current resilient discovery v3 live calibration residual gates: strict `pnpm test:discovery:live-acceptance:compose` was not run after the latest full calibration because the known `gap_procurement_web` weak result would intentionally fail the MVP quality gate. Next tuning should focus only on procurement web discovery: better procurement-specific seed URLs/domains/query templates, missing-evidence explanations from website probes, provider-health noise classification, and website manual-review thresholds, while keeping website auto-promotion disabled and proving any threshold/prompt/policy change through the recorded replay eval path before claiming quality improvement.
- Current resilient discovery v3 live procurement polish proof passed: yes; procurement-specific live tuning added official procurement seed URLs/domains for SAM.gov, TED Europa, and Find a Tender, expanded procurement query templates, made seed URL/domain hypotheses inherit the missing procurement role instead of defaulting to technical change, classified official procurement opportunity/search paths as procurement endpoints, preserved URL-validation evidence when merging website probe evidence, and allowed operator-supplied procurement seed URLs to become `manual_review` candidates with explicit missing evidence while still preventing website auto-promotion. Targeted `pnpm test:discovery:live-calibration:compose -- --cases=gap_procurement_web` passed with artifact `/tmp/discovery-live-calibration-8ee4915d.json` and `/tmp/discovery-live-calibration-8ee4915d.md`, runtime/quality/final verdict `pass`, `gap_procurement_web` 3/3, three reviewable procurement website endpoints per iteration, replay suite `eecaecde-2ffb-4b39-8abc-5443c30e478e`, and eval run `74b8a7a9-d6d9-4352-bc8a-68d4cacdc2d1`.
- Current resilient discovery v3 live acceptance proof passed: yes; full `pnpm test:discovery:live-calibration:compose` after procurement tuning passed with artifact `/tmp/discovery-live-calibration-20dc43f9.json` and `/tmp/discovery-live-calibration-20dc43f9.md`, runtime/quality/final verdict `pass`, all six case families 3/3, replay suite `6ef0948f-c7ed-4f6a-8484-08ae75cb0799`, eval run `0c3fbb60-955e-4417-8868-b8a97a903278`. Strict `pnpm test:discovery:live-acceptance:compose` initially hit only local Docker socket sandbox denial, then passed after approved compose access with artifact `/tmp/discovery-live-calibration-d4b1905f.json` and `/tmp/discovery-live-calibration-d4b1905f.md`, runtime/quality/final verdict `pass`, all six case families 3/3, replay suite `9b90e70a-d6fa-41ba-8c9c-c42218916002`, eval run `9195d13a-6752-426c-b93f-c2fa348c0221`. Proof passed with `PYTHONPATH=. python -m unittest tests.unit.python.test_discovery_v3_stability tests.unit.python.test_discovery_v3_policy` (35/35), `pnpm unit_tests:ts -- discovery-live-calibration` (345/345), `pnpm lint:py`, and `git diff --check`.
- Current resilient discovery v3 live acceptance residual gates: provider-health events still appear in every live run and should remain a circuit-breaker/tuning track rather than hypothesis failure evidence; duplicate pressure remains visible in RSS/bootstrap and replacement flows, so the next polish should be identity dedupe/novelty budget tuning backed by the replay suites above. Website auto-promotion remains disabled; procurement endpoints are reviewable only and carry missing evidence rather than source-channel promotion.
