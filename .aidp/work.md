# Work

Этот файл хранит только live execution state. Он не является backlog, blueprint или историей.

## Свежесть live state

- Последняя проверка этого файла по worktree reality: 2026-05-12
- Последняя проверка blockers/dependencies: 2026-05-12
- Следующая revalidation для blocked items: n/a

## Текущий режим

- Workflow mode: normal
- Разрешенные workflow modes: setup | normal | repair
- Work route: docs-operator
- Разрешенные work routes: bootstrap | micro-patch | capability | bugfix | sweep | audit | docs-operator | delivery
- Route phase: awaiting-operator-review
- Route-specific next step: operator review of AIDP 1.8.12 migration diff; staging/commit is separate if desired.
- Route-specific proof: owner-file alignment inspection passed; router thinness preserved; `python3 ./aidp-monitor/server.py --repo . --check` exited 0 with one dirty-worktree warning; `python3 ./aidp-monitor/server.py --repo . --check --json` exited 0 with the same warning; `python3 ./aidp-monitor/server.py --repo . --check --strict` exited 1 as expected for warnings-only strict mode; monitor server/API smoke returned `/api/state` JSON and UI GET 200; hidden reference scan found no removed distribution path refs; setup flags preserved.
- Planning required by route: yes
- Planning source: external-spec
- Plan/spec status: accepted-for-this-item
- Plan summary: user-requested soft migration from AIDP 1.7.3 to 1.8.12; preserve project truth and Russian owner-file style, add monitor contract/projections/check mode/write-ahead/context/document-intake/consolidation mechanisms, do not create new routes or second canon.
- Audit overlay: approved-for-apply
- Разрешенные audit overlay values: none | requested | active-read-only | approved-for-apply
- Фокус аудита: initial read-only repair audit found stale live-state/work.md ambiguity; owner-file sync is approved by the current migration request.
- 2026-05-11 audit observation: MCP read-back shows the retained 9460-channel source pool has 0 Upwork, LinkedIn, We Work Remotely, RemoteOK, Remotive, Greenhouse, Lever, Ashby, Workable, PeoplePerHour, Guru, Reddit, or Wellfound channels; only HNRSS/Hacker News is partially present and StackOverflow appears as broken RSS-shaped channels instead of official Stack Exchange API ingestion. External source research shows direct project/hiring/community sources exist, but many are API/access/adapter-shaped rather than RSS/news-feed shaped. This is an acquisition/provider-contract gap plus filtering/hold-quality gap, not evidence that thematic demand is rare.
- 2026-05-11 implementation update: user approved `Experimental Adapter Layer` plan. New stage will keep web/RSS lanes, add thematic source-role coverage and adapter research as MCP-readable first-class discovery evidence, use existing `provider_type=api` plus `adapterKey` for official/public and research-only GitHub-derived approaches, and keep source roles/adapter risk independent from final selection/ranking/web visibility.
- 2026-05-11 implementation update: user approved `Coverage-First Funnel Calibration без автоматического отключения шумных каналов`. New stage adds MCP-readable source-family coverage and lifecycle labels (`working_high_signal`, `working_noisy_semantic_match`, `working_low_yield`, `negative_control_useful`, repair/access/policy labels), plus autoplan/iteration recommendations that can adjust cadence/repair/measurement but must not auto-disable working semantic-close noisy sources. Selection remains strict and source-independent.
- Почему сейчас: user requested migration + repair of the already initialized AIDP OS from 1.7.3 to 1.8.12 using an unpacked release package, explicitly preserving existing project-specific truth and deleting the distribution folder after installation.
- Active item id: AIDP-OS-1-8-12-MIGRATION-REPAIR-DOCS-OPERATOR-1
- Active item status: ready
- Item status: ready
- Risk: medium because runtime core, monitor tooling and routers are being changed; no product/source/config/test writes are in scope.
- Approval required: no for the requested owner-file/router/monitor migration and package cleanup; yes for destructive cleanup outside the unpacked release package, production deployment, schema/data migration, or product/source/config/test writes.
- Approval reason: current user request explicitly asks to migrate/repair AIDP OS and remove the unpacked distribution after installation.
- Operator approval recorded: 2026-05-15 user request for AIDP 1.7.3 -> 1.8.12 migration + repair.
- Request matches active item: yes
- Pre-write work-state gate: satisfied-for-AIDP-migration; product/source/config/test writes remain blocked unless a separate matching product item is created first.

<!-- aidp-monitor:start aidp_state v1 -->
```yaml
aidp_state:
  schema: 1
  projection_status: current
  lifecycle_mode: normal
  work_route: docs-operator
  route_phase: awaiting-operator-review
  active_item_id: AIDP-OS-1-8-12-MIGRATION-REPAIR-DOCS-OPERATOR-1
  item_status: ready
  risk: medium
  approval_required: no
  approval_status: not-required
  planning_required: yes
  planning_source: external-spec
  plan_status: accepted-for-this-item
  plan_summary: "Soft migration from AIDP 1.7.3 to 1.8.12: preserve NewsPortal truth, add monitor/check/write-ahead mechanisms, install root aidp-monitor, remove unpacked release."
  blueprint_context: checked
  proof_status: passed
  cleanup_required: yes
  cleanup_status: done
  blocked: no
  request_matches_active_item: yes
  pre_write_state: satisfied-for-AIDP-migration
  pre_write_ref: ".aidp/work.md#текущий-режим"
  allowed_paths:
    - ".aidp/**"
    - "AGENTS.md"
    - "aidp-monitor/**"
  context_manifest:
    implementation_refs:
      - ref: ".aidp/AGENTS.md"
        reason: "AIDP runtime contract owner"
      - ref: ".aidp/os.yaml"
        reason: "machine-readable package, policies, monitor contract"
      - ref: ".aidp/work.md"
        reason: "live state, open ledger, monitor projections"
      - ref: ".aidp/routes.md"
        reason: "route dispatcher and subprocedures"
      - ref: ".aidp/blueprint.md"
        reason: "AIDP/process boundary and durable technical index"
      - ref: ".aidp/engineering.md"
        reason: "engineering discipline and write-ahead rules"
      - ref: ".aidp/history.md"
        reason: "archive and history index"
      - ref: "AGENTS.md"
        reason: "root thin router"
    verification_refs:
      - ref: ".aidp/verification.md"
        reason: "migration, monitor, cleanup, check-mode gates"
    research_refs:
      - ref: ".aidp/contracts/"
        reason: "existing project-specific deep contracts to preserve"
      - ref: "docs/product/"
        reason: "existing human docs checked as observations, not canon"
  refs:
    active_item: ".aidp/work.md#текущий-режим"
    route: ".aidp/routes.md#route-docs-operator"
    plan: ".aidp/work.md#текущий-режим"
    blueprint: ".aidp/blueprint.md#product-docs-vs-aidp-runtime"
    proof: ".aidp/work.md#текущий-режим"
    cleanup: ".aidp/work.md#test-artifacts-and-cleanup"
    approval: ".aidp/work.md#текущий-режим"
  updated_at: "2026-05-15"
```
<!-- aidp-monitor:end -->

<!-- aidp-monitor:start aidp_open_ledger v1 -->
```yaml
aidp_open_ledger:
  schema: 1
  projection_status: current
  open_items:
    - id: AIDP-OS-1-8-12-MIGRATION-REPAIR-DOCS-OPERATOR-1
      title: "Soft migration and repair of AIDP OS 1.7.3 to 1.8.12"
      route: docs-operator
      status: ready
      risk: medium
      severity: medium
      created_at: "2026-05-15"
      review_trigger: "before migration closure"
      next_step: "operator review of AIDP 1.8.12 migration diff"
      owner_ref: ".aidp/work.md#текущий-режим"
  parked_items:
    - id: NEWSPORTAL-DDGS-LEGACY-URL-REMATERIALIZATION
      title: "Optional retained DDGS channel display URL re-materialization"
      status: ready
      risk: low
      review_trigger: "when operator wants retained legacy DDGS channel identity URLs refreshed"
      owner_ref: ".aidp/history.md#newsportal-ddgs-direct-fetchers-adapter-bugfix-1"
  open_risks:
    - id: AIDP-WORK-MD-LIVE-MEMORY-DRIFT
      title: "work.md still carries retained historical detail from prior product work"
      severity: medium
      status: open
      mitigation: "do not treat old retained sections as current active items; future consolidation should move stale detail to history through audit/docs-operator"
      review_trigger: "next memory consolidation review"
      owner_ref: ".aidp/work.md#активное-execution-state"
  blockers: []
  pending_proofs: []
  cleanup_obligations: []
  capabilities: []
  document_intake: []
  consolidation_candidates:
    - id: CONSOLIDATE-WORK-MD-PRIOR-DETAIL
      title: "Move retained old live details from work.md to history when operator requests memory consolidation"
      status: candidate
      owner_ref: ".aidp/work.md#активное-execution-state"
```
<!-- aidp-monitor:end -->

## Проверки закрытия route

- `.aidp/os.yaml` initialization flag: true
- `.aidp/os.yaml` placeholder flag: false
- Setup route: закрыт 2026-04-24
- Repair route: закрыт 2026-04-25 after live-state/docs cleanup repair
- Current lifecycle mode: `normal`
- Current work route: bugfix
- Last completed work route: `capability` for `NEWSPORTAL-OUTSOURCING-DEMAND-DISCOVERY-PRODUCT-TEST-1`.
- Normal mode note: `normal` не является work route; next work must select an explicit route.

## Item state machine

- Разрешенные item statuses: planned | ready | active | blocked | done | cancelled | superseded | archived
- `done` разрешен только после route-specific proof and close gate.
- `archived` разрешен только после sync в `.aidp/history.md`.
- `superseded` требует named replacing item.
- `blocked` требует blocker and next unblock condition.

## Текущая память

- NewsPortal — pnpm polyglot monorepo with Astro web/admin, FastAPI API, Node fetchers/relay/MCP, Python workers/ML/indexer, PostgreSQL, Redis/BullMQ and Docker Compose local baseline.
- PostgreSQL is durable business truth; Redis/BullMQ, HNSW, snapshots, queues and cache are derived/runtime state.
- Canonical AIDP runtime truth lives in `.aidp/*`; root/tool router files must remain thin.
- Product/reference docs remain under `docs/product`; runtime-agent contracts live under `.aidp/contracts/*`.
- Stateful proof must follow `.aidp/contracts/test-access-and-fixtures.md`.

## Активное execution state

### Primary active item

- ID: AIDP-OS-1-8-12-MIGRATION-REPAIR-DOCS-OPERATOR-1
- Parent capability: AIDP OS maintenance.
- Почему это primary active work: текущий запрос оператора — soft migration + repair уже установленной AIDP 1.7.3 до 1.8.12.
- Lifecycle mode: normal.
- Work route: docs-operator.
- Route phase: awaiting-operator-review.
- Item status: ready.
- Scope: `.aidp/*` owner-file migration, root router managed block, root `aidp-monitor/` install from 1.8.12, read-only monitor/check verification, cleanup of the unpacked release package.
- Out of scope: product/source/config/test writes, new work routes, `.aidp/state.yaml`, `.aidp/events.ndjson`, `.aidp/tools/`, `.aidp/skills/`, hooks, MCP configs, Python doctor, production deploy, database/schema/data migration.
- Allowed paths: `.aidp/**`, `AGENTS.md`, `aidp-monitor/**`, deleted unpacked release package.
- Accepted plan/spec: user-provided migration request from 2026-05-15 accepted as external-spec for this item.
- Context manifest: refs-only projection in `aidp_state.context_manifest`; refs were read for current migration scope.
- Blueprint context checked: Product docs vs AIDP runtime, durable AIDP/runtime documentation boundary, canonical neighborhoods and deep contracts list.
- Risk: medium.
- Approval required: no for the requested migration/repair/install/remove-package work; yes for destructive cleanup outside the unpacked release package or any product/source/config/test writes.
- Required proof: passed. Owner-file alignment inspected; setup flags preserved; route list unchanged with no forbidden new routes; monitor blocks only in owner files; router remains thin; root monitor/check installed; check/check-json/check-strict executed; monitor server/API smoke passed; no lingering distribution-path references after cleanup.
- Cleanup status: done for the unpacked release package; distribution directory removed and hidden reference scan passed.
- Notes: earlier product-flow/DDGS details retained below are legacy handoff/history context, not current active implementation authorization.

### Secondary active item

- ID: none
- Почему существует: n/a
- Разрешенные overlap paths: n/a
- Условие выхода к одному primary item: n/a

### Closed DDGS direct fetchers adapter bugfix

- ID: NEWSPORTAL-DDGS-DIRECT-FETCHERS-ADAPTER-BUGFIX-1
- Parent capability: Discovery acquisition / fetcher API adapters.
- Lifecycle mode: normal.
- Work route: bugfix.
- Scope: `@newsportal/fetchers` direct DDGS research adapter execution, DDGS channel materialization in control-plane, removal of FastAPI DDGS bridge route, `.env.example`/`.env.dev` cleanup, MCP/operator wording updates, source-bottleneck legacy bridge warning, focused unit/static proof.
- Out of scope: product-test DB cleanup/delete/reset, migration of retained DDGS channels, production certification for DDGS, changing selected/content semantics, production deployment, or re-enabling the closed product-flow heartbeat.
- Accepted plan/spec: user-provided `Исправление DDGS Search Lane Без Internal API Bridge` from 2026-05-12.
- Blueprint context checked: source/channel acquisition boundary, MCP/control-plane source onboarding, API/fetchers service boundary, external search provider risk, and source metadata not influencing selection.
- Safety gates: DDGS remains `research_only`; existing legacy DDGS channels must keep working from adapter config without using `http://api:8000`; generic API channels must still reject private/internal hosts unless explicitly allowlisted; source/search metadata must not affect final selection/ranking/web visibility.
- Proof completed 2026-05-12: added fetchers-owned `ddgs_search` execution through `duck-duck-scrape`, removed the FastAPI `/maintenance/discovery/search/ddgs` route, changed DDGS indirect channel materialization to safe `https://duckduckgo.com/?q=...` identity URLs, emptied baseline `FETCHERS_ACQUISITION_PRIVATE_HOST_ALLOWLIST`, updated MCP/operator guidance, and added legacy internal-bridge warnings to the channel bottleneck read model. Existing retained DDGS channels keep working because execution uses `adapter.searchQuery`, not their legacy `fetchUrl`.
- Required proof passed: `pnpm --filter @newsportal/fetchers typecheck`, `pnpm --filter @newsportal/control-plane typecheck`, `pnpm --filter @newsportal/mcp typecheck`, `pnpm unit_tests:ts -- api-adapter-registry thematic-discovery feed-probe channel-bottlenecks mcp-control-plane` (407/407), `python -m py_compile services/api/app/routes/discovery_routes.py services/workers/app/task_engine/adapters/web_search.py`, `pnpm check:dependency-compliance`, `pnpm lint:ts`, and `git diff --check`.
- Cleanup status: no DB cleanup/delete/reset planned.

### Closed rare-signal source-prior MCP stage

- ID: NEWSPORTAL-RARE-SIGNAL-SOURCE-PRIOR-MCP-1
- Parent capability: Discovery acquisition.
- Lifecycle mode: normal.
- Work route: capability.
- Scope: source-level rare-signal prior scoring policy, MCP-first `discovery.source_priors.evaluate/apply/list` operations, FastAPI maintenance endpoints, SDK bindings, MCP resources/report verification guidance, focused unit/contract proof, and retained-DB-safe JSON annotations in `contract_json.rareSignalPrior` and `source_channels.config_json.discovery.rareSignalPrior`.
- Out of scope: raw SQL product mutations, DB cleanup/deletion, schema-heavy migrations, automatic article selection from source priors, unsafe social scraping, production deploy, or loosening selected/web filters just to hit counts.
- Accepted plan/spec: user-provided `Research Plan: Rare-Signal Source Priors for Outsourcing Discovery` from 2026-05-09.
- Blueprint context checked: PostgreSQL source-of-truth, Discovery acquisition, Operator/admin control plane, MCP strict typed facade, Source model vs provider adapters, System selection vs personalization, safe-by-default discovery and restricted social provider boundaries.
- Safety gates: source prior proves "worth watching" only, not "signal found"; prior-only monitor/probation keeps downstream/coverage contribution at zero; source prior must not influence article/content selection, ranking, escalation, web visibility, or signal counts; web-selected content remains gated by real article/claim evidence; writes go through MCP/API surfaces and existing JSON storage; no cleanup/deletion is performed.
- Required proof: Python unit proof for scoring thresholds, negative evidence penalties, monitor/probation tiering and selected-content non-effect; API/MCP/SDK contract proof for evaluate/apply/list; `operator.report.verify` read-back support; typecheck/lint where touched; retained DB unaffected except explicit MCP/API JSON annotations when operator applies them.
- Proof passed: yes. Implemented domain-neutral rare-signal source-prior scoring in `services/workers/app/rare_signal_source_prior.py`; added FastAPI maintenance endpoints `/maintenance/discovery/source-priors`, `/evaluate`, `/apply`; added SDK and MCP tools `discovery.source_priors.list/evaluate/apply`; added `operator.report.verify reportKind=source_prior`; documented the guardrail that prior-only evidence has zero coverage/downstream contribution and cannot select articles. Follow-up user-review correction clarified the guardrail shape: `priorCanSelectArticle=false`, `priorCanRankArticle=false`, `priorCanEscalateArticle=false`, but `articleFromSourceSelectionEligible=true`, meaning a monitored/probation source prior cannot itself select, rank, or escalate content, while any article from that source can still pass normal independent article/claim evidence and system-interest selection. Verification: `PYTHONPATH=. python -m unittest tests.unit.python.test_rare_signal_source_prior tests.unit.python.test_api_discovery_v3_cutover` passed 12/12; `pnpm unit_tests:ts -- mcp-control-plane` passed 352/352; `pnpm --filter @newsportal/sdk typecheck` passed; `pnpm --filter @newsportal/mcp typecheck` passed; `pnpm lint:py` passed; `pnpm lint:ts` passed; `git diff --check --` touched paths passed. Runtime proof: `docker compose --env-file .env.dev -f infra/docker/compose.yml -f infra/docker/compose.dev.yml up --build -d api mcp` rebuilt/recreated API and MCP without DB cleanup; API `/health`, MCP `/health`, nginx `/health` passed; `docker-api-1` and `docker-mcp-1` are healthy; live API read-back `/maintenance/discovery/source-priors?pageSize=1` returned an empty paginated result, confirming the new route is reachable.
- Operational handoff: heartbeat automation `continue-tuned-outsourcing-discovery` previously ran at `FREQ=MINUTELY;INTERVAL=45`, but was paused during the 2026-05-12 product-flow closeout. Resume it only under a new explicit product-test continuation item. Source priors remain forbidden from affecting article/content selection, ranking, escalation, web visibility, or signal counts.
- Cleanup status: no cleanup after implementation; product-test DB rows and any prior annotations remain retained.

### Closed hold policy and replay observability stage

- ID: NEWSPORTAL-HOLD-QUALITY-REPLAY-OBSERVABILITY-1
- Parent capability: MCP strict typed control-plane facade, worker selection/replay runtime, and ongoing outsourcing-demand product test.
- Lifecycle mode: normal.
- Work route: capability.
- Scope: replay/backfill progress heartbeat fields and derived states, soft-failure handling for replay LLM reviews, MCP read-only `articles.holds.summary/list/explain`, `operator.report.verify reportKind=selection_hold_quality`, generic config-driven candidate-signal tiers, residual bucket split for context/buyer/project holds, tuning guidance for smaller bounded replay chunks, focused proof, and retained-DB product read-back.
- Out of scope: DB cleanup/deletion/archive/reset, expanding the 9460-channel source pool by default, domain-specific outsourcing/SMB hardcode in runtime, new social/API/ATS adapters, source-prior/source-health influence on article selection, or raw SQL product mutations.
- Accepted plan/spec: user-provided `Fix Hold Policy / Semantic Quality и Replay Observability` from 2026-05-11.
- Blueprint context checked: PostgreSQL source-of-truth, worker/selection/discovery ownership, MCP control-plane boundary, System selection vs personalization, replay/backfill observability, and source/content pipeline invariants.
- Safety gates: source priors/source health remain observation-only; selected content requires article-level semantic match or configured buyer/project evidence; context-only candidate cues are diagnostic and must not inflate selected/web-visible counts; replay chunks stay bounded and are verified before continuation; product mutations after implementation go through MCP/admin config surfaces only.
- Required proof: targeted Python unit tests for replay progress/LLM review soft failure and candidate-signal tiers; targeted MCP/TS proof for hold tools, selection-hold report verification and tuning recommendation; typecheck/lint for touched packages; runtime health after rebuild; MCP-backed retained-DB read-back and bounded replay proof when justified.
- Cleanup status: no product-test DB cleanup, deletion, archive, or reset is planned.

### Closed executable indirect search and project extraction stage

- ID: NEWSPORTAL-EXECUTABLE-INDIRECT-SEARCH-ADAPTERS-1
- Parent capability: Discovery acquisition and MCP control-plane operations.
- Lifecycle mode: normal.
- Work route: capability.
- Scope: executable indirect/search API adapter lane, MCP read-only materialization from detect-only indirect endpoints into bulk-onboard-ready API channel rows, DB-backed report verification for indirect search execution and marketplace extraction quality, improved generic marketplace/forum project-detail extraction, public Discourse search adapter, guidance updates, focused unit/contract proof, and retained-DB-safe product proof.
- Out of scope: production certification of unofficial scraping, broad RSS expansion, DB cleanup/deletion/archive/reset, source-role/source-health/source-prior influence on article selection/ranking/escalation/web visibility/counts, direct raw SQL product mutations, and domain-specific outsourcing hardcode in runtime.
- Accepted plan/spec: user-provided `Executable Indirect/Search Adapter Lane + Marketplace/Forum Project Extraction` from 2026-05-11.
- Blueprint context checked: PostgreSQL source-of-truth, fetchers acquisition ownership, Source model vs provider adapters, Discovery acquisition, MCP strict typed facade, and System selection vs personalization.
- Safety gates: search adapters are acquisition only; indirect/search provider metadata and project-detail confidence cannot select content; SearXNG is default self-host/free-compatible lane but reports `needs_config` if no base URL exists; Brave/Tavily/Exa require explicit config/key; SerpAPI remains research-only/high-risk; Upwork/LinkedIn stay indirect/access-required unless a safe unauthenticated path is proven; channel creation must still use `channels.bulk_onboard.plan/apply/verify`.
- Required proof: TS unit coverage for search adapter normalization, marketplace project-detail extraction, Discourse adapter normalization, indirect-target channel planning, MCP tool/report schemas, typecheck/lint, and MCP-backed retained-DB read-back without cleanup.
- 2026-05-11 continuation proof: implemented DDGS/search and marketplace noise hardening discovered by retained-DB product flow. Fetcher acquisition now drops search ad-click URLs and PeoplePerHour category/listing wrappers before ingestion; worker selection now treats document-level wrapper/category/ad/tag noise as a veto and prevents generic advice titles from candidate-signal uplift. Product tuning/replay was performed only through MCP: bounded `maintenance.reindex.request` chunks replayed noisy selected docIds, verified with `maintenance.reindex_jobs.list`, `operator.report.verify`, `articles.residuals.summary`, `articles.holds.summary`, `operator.system.health`, `indirect_search_execution`, `marketplace_extraction_quality`, and `content_items.list`. Latest MCP read-back: 9492 active channels; 20 active DDGS indirect search channels; 348 DDGS articles with 15 selected / 333 rejected; PeoplePerHour research adapter 53 articles with 33 project-detail items and 10 selected; 82 selected rows in final selection; 50 web-visible content items; stale pass-through 0; visible ad/category/tag/how-to detector 0. Residual risk: the remaining 50 items still need human/LLM precision review for weak LinkedIn/jobs/thought-leadership style content before declaring the product-test quality goal fully complete.
- 2026-05-11 precision calibration proof: user accepted the `Precision Calibration Before More Discovery` plan. First MCP-only audit artifact `/tmp/newsportal-precision-audit-1778510276245.json` classified the then-visible selected pool as 42 strong/probable out of 60 inspected, below quality gate, with repeated professional-network/jobs/thought-leadership/context-only false positives. A generic worker guard was added for professional-network Pulse/profile/jobs and job-only pages without project/proposal markers; proof passed with `PYTHONPATH=. python -m unittest tests.unit.python.test_worker_hard_filters tests.unit.python.test_final_selection` (42/42) and `pnpm lint:py`. Worker was rebuilt and healthy. Product tuning then ran only through MCP: 30 active ODPT/H100 system interests were updated via `system_interests.update` to add professional-network, repo-internal-change, support-context-only, market-context-only, and seller-listicle negative cues; bounded replay job `31548654-1052-4afd-a62b-e094815aeda3` completed 29/29 visible weak/noisy docs with 160 criterion LLM reviews and 0 failures, moving 20/29 to rejected; bounded hold recovery job `fd2e05eb-eb60-4662-8dc2-d2dcc92950f9` completed 17/17 project-intent holds with 56 criterion LLM reviews and selected 8; final weak-visible cleanup job `e7a432aa-afe5-4336-aa3b-e0cc499017ec` completed 24/24 but only rejected 2 more. Final MCP read-back artifact `/tmp/newsportal-final-readback-after-precision-1778512334701.json`: 74 visible items inspected, 51 strong/probable by the precision audit classifier, 13 strong and 38 probable, 14 context-only and 9 noise, 132 selected rows, 185 holds, 26962 rejected, stalePassThroughCount 0, missingInterestFilterResults 1, DDGS 388 articles with 13 selected / 375 rejected, PeoplePerHour 57 articles with 37 project-detail and 12 selected, Freelancer 52 articles with 16 project-detail and 3 selected. Follow-up correction from the user: do not create a separate internal/public selected split; final selection itself must be the only web truth, quality matters more than any fixed count, and selected-count may drop after precision fixes.
- 2026-05-11 selection-quality implementation proof: implemented the corrected no-public-split plan at source level. `summarize_final_selection_result` no longer promotes strong gray-zone consensus to selected unless the article has item-level `buyer_intent` or `project_intent` candidate-signal evidence; context-only/weak gray-zone consensus remains `gray_zone`/hold. Added MCP read-only `operator.selection.precision_audit` and strengthened `operator.report.verify reportKind=selection` to return selected precision buckets (`strong_project_signal`, `probable_signal`, `context_only`, `noise`), `whySelected`, `vetoMissed`, stale selection counts, weak selected samples, and bounded MCP follow-up actions without creating a separate public gate. Updated MCP context/resources to state that selected is the only web truth and noisy selected rows must be fixed through MCP/admin selection tuning plus bounded replay. Follow-up domain-bias audit found no outsourcing-specific terms in final selection, but did find acquisition defaults in source-role/indirect planning and marketplace extraction; those were neutralized so `outsourc` no longer drives rare-signal source-role detection, fallback indirect queries are generic, and project-detail extraction no longer requires specific commerce/CRM/ERP platform names. Proof passed: `PYTHONPATH=. python -m unittest tests.unit.python.test_final_selection tests.unit.python.test_worker_hard_filters` passed 43/43; `pnpm unit_tests:ts -- mcp-control-plane` passed 396/396; `pnpm unit_tests:ts -- api-adapter-registry thematic-discovery api-channel-config` passed 396/396; `pnpm lint:py`, `pnpm lint:ts`, `pnpm --filter @newsportal/mcp typecheck`, `pnpm --filter @newsportal/fetchers typecheck`, and `git diff --check --` touched paths passed. Runtime rebuild: `docker compose --env-file .env.dev -f infra/docker/compose.yml -f infra/docker/compose.dev.yml up --build -d mcp fetchers` rebuilt/recreated MCP and fetchers without DB cleanup; API/MCP/fetchers/worker containers are healthy. MCP retained-DB read-back after the stricter precision audit: 122 selected rows, 66 web-visible content items, precision buckets 7 strong / 23 probable / 70 context-only / 22 noise, stale pass-through 0, missing filter results 1, 228 holds plus 13 gray-zone rows. Next MCP-only step remains bounded replay/tuning of weak selected rows and project-intent holds; selected count may decrease and quantity is no longer a success gate.
- 2026-05-11 full replay request: user asked to reindex all articles for now. Through MCP only, queued `maintenance.reindex.request` job `907e0ffd-69f5-46b0-9bad-4ce4b61bcaee` with `indexName=interest_centroids`, `jobKind=backfill`, `batchSize=25`, `includeEnrichment=false`, `forceEnrichment=false`, `retroNotifications=skip`, reason `selection-full-replay-20260511`. Immediate MCP read-back showed the job running with `phase=snapshot_ready`, `totalArticles=27621`, `processedArticles=0`, `llmReviewFailures=0`; no DB cleanup/delete/archive/reset was performed. Next proof must wait for `maintenance.reindex_jobs.list` to show completed or failed before claiming replay effects.
- Cleanup status: no product-test DB cleanup, deletion, archive, or reset is planned.

### Closed channel validation, alternatives, and hidden-claim funnel stage

- ID: NEWSPORTAL-CHANNEL-VALIDATION-ALTERNATIVES-HIDDEN-CLAIM-FUNNEL-1
- Parent capability: Discovery acquisition and MCP control-plane operations.
- Lifecycle mode: normal.
- Work route: capability.
- Scope: control-plane channel provider-shape validation before bulk onboarding, admin bulk-import parity with the shared plan, MCP `channels.alternatives.plan/start`, RSS alternative discovery through the existing fetchers feed-probe contract, discovery source expansion from discovered feed evidence, config-driven hidden-claim extraction from system-interest candidate-signal groups, MCP/operator guidance and report verification updates, and focused proof.
- Out of scope: implementing StackExchange/GitHub/ProductHunt/ATS/provider-specific adapters, scraping restricted social sources, raw SQL product mutations, source-prior influence on article/content selection, DB cleanup/deletion/archive/reset, or domain-specific outsourcing code paths. Adapter implementation is deferred to a separate approved stage and recorded in `docs/product/operator/discovery/channel-adapter-gap.md`.
- Accepted plan/spec: user-provided `Channel Validation, RSS Alternatives и Hidden Claim Funnel без доменного хардкода` from 2026-05-09.
- Safety gates: RSS rows that look like website pages/root URLs are blocked unless they have feed-probe evidence; API-like URLs become `api_mapping_required`; blocked onboarding rows require `mode=allow_overrides`, `confirm=true`, and `overrideReason` to apply; alternatives planning is read-only and does not create channels; alternatives start creates bounded replacement discovery runs only; hidden-claim extraction reads generic positive/negative groups from MCP/admin system-interest configuration; source priors remain observation-only and cannot affect selection, ranking, escalation, web visibility, or counts.
- Proof passed: yes. Implemented `ChannelProviderShapeValidation` in `packages/control-plane/src/channel-bulk-onboarding.ts`, added `packages/control-plane/src/channel-alternatives.ts`, exported it through control-plane, exposed MCP `channels.alternatives.plan/start`, and updated admin bulk import preflight/apply flow to reuse the shared onboarding plan and display blocked rows. Added operator report provider-shape risk read-back, MCP resources/context guidance, and the adapter-gap risk document. Python runtime now carries selection-profile `candidateSignals`/`hiddenClaimExtraction` from active system interests into discovery targets/frontier hypotheses and hidden claim scoring; source expansion consumes discovered feed evidence without domain-specific rules. Follow-up code audit removed platform/domain keyword routing from the touched discovery hypothesis runtime; target source-role narrowing is now driven by explicit `preferredSourceRoles`/policy config instead of built-in platform names. Verification: `pnpm unit_tests:ts -- admin-bulk-channel-import mcp-control-plane` passed 360/360; `python -m unittest tests.unit.python.test_discovery_v3_intelligence tests.unit.python.test_discovery_v3_stability` passed 42/42; `pnpm --filter @newsportal/control-plane typecheck`, `pnpm --filter @newsportal/mcp typecheck`, `pnpm --filter @newsportal/admin typecheck`, `pnpm lint`, follow-up `pnpm lint:py`, and `git diff --check` passed. Admin typecheck retained existing Astro warnings/hints but 0 errors.
- Cleanup status: no product-test DB cleanup, deletion, archive, or reset performed.

### Closed source bottleneck, deep alternatives, and polite polling stage

- ID: NEWSPORTAL-SOURCE-BOTTLENECK-DEEP-ALTERNATIVES-POLITE-POLLING-1
- Parent capability: Discovery acquisition and MCP control-plane operations.
- Lifecycle mode: normal.
- Work route: capability.
- Scope: DB-backed source bottleneck read model, MCP `channels.bottlenecks.summary/list/explain`, `operator.report.verify reportKind=source_bottleneck`, admin observability parity with the shared model, deeper channel alternatives via the existing fetchers feed-probe contract, bounded `channels.alternatives.start`, and fetcher failure-aware/polite retry scheduling.
- Out of scope: implementing new StackExchange/GitHub/ProductHunt/ATS/API adapters, proxy support, domain-specific outsourcing code paths, source-health influence on article/content selection, raw SQL product mutations, or DB cleanup/deletion/archive/reset of retained product-test evidence.
- Accepted plan/spec: user-provided `Source Bottleneck Read Model, Deep Alternatives и Polite Polling` from 2026-05-10.
- Safety gates: low yield alone never deactivates/deletes a source; technical failures are classified into repair lanes; noisy working sources remain monitorable; alternatives planning is read-only; alternatives start must honor explicit bounded inputs/maxCandidates; source repair improves acquisition only and cannot select/rank/escalate articles or change web visibility; proxies remain future optional module.
- Required proof: TS unit/MCP proof for bottleneck tools/report verification, deep alternatives and bounded start; fetcher unit proof for hard-failure backoff, Retry-After date/seconds and jitter bounds; admin typecheck or focused proof where touched; `git diff --check` for touched paths.
- Proof passed: yes for code-level capability. Added `packages/control-plane/src/channel-bottlenecks.ts`, exported shared source bottleneck summary/list/explain, exposed MCP `channels.bottlenecks.summary/list/explain`, added `operator.report.verify reportKind=source_bottleneck`, and wired admin Observability fetch-health mode to the same summary read model. Extended alternatives through the fetchers-owned feed-probe contract: feed probe now handles `<base>` and HTTP `Link: rel=alternate`, fetchers exposes `/internal/discovery/feeds/alternatives`, and control-plane `channels.alternatives.plan` consumes deep candidates while preserving bulk-onboard validation as the apply gate. Fixed `channels.alternatives.start` so replacement runs are started only for bounded returned candidates, not every planned input. Updated fetch scheduling so repeated hard failures use repair backoff with deterministic jitter and host-level polite due-channel budgeting, while Retry-After remains honored and source health still cannot affect article/content selection. Added an explicit bulk-onboarding guard so RSS alternatives still marked `needs_probe` are blocked until valid `feedProbeEvidence` is included. Verification: `pnpm unit_tests:ts -- adaptive-scheduling feed-probe feed-alternatives admin-bulk-channel-import mcp-control-plane` passed 366/366 before the final needs-probe guard, then `pnpm unit_tests:ts -- admin-bulk-channel-import mcp-control-plane` passed 367/367 after the guard; `pnpm --filter @newsportal/control-plane typecheck`, `pnpm --filter @newsportal/mcp typecheck`, `pnpm --filter @newsportal/fetchers typecheck`, `pnpm --filter @newsportal/admin typecheck` passed with only pre-existing Astro hints; `pnpm lint:ts` passed; `git diff --check` on touched paths passed.
- Cleanup status: no product-test DB cleanup, deletion, archive, or reset planned or performed.

### Closed MCP bounded replay guidance micro-patch

- ID: NEWSPORTAL-MCP-BOUNDED-REINDEX-REPLAY-GUIDANCE-MICROPATCH-1
- Parent capability: MCP strict typed control-plane facade and ongoing outsourcing-demand product test.
- Lifecycle mode: normal.
- Work route: micro-patch within active capability.
- Scope: make the existing `maintenance.reindex.request` MCP contract explicit for bounded/chunked selection replay and make `operator.tuning.recommend` point gray-zone hold recall tuning through MCP read-back, bounded `docIds` replay chunks, and report/effect verification.
- Out of scope: running product-test replay chunks, raw SQL product mutations, deleting retained DB evidence, changing selection semantics, source-prior influence on article selection, or adding domain hardcode.
- Proof passed: yes. `maintenance.reindex.request` now exposes a bounded backfill options schema (`docIds`, `parentReindexJobId`, `reason`, batch/enrichment flags), rejects unsupported runtime-only options such as `progress`, and returns chunk-specific read-back guidance. `operator.tuning.recommend` for `domain=selection`, `objective=increase_recall`, `residualBucket=gray_zone_hold` now recommends the MCP-only sequence `articles.residuals.summary/list -> maintenance.reindex.request(docIds chunk) -> maintenance.reindex_jobs.list -> operator.report.verify -> operator.effect.verify`. Verification: `pnpm unit_tests:ts -- mcp-control-plane` passed 368/368; `pnpm --filter @newsportal/mcp typecheck` passed; `pnpm lint:ts` passed; `git diff --check` for touched paths passed. Runtime MCP was rebuilt with `docker compose --env-file .env.dev -f infra/docker/compose.yml -f infra/docker/compose.dev.yml up --build -d mcp`; nginx `/health` returned `ok`, direct MCP `/health` returned `{"service":"mcp","status":"ok"}`, and live MCP `tools/list` plus `operator.tuning.recommend` read-back showed the new bounded replay schema/recommendation.
- Cleanup status: no product-test DB cleanup, deletion, archive, reset, or replay mutation was performed.

### Closed MCP funnel calibration stage

- ID: NEWSPORTAL-MCP-FUNNEL-CALIBRATION-AUDIT-1
- Parent capability: MCP strict typed control-plane facade and ongoing outsourcing-demand product test.
- Lifecycle mode: normal.
- Work route: capability.
- Scope: add read-only `operator.funnel.audit`, add `operator.report.verify reportKind=funnel_calibration`, strengthen MCP funnel-calibration guidance/resources/prompts, clarify stale Example C companion JSON status, add focused MCP unit proof, and run MCP-backed read-only calibration proof against the retained local DB.
- Out of scope: hardcoding outsourcing/SMB vocabulary into selection/discovery runtime, deleting/cleaning/archiving retained product-test DB evidence, implementing new API/social/ATS adapters, adding more channels before calibration read-back, or letting source health/source priors influence article selection, ranking, escalation, web visibility, or counts.
- Accepted plan/spec: user-provided `MCP Funnel Calibration После Аудита Example C` from 2026-05-11.
- Safety gates: reference bundles are calibration evidence, not runtime truth; the new audit tool is read-only and produces recommendations only; any domain changes must go through MCP/admin configuration and separate read-back; bounded replay uses `maintenance.reindex.request` chunks only after explicit MCP read-back.
- Required proof: targeted TS unit proof for new MCP tool/report/resource behavior; MCP typecheck/lint; `git diff --check`; live MCP `tools/list`, `operator.funnel.audit`, `operator.report.verify reportKind=funnel_calibration`, and no-cleanup handoff.
- Proof passed: yes for the MCP capability and retained-DB read-back. Added read-only `operator.funnel.audit`, `operator.report.verify reportKind=funnel_calibration`, MCP guide/prompt/context guidance, MCP contract docs, and reference-only notes for stale Example C JSON companion assets. Follow-up bias hardening removed the hidden `example_c`/`outsourcing` reference-bundle inference from the audit implementation and changed unit proof to generic rare buyer-demand reference text, so Example C can only influence a run when its content is explicitly supplied as reference evidence through MCP. Verification passed: `pnpm unit_tests:ts -- mcp-control-plane` (372/372), `pnpm --filter @newsportal/mcp typecheck`, `pnpm lint:ts`, and `git diff --check` over touched MCP/test paths. Runtime MCP was rebuilt and `/health` passed. Live MCP proof: `tools/list` exposes `operator.funnel.audit`; key-only `referenceBundleKey=example_c_outsourcing` produced only generic source-to-selection calibration with default `editorial/listing` kinds and no prompt guardrails, while explicit reference text produced the rare-signal funnel spec. Post-alignment `operator.report.verify reportKind=funnel_calibration` showed ODPT compile status 18/18 active/compiled, hardGateDrift 0, contentKindDrift 0, promptGuardrailDrift 0, duplicateInterestRisk 3, sourceRoleGap 25, adapterRequiredGap 10, selected 27, webVisibleEligible 27, stalePassThroughCount 0, and missingInterestFilterResults 0. Bounded MCP replay job `6037140b-3d7e-4ba7-852a-0ceb07c64d1f` was queued for 75 hold docs and remains running with progress 0/75 after read-back; earlier retained bounded replay chunks completed 50/50 and 100/100 but did not raise selected above 27. Current yield blocker is policy/semantic hold and source-role/adapter gaps, not Example C bias, missing filter results, or web pass-through.
- Cleanup status: no cleanup, deletion, archive, or reset planned or performed.

### Archived outsourcing demand discovery product test

- ID: NEWSPORTAL-OUTSOURCING-DEMAND-DISCOVERY-PRODUCT-TEST-1
- Parent capability: Discovery acquisition.
- Lifecycle mode: normal.
- Work route: capability.
- Scope: empty local dev DB baseline after approved volume reset; local compose runtime start/migrations; MCP token/readiness; MCP resources/prompts orientation; system interests and LLM templates for outsourcing-project demand signals; resilient discovery v3 targets/runs/tuning across US, Australia, New Zealand, Canada and EU; source expansion, guarded promotion to probation, fetch/article/read-back verification, web/admin viewing proof, multi-hour monitoring if needed, and final retained-state report.
- Out of scope: production deploy, production/shared DB reset, post-test cleanup/deletes, unsafe social scraping, bypassing MCP for product mutations, direct SQL for normal product operations, browser-driven mutations, or claiming quality improvements without MCP/read-back evidence.
- Accepted plan/spec: user-provided `Product Test: Outsourcing Demand Discovery Through MCP` from 2026-05-08.
- Blueprint context checked: PostgreSQL source-of-truth, Discovery acquisition, Operator/admin control plane, MCP strict typed facade, Source model vs provider adapters, System selection vs personalization, Discovery live search/LLM budget, safe-by-default discovery and restricted social provider boundaries.
- Safety gates: local dev DB reset requires explicit tool approval; after reset no cleanup/deletion is allowed; all product writes go through MCP tools; hidden/social signals remain monitor-only unless official provider access/card/config and claim/control evidence exist; promotions require endpoint/probe evidence and source contracts; final counts must be verified through MCP `operator.report.verify` or MCP read-back surfaces.
- Required proof: runtime health for API/admin/web/MCP/nginx; MCP initialize/tools/resources/prompts read-back; MCP creation/read-back of system interests and LLM templates; MCP discovery target/run/coverage/diagnosis/read-back; MCP endpoint/claim/contract/negative-evidence/provider-health/LLM decision evidence; channel/fetch/article/web-resource verification for promoted sources; web/admin browser viewing proof; final report artifact and no-cleanup handoff.
- Proof passed: yes for the retained local product test. Empty local dev DB baseline was approved/reset and retained afterward. Runtime health passed for API/admin/web/MCP/nginx. MCP contract proof covered initialize, tools/list, resources/list/read, prompts/list, and read/write tools. The first ODPT strict run `odpt-db878043` produced 60 recent article/source items and ingestion/onboarding proof, but it did not prove the hidden-signal idea because selection rejected 183/183 and no claim-backed hidden signals were found. Follow-up tuning added MCP-executed hidden-signal scan support for claim/control persistence, corrected hidden hypothesis schema writes, tightened entity/context filtering to reject generic pain chatter, improved hidden query templates and negative-control queries, and added claim-evidence link persistence. Focused MCP proof artifact `/tmp/newsportal-hidden-signal-proof-hidden-proof-26298462.json` produced 1 DB-backed `confirmed_signal` hidden claim for VMware/Broadcom vendor-replacement/migration pressure with 9 supporting evidence links, target/control comparison 9/10 vs 1/2, specificity 1.8, confidence 0.694, and a completed follow-up `gap_fill` run verified by `operator.report.verify`. Follow-up domain-neutrality correction removed runtime vendor/entity hardcoding and `outsourcing` buyer-cue defaults; entities now come from target/hypothesis configuration, while domain examples remain only in tests/AIDP/product-test artifacts. First heartbeat scale-out artifact `/tmp/newsportal-hidden-signal-scaleout-hidden-scaleout-6c7ccb01.json` added 6 MCP targets, 4 hidden claims, 1 new DB-backed `confirmed_signal` for NIS2 implementation deadline/capacity pressure with evidence 11/20 vs control 4/20, specificity 2.75 and confidence 0.8087, plus 3 candidates and 1350 endpoint rows. Second heartbeat scale-out artifact `/tmp/newsportal-hidden-signal-scaleout-hidden-scaleout-a65d3d7a.json` added 6 more MCP targets, 1320 endpoint rows, and 1 low-confidence multilingual EU procurement candidate, but no additional confirmed signal. Third heartbeat scale-out artifact `/tmp/newsportal-hidden-signal-scaleout-hidden-scaleout-ec8cf89f.json` added 6 MCP targets, 4 hidden claims, 1 new DB-backed `confirmed_signal` for DORA compliance implementation/deadline delivery pressure with evidence 14/20 vs control 3/12, specificity 2.8 and confidence 0.838, plus AI Act/Cyber Resilience Act/Canada digital adoption candidates and 1350 endpoint rows. Fourth heartbeat scale-out artifact `/tmp/newsportal-hidden-signal-scaleout-hidden-scaleout-4dbf5170.json` added 6 MCP targets, 3 candidate claims, and 1320 endpoint rows but no new confirmed signals; AI Act remained near-miss at evidence 7/20, specificity 2.3333, confidence 0.6633. MCP selection/source-yield iterations then created domain-configured interests/templates/targets, onboarded and expanded quality source sets, and reindexed through MCP only. Key retained artifacts: `/tmp/newsportal-mcp-selection-visibility-flow-884bf9d1.json`, `/tmp/newsportal-mcp-selection-finetune-pass2-5d79f385.json`, `/tmp/newsportal-mcp-source-scaleout-continue-4846bb54.json`, `/tmp/newsportal-mcp-yield-readback-5b5f0b28.json`, `/tmp/newsportal-mcp-quality-source-iteration-82d47bb8.json`, and `/tmp/newsportal-mcp-final-yield-push-a9586bd7.json`. Final MCP-backed counts from the last artifact: 80 active channels, 1355 article observations, 107 selected articles, 58 public web content items, and 57 qualifying public content items published/observed on or after 2026-02-08. Web proof passed via `http://127.0.0.1:4321/`, which rendered `58 content items in the system-selected collection` and page `1 / 3` with `system-selected` cards. Hidden-signal separation remains explicit: 4 `confirmed_signal` hidden claims plus candidates/near-misses remain retained separately from direct article/content-item yield. Residual quality risks: one stale Digital.gov `Blogs` public item from 2024 remains a noisy selected item, provider health is degraded with recent fetch failures/rate limits, 35 articles still show missing interest filter results after the final push, and some selected items are broad enterprise/tech signals requiring tighter downstream qualification.
- Latest heartbeat proof: the public selected gate was tightened to production-like behavior and web proof now shows `0 content items in the system-selected collection`, with stale pass-through `0`. SMB/mid-market expansion was then executed through MCP only: `/tmp/newsportal-mcp-smb-midmarket-expansion-0fb44bec.json` upserted 3 SMB/mid-market canonical interests, created target `c657840c-8765-4386-94b6-fd397da035f3`, planned 50 sources, applied 47 channels, and completed follow-up reindex job `fa7b1bd7-c755-41eb-8d23-db1747f83b16`; `/tmp/newsportal-mcp-smb-readback-6fab24e4.json` verified 258 active channels, 287 new-content fetch runs in 24h, 4452 processed documents, 0 selected, stale pass-through `0`, 4 retained confirmed hidden claims, and 175 queued discovery runs. Raw candidate audit `/tmp/newsportal-mcp-smb-raw-candidate-audit-6cc2e939.json` found 30 SMB/mid-market near-misses, including Australian small-business cyber resilience grant pages and Ireland/EU voucher/program pages, but explain read-back showed all were blocked before semantic selection by `content_kind` and/or noise constraints. MCP tuning artifact `/tmp/newsportal-mcp-smb-content-kind-tune-1f693bb3.json` updated 8 active ODPT interests to include pipeline resource kinds `editorial/listing/document/entity` and added SMB noise stops; it produced 1 hold/gray-zone signal but still 0 selected. The latest reindex job `b52df89f-87a7-447d-b7d8-542aa0c1c2fd` failed while worker logs showed repeated `extract_resource` timeouts and `interest_centroids` rebuilt with 0 active centroids; this is the next operational blocker to diagnose, not a reason to loosen web selection.
- 2026-05-09 H100 source-finder continuation: user supplied 100 outsourcing-demand hypotheses and asked to run several more discovery passes for new source hypotheses. Through MCP, three existing broad live discovery batches were run with artifacts `/tmp/newsportal-mcp-h100-live-runs-c9d1965f-fd90-4149-b4a2-f59b3d8f8691.json`, `/tmp/newsportal-mcp-h100-live-runs-afcb19a7-77b3-427f-a2c6-45d5091cf981.json`, and `/tmp/newsportal-mcp-h100-live-runs-704c1978-9357-4a18-bc51-37a053edf0fc.json`; each dispatched 10 retained queued runs and completed 12 bounded live provider runs without errors. Extra H100 source-finder MCP batches then created and ran 6 source targets per pass for procurement/local portals, grants/research deliverables, developer-community paid-help, SMB SaaS/ERP/CRM forums, ATS hiring capacity gaps, and roadmap/marketplace/deprecation pressure. During live read-back, a systemic source-role noise bug was found and fixed: deterministic frontier no longer lets default multi-role coverage force procurement seed probes for community, grants/research, or ATS/job-board targets, and endpoint classification now preserves `/api` and `/data`/`open-data` paths as API/dataset even on procurement domains. Proof: `PYTHONPATH=. python -m unittest tests.unit.python.test_discovery_v3_policy tests.unit.python.test_discovery_v3_stability` passed 43/43; `pnpm lint:py` passed; worker was rebuilt after the fixes; final MCP artifact `/tmp/newsportal-mcp-h100-extra-source-finder-8707ed06-b46c-4bd7-97ef-de09f6d5db39.json` completed 6/6 runs in `bounded_live_provider_execution`, with 102 active targets, 258 completed discovery runs, 23 registered/probation contracts, 122 detect-only endpoints, 144 manual-review endpoints, 6451 monitor-only endpoints, and no errors. Final endpoint samples showed procurement/tender endpoint kinds only in the procurement target; grants, developer-community, SMB forums, ATS, and roadmap targets no longer leaked procurement/tender kinds in samples. Selection remains strict and unchanged for source discovery: 46 selected rows, 550 gray-zone, 7210 rejected, 24 content items total, and stale pass-through `0`.
- 2026-05-09 heartbeat continuation artifact `/tmp/newsportal-mcp-h100-live-runs-c402f2ae-e47b-45c5-a82a-3f4e4e581132.json`: through MCP, dispatched 10 retained queued discovery runs with 0 failures and completed 12 H100 bounded live provider runs. Queue decreased to 122 retained queued runs; completed discovery runs increased to 280. Selection remained strict and unchanged for web/content yield: 46 selected rows, 550 gray-zone, 7219 rejected, 24 content items total, and stale pass-through `0`. Provider health remained degraded for website/web_search/rss and disabled for restricted social/custom providers.
- 2026-05-09 heartbeat continuation artifact `/tmp/newsportal-mcp-h100-live-runs-76fa8dae-fbbf-4e51-9992-6c7d260524d0.json`: through MCP, dispatched another 10 retained queued discovery runs with 0 failures and completed 12 H100 bounded live provider runs. Queue decreased to 112 retained queued runs; completed discovery runs increased to 302. Selection remained strict and unchanged for web/content yield: 46 selected rows, 550 gray-zone, 7233 rejected, 24 content items total, and stale pass-through `0`. Provider health remained degraded for website/web_search/rss and disabled for restricted social/custom providers.
- Heartbeat automation `continue-tuned-outsourcing-discovery` was paused during the 2026-05-12 product-flow closeout. Historical heartbeat passes remain retained as evidence, but no periodic discovery/fine-tuning should continue unless a new explicit continuation item is opened.
- Cleanup status: no cleanup after product test; DB rows, channels, discovery runs, contracts, articles/resources, MCP logs and report artifacts remain intentionally retained.

### Active discovery v3 intelligence completion

- ID: NEWSPORTAL-DISCOVERY-V3-INTELLIGENCE-COMPLETION-1
- Parent capability: Discovery acquisition.
- Lifecycle mode: normal.
- Work route: capability.
- Scope: additive `0053_discovery_v3_intelligence_layer.sql`, v3 LLM gateway/schemas/tasks/autopilot worker modules, runtime wiring for Explorer -> Constructive Skeptic -> Repairer -> Verification Skeptic -> deterministic Referee, bounded provider fanout/source-directory/source-expansion completion, API/SDK/MCP/admin surfaces for create-simple/config/explain/diagnose/LLM decisions/source expand/replace, and focused proof.
- Out of scope: editing already-applied `0052`, production/shared DB migration or deploy, unsafe social scraping, website auto-promotion by default, deleting runtime evidence rows, or git commits.
- Accepted plan/spec: user-provided `Discovery V3 Completion Audit And Remaining Implementation Plan` from 2026-05-07.
- Blueprint context checked: PostgreSQL source-of-truth, Discovery acquisition, Operator/admin control plane, Public API vs admin/operator API, Source model vs provider adapters, LLM/search/social provider risk boundaries, MCP strict typed facade.
- Safety gates: all new schema work is additive in `0053`; LLM output remains proposal-only; deterministic probes/contracts/provider policy/thresholds remain final authority; social providers remain official-API-only and `monitor_only`/`needs_config`; replay eval proof is required before claiming threshold/prompt/policy improvement.
- Required proof: `pnpm test:migrations:smoke`, targeted Python unit discovery intelligence tests, targeted TS MCP/admin/API tests if touched, `pnpm lint:py`, `pnpm lint:ts`, `pnpm typecheck`, `git diff --check`, and bounded/fixture compose proof where feasible.
- Proof passed: source-level intelligence completion proof yes. Added additive `0053_discovery_v3_intelligence_layer.sql`; discovery LLM decision/template tables; v3 LLM gateway/schemas/tasks/autopilot modules; search fanout adapter; source-expansion/replacement planner; bounded source-directory live extraction; orchestrator wiring for graph expansion, Constructive Skeptic -> Repairer -> Verification Skeptic -> Referee, debate persistence and bounded generations; API/SDK/MCP/admin surfaces for create-simple, autopilot profiles, config simplify, coverage explain, run diagnose, endpoint explain, LLM decisions and source expand/replace. Verification: `PYTHONPATH=. python -m unittest tests.unit.python.test_discovery_v3_policy tests.unit.python.test_discovery_v3_stability tests.unit.python.test_api_discovery_v3_cutover tests.unit.python.test_discovery_v3_intelligence` passed 43/43; `pnpm unit_tests:ts -- mcp-control-plane` passed 340/340; `pnpm test:migrations:smoke`, `pnpm lint:py`, `pnpm lint:ts`, `pnpm typecheck`, and `git diff --check` passed. Admin/package typecheck retains pre-existing Astro hints but 0 errors.
- Residual proof not run in this source-level pass: heavy compose UI/MCP/admin nonregression suites and live external provider calls; production/shared migration/deploy remains out of scope.

### Active resilient discovery capability

- ID: NEWSPORTAL-RESILIENT-DISCOVERY-CAPABILITY-1
- Parent capability: Discovery acquisition.
- Scope stage 1: `.aidp/work.md`, `.aidp/contracts/discovery-agent.md`, `.aidp/contracts/independent-recall-discovery.md`, `docs/product/operator/discovery/resilient-discovery.md`, `database/migrations/0052_resilient_discovery_rebuild.sql`, `services/workers/app/discovery_v3_settings.py`, `services/workers/app/discovery_v3_provider_capabilities.py`, `services/workers/app/discovery_v3_endpoint_classification.py`, `services/workers/app/discovery_v3_scoring.py`, `services/workers/app/discovery_v3_actions.py`, focused unit tests, and proof.
- Out of scope stage 1: applying migration to the live local DB, deleting old API/MCP/admin/worker runtime code, replacing admin UI, changing fetcher network behavior, adding live social providers, production deploy, or disabling existing `source_channels`.
- Accepted plan/spec: tool-native plan from 2026-05-07 accepted for this item; implementation proceeds in stages, beginning with canonical docs plus pure policy/runtime skeleton.
- Blueprint context checked: `.aidp/blueprint.md` PostgreSQL source-of-truth, Discovery acquisition, Operator/admin control plane, Source model vs provider adapters, Product docs vs AIDP runtime; `.aidp/contracts/discovery-agent.md`, `.aidp/contracts/independent-recall-discovery.md`, `.aidp/contracts/mcp-control-plane.md`, `.aidp/contracts/feed-ingress-adapters.md`, `.aidp/contracts/runtime-migrations-and-derived-state.md`.
- Required proof stage 1: targeted Python unit proof for new pure discovery modules; `pnpm lint:py` or focused Python syntax proof if lint is blocked by broader repo state; `git diff --check --` touched paths; migration source inspection. Migration smoke is required before any stage can claim DB cutover complete.
- Proof passed: yes; `PYTHONPATH=. python -m unittest tests.unit.python.test_discovery_v3_policy` passed 8/8; `pnpm lint:py` passed; `git diff --check -- .aidp/work.md .aidp/contracts/discovery-agent.md .aidp/contracts/independent-recall-discovery.md docs/product/README.md docs/product/operator/discovery/resilient-discovery.md database/migrations/0052_resilient_discovery_rebuild.sql services/workers/app/discovery_v3_settings.py services/workers/app/discovery_v3_provider_capabilities.py services/workers/app/discovery_v3_endpoint_classification.py services/workers/app/discovery_v3_scoring.py services/workers/app/discovery_v3_actions.py tests/unit/python/test_discovery_v3_policy.py` passed.
- Cleanup status: no runtime state cleanup or destructive DB migration is planned in stage 1.

### Active resilient discovery stage 2 stability guards

- ID: NEWSPORTAL-RESILIENT-DISCOVERY-STABILITY-GUARDS-STAGE-2
- Parent capability: Discovery acquisition.
- Scope stage 2: amend not-yet-applied `database/migrations/0052_resilient_discovery_rebuild.sql`, update discovery AIDP/product docs, extend pure v3 worker modules for Constructive Skeptic, source evidence contracts/probation, negative evidence, claims/control comparisons, provider health/circuit breakers, source identity, replay eval, diversity budgets and blast-radius limits, plus focused unit proof.
- Out of scope stage 2: applying migration to live DB, deleting old API/MCP/admin/worker runtime code, implementing full API/MCP/admin cutover, adding live social provider calls, production deploy, or changing existing source-channel ingestion runtime behavior.
- Accepted plan/spec: tool-native follow-up plan from 2026-05-07 accepted for this stage.
- Blueprint context checked: same as stage 1; this stage adds explicit poisoning/blast-radius controls before promotion/runtime integration.
- Required proof stage 2: targeted Python unit proof for new pure discovery guard modules; `pnpm lint:py`; `git diff --check --` touched paths. Migration smoke remains required before any DB cutover claim.
- Proof passed: yes for source-level guard stage; `PYTHONPATH=. python -m unittest tests.unit.python.test_discovery_v3_policy tests.unit.python.test_discovery_v3_stability` passed 21/21; `pnpm lint:py` passed; `git diff --check -- .aidp/work.md .aidp/contracts/discovery-agent.md .aidp/contracts/independent-recall-discovery.md docs/product/README.md docs/product/operator/discovery/resilient-discovery.md database/migrations/0052_resilient_discovery_rebuild.sql services/workers/app/discovery_v3_actions.py services/workers/app/discovery_v3_adversarial.py services/workers/app/discovery_v3_claims.py services/workers/app/discovery_v3_contracts.py services/workers/app/discovery_v3_endpoint_classification.py services/workers/app/discovery_v3_eval.py services/workers/app/discovery_v3_identity.py services/workers/app/discovery_v3_negative_evidence.py services/workers/app/discovery_v3_provider_capabilities.py services/workers/app/discovery_v3_provider_health.py services/workers/app/discovery_v3_referee.py services/workers/app/discovery_v3_repair.py services/workers/app/discovery_v3_scoring.py services/workers/app/discovery_v3_settings.py services/workers/app/discovery_v3_skeptic.py tests/unit/python/test_discovery_v3_policy.py tests/unit/python/test_discovery_v3_stability.py` passed.
- Cleanup status: no runtime state cleanup or destructive DB migration planned in stage 2.

### Active resilient discovery stage 3 runtime cutover

- ID: NEWSPORTAL-RESILIENT-DISCOVERY-RUNTIME-CUTOVER-STAGE-3
- Parent capability: Discovery acquisition.
- Scope stage 3: apply approved local runtime `0052`, prove v3 runtime DB, enable bounded v3 provider execution path, disable old mission/recall orchestrator task runtime, remove reachable legacy admin discovery pages, and prove promotion/probation/contract behavior.
- Out of scope stage 3: production deployment, live social scraping/provider access, website auto-promotion, destructive cleanup outside the approved local discovery cutover, broad removal of historical migration files, or deleting unrelated old docs/components not reachable from runtime routes.
- Accepted plan/spec: user-provided full resilient discovery cutover plan plus follow-up capability-completion plan from 2026-05-07.
- Blueprint context checked: PostgreSQL source-of-truth, Runtime-поверхности, PostgreSQL vs derived state, Source model vs provider adapters, Public API vs admin/operator API, Discovery acquisition, Operator/admin control plane.
- Proof passed: local-runtime cutover proof yes. `pnpm test:migrations:smoke` passed before runtime migration; runtime DB check showed `0052` absent, then `pnpm db:migrate` applied `0052`; runtime read-back showed old checked discovery tables gone, v3 relations/constraints present, retained ingestion tables untouched, and `discovery_legacy_archive_batches` populated. Added bounded live execution in `services/workers/app/discovery_v3_execution.py`, v3 result persistence/provider-health repair handling in `services/workers/app/discovery_v3_orchestrator.py` and `services/workers/app/discovery_v3_repository.py`, v3-only orchestrator task names, and legacy API worker boundary stubs. Runtime smoke created a v3 target/run, persisted an endpoint from fixtures, promoted the RSS endpoint through `PostgresSourceRegistrarAdapter`, emitted `source.channel.sync.requested`, created a probation contract with coverage `0.25`/downstream `0.3`, then contract evaluation promoted it to active coverage/downstream `1.0`. API route inspection showed only v3 `/maintenance/discovery/*` routes and no mission/recall/candidate/class/profile routes. Deleted reachable old admin discovery subpages under `apps/admin/src/pages/discovery/*`, deleted legacy admin discovery components/view-model payload helpers, replaced admin/operator tests with v3 coverage, retired legacy API modules behind explicit removed stubs, replaced the worker discovery-enabled smoke with v3 adapter proof, and replaced the old live mission/recall examples harness with a v3 read-surface/yield proof harness that preserves evidence artifacts without touching old tables.
- Verification passed: `PYTHONPATH=. python -m unittest discover -s tests/unit/python -p 'test_*.py'` passed 324/324; targeted v3 Python discovery suites passed 57/57; `pnpm unit_tests:ts -- mcp-control-plane discovery-admin admin-operator-surfaces mcp-http-doc-parity mcp-http-live-diagnostics` passed 340/340; `pnpm test:discovery:admin:compose` passed; `node infra/scripts/test-live-discovery-examples.mjs` passed and wrote `/tmp/newsportal-live-discovery-examples-68009e85.json`; `pnpm test:discovery:yield:compose` passed and wrote `/tmp/newsportal-live-discovery-yield-proof-6bdc6d5b.json`; `DISCOVERY_ENABLED=1 PYTHONPATH=. python -m infra.scripts.workers.smoke discovery-enabled` passed; `pnpm test:migrations:smoke`, `pnpm lint:py`, `pnpm lint:ts`, `pnpm typecheck`, and `git diff --check` passed. `pnpm typecheck` retained pre-existing Astro hints but 0 errors.
- Cleanup status: local runtime proof intentionally left one `codex-runtime-smoke` source channel/target/run/contract row as read-back evidence for the approved local cutover; no production state was touched.

### Active MCP template/token hygiene bugfix

- ID: NEWSPORTAL-MCP-TEMPLATE-TOKEN-HYGIENE-BUGFIX-1
- Parent capability: MCP strict typed control-plane facade and ongoing operating intelligence.
- Scope: `.aidp/work.md`, `packages/control-plane/src/templates.ts`, `packages/control-plane/src/mcp-tokens.ts`, `services/mcp/src/tools/admin-tools.ts`, `services/mcp/src/resources.ts`, `tests/unit/ts/mcp-control-plane.test.ts`, targeted proof, and local DB expired test-token cleanup.
- Out of scope: adding per-interest `promptTemplateId` linkage, schema migrations, production deploy, changing MCP token auth expiry semantics, deleting non-expired tokens, or changing read-only id prefix behavior.
- Accepted fix: hydrate existing interest/LLM template rows before update so omitted optional fields preserve current DB values; expose token `effectiveStatus`, `activeUsable`, and `expiredActive` in MCP token inventory/summary surfaces; delete local expired test tokens after verification.
- Blueprint context checked: `.aidp/blueprint.md` PostgreSQL source-of-truth, Operator/admin control plane, Public API vs admin/operator API; `.aidp/contracts/mcp-control-plane.md` token inventory and strict typed facade.
- Required proof: targeted TS unit proof for template update preservation and token effective counts; `pnpm --filter @newsportal/control-plane typecheck`; `pnpm --filter @newsportal/mcp typecheck`; `pnpm lint:ts`; `git diff --check --` touched paths; runtime MCP rebuild and health check; DB before/after cleanup count.
- Proof passed: yes; `pnpm unit_tests:ts -- mcp-control-plane` passed 352/352; `pnpm --filter @newsportal/control-plane typecheck`, `pnpm --filter @newsportal/mcp typecheck`, `pnpm lint:ts`, and `git diff --check -- .aidp/work.md packages/control-plane/src/templates.ts packages/control-plane/src/mcp-tokens.ts services/mcp/src/tools/admin-tools.ts services/mcp/src/resources.ts services/mcp/src/tools/shared.ts services/mcp/src/tools/sequences-tools.ts services/mcp/src/tools/discovery/write-tools.ts services/mcp/src/tools/channels-tools.ts services/mcp/src/tools/templates-tools.ts tests/unit/ts/mcp-control-plane.test.ts` passed. DB cleanup verification shows no remaining expired active MCP tokens (`active=56`, `revoked=11`, `expired_count=0`); deletion query removed 0 additional rows because the expired local test rows were already gone. MCP container was rebuilt with `docker compose --env-file .env.dev -f infra/docker/compose.yml -f infra/docker/compose.dev.yml up --build -d mcp`; `/health` returned `{"service":"mcp","status":"ok"}`, `docker-mcp-1` is healthy, and recent `mcp_request_log` check showed 0 errors in the last 5 minutes.
- Cleanup status: expired active local MCP test-token cleanup verified complete; no non-expired tokens were deleted.

### Active MCP sequence id guard bugfix

- ID: NEWSPORTAL-MCP-SEQUENCE-ID-GUARD-BUGFIX-1
- Parent capability: MCP strict typed control-plane facade and ongoing operating intelligence.
- Scope: `.aidp/work.md`, `services/mcp/src/tools/shared.ts`, `services/mcp/src/tools/sequences-tools.ts`, `tests/unit/ts/mcp-control-plane.test.ts`, and targeted proof.
- Out of scope: changing sequence backend semantics, accepting ID prefixes for mutating tools, schema migrations, production deploy, or deleting runtime sequence rows/runs.
- Accepted fix: add a shared full-UUID reader for mutating MCP id arguments and use it for sequence update/run/retry/cancel/archive paths; read-only sequence tools keep existing alias/prefix behavior.
- Blueprint context checked: `.aidp/blueprint.md` Operator/admin control plane, PostgreSQL source-of-truth, API/control-plane boundaries; `.aidp/contracts/mcp-control-plane.md` strict typed facade and sequence cleanup/reindex guidance.
- Required proof: targeted MCP unit proof that invalid sequence write ids fail with MCP `-32602` before backend fetch; `pnpm --filter @newsportal/mcp typecheck`; `pnpm lint:ts`; `git diff --check --` touched paths; runtime MCP rebuild and health check.
- Proof passed: yes; follow-up similarity sweep found adjacent malformed-UUID risk in discovery write ids, discovery id-bearing payloads, template update/destructive ids, MCP token lifecycle ids, and channel delete. Added shared optional/required UUID readers and applied full-UUID MCP boundary validation while preserving read-only alias/prefix behavior and string-key `discovery.classes.*` handling. `pnpm unit_tests:ts -- mcp-control-plane` passed 350/350 including sequence and adjacent malformed-id regression coverage; `pnpm --filter @newsportal/mcp typecheck`, `pnpm lint:ts`, and `git diff --check -- .aidp/work.md services/mcp/src/tools/shared.ts services/mcp/src/tools/sequences-tools.ts services/mcp/src/tools/discovery/write-tools.ts services/mcp/src/tools/admin-tools.ts services/mcp/src/tools/channels-tools.ts services/mcp/src/tools/templates-tools.ts tests/unit/ts/mcp-control-plane.test.ts` passed. Final `rg` found no remaining `readRequiredString(...Id)` in MCP tools except the private `resolveChannelId` helper, which immediately validates full UUID/unique prefix. MCP container was rebuilt with `docker compose --env-file .env.dev -f infra/docker/compose.yml -f infra/docker/compose.dev.yml up --build -d mcp`; `/health` returned ok, `docker-mcp-1` is healthy, and a fresh 5-minute `mcp_request_log` check showed 0 new errors.
- Cleanup status: compose stack remains running for product testing; no destructive cleanup planned.

### Согласованность worktree

- Existing dirty worktree contains prior AIDP/admin/control-plane changes; do not revert unrelated changes.
- Scope warning: do not run broad `git clean -fdX`; ignored `.env.*`, `.idea`, `node_modules`, `dist`, `.astro`, `data/models`, `data/snapshots` and other runtime/build artifacts may be locally useful and must only be removed by explicit targeted request.
- No commit/stage/branch changes were requested or performed for the latest sweep/stages.

### Active MCP recall evidence update guard bugfix

- ID: NEWSPORTAL-MCP-RECALL-EVIDENCE-UPDATE-GUARD-BUGFIX-1
- Parent capability: MCP strict typed control-plane facade and ongoing operating intelligence.
- Scope: `.aidp/work.md`, `packages/contracts/src/mcp-schemas.ts`, `services/mcp/src/tools/discovery/write-tools.ts`, `tests/unit/ts/mcp-control-plane.test.ts`, and targeted proof.
- Out of scope: backend/admin recall candidate edit semantics, live network feed probing, schema migrations, production deploy, or deleting the already-promoted product-test channel/candidate.
- Accepted fix: remove `evaluationJson` from MCP recall candidate create/update payload shapes and tool guidance, so MCP clients cannot forge promotion evidence through either candidate creation or later review-state updates; keep review status/reason fields writable and keep promotion evidence checks on acquisition-owned stored candidate evidence.
- Blueprint context checked: `.aidp/blueprint.md` Operator/admin control plane, Discovery acquisition, PostgreSQL source-of-truth; `.aidp/contracts/mcp-control-plane.md` recall promotion evidence guard.
- Required proof: targeted MCP unit proof that `discovery.recall_candidates.create/update` with `evaluationJson` fails at MCP schema boundary before backend fetch; `pnpm --filter @newsportal/contracts typecheck`; `pnpm --filter @newsportal/mcp typecheck`; `pnpm lint:ts`; `node --check infra/scripts/lib/mcp-http-scenarios.mjs`; `git diff --check --` touched paths; runtime MCP rebuild and health check.
- Proof passed: yes; live `mcp_request_log` audit showed most errors were expected MCP guards, but found a real bypass where a client used `discovery.recall_candidates.update` to rewrite `evaluationJson.validFeed/discoveredFeedUrls` after an RSS promotion denial, then promoted successfully. Follow-up logic audit found the same trust edge on MCP candidate create, so `evaluationJson` is now removed from both MCP recall candidate create and update schemas. Updated tool guidance and MCP HTTP scenarios so acquisition/probe evidence cannot be forged through MCP candidate writes; explicit source rows should use channel/bulk onboarding and website candidate shell promotion needs `overrideReason`. `pnpm unit_tests:ts -- mcp-control-plane` passed 348/348; `pnpm --filter @newsportal/contracts typecheck`, `pnpm --filter @newsportal/mcp typecheck`, `pnpm lint:ts`, `node --check infra/scripts/lib/mcp-http-scenarios.mjs`, and `git diff --check -- .aidp/work.md packages/contracts/src/mcp-schemas.ts services/mcp/src/tools/discovery/write-tools.ts tests/unit/ts/mcp-control-plane.test.ts infra/scripts/lib/mcp-http-scenarios.mjs` passed; MCP container was rebuilt with `docker compose --env-file .env.dev -f infra/docker/compose.yml -f infra/docker/compose.dev.yml up --build -d mcp`; `/health` returned ok and `docker-mcp-1` is healthy.
- Cleanup status: compose stack remains running for product testing after the MCP rebuild; no destructive cleanup was run.

### Active MCP automation-first guidance micro-patch

- ID: NEWSPORTAL-MCP-AUTOMATION-FIRST-GUIDANCE-MICROPATCH-1
- Parent capability: MCP strict typed control-plane facade and ongoing operating intelligence.
- Scope: `.aidp/work.md`, `.aidp/contracts/mcp-control-plane.md`, `services/mcp/src/context.ts`, `services/mcp/src/resources.ts`, `services/mcp/src/prompts.ts`, `services/mcp/src/operating-intelligence.ts`, and targeted checks.
- Out of scope: changing backend discovery auto-promotion semantics, bypassing recall promotion evidence guards, making destructive operations automatic, schema migrations, or production deploy.
- Accepted approach: make server instructions, discovery resources, and relevant prompts say that NewsPortal is guarded-automation-first; MCP clients should plan automatic setup/run/verify/tuning when profiles/policies/evidence make it safe, and treat manual approval as explicit operator intent or fallback for missing policy/evidence, destructive actions, unsafe promotions, or unclear decisions.
- Blueprint context checked: `.aidp/blueprint.md` Operator/admin control plane and discovery safe-by-default invariants; `.aidp/contracts/mcp-control-plane.md` tool/resource contract and report verification guidance.
- Required proof: `pnpm --filter @newsportal/mcp typecheck`; `pnpm lint:ts`; `git diff --check --` touched paths; runtime MCP rebuild and health check.
- Proof passed: yes; MCP server instructions now state the default planning posture is guarded automation, not manual review. Discovery scenario resources and prompts now tell clients to use/create profiles and pass `profileId` for graph and recall missions when the operator did not request manual approval, with manual-review-only described as fallback for missing policy/evidence/ambiguity. The MCP control-plane contract records automation-first planning as part of initialize guidance. `pnpm --filter @newsportal/mcp typecheck`, `pnpm lint:ts`, and `git diff --check -- .aidp/work.md .aidp/contracts/mcp-control-plane.md services/mcp/src/context.ts services/mcp/src/resources.ts services/mcp/src/prompts.ts services/mcp/src/operating-intelligence.ts` passed; MCP container was rebuilt with `docker compose --env-file .env.dev -f infra/docker/compose.yml -f infra/docker/compose.dev.yml up --build -d mcp`; `/health` returned ok and `docker-mcp-1` is healthy.
- Cleanup status: compose stack remains running for product testing after the MCP rebuild; no destructive cleanup was run.

### Active recall no-profile guidance bugfix

- ID: NEWSPORTAL-MCP-RECALL-NO-PROFILE-GUIDANCE-BUGFIX-1
- Parent capability: MCP strict typed control-plane facade and ongoing operating intelligence.
- Scope: `.aidp/work.md`, `services/mcp/src/tools/discovery/write-tools.ts`, `services/mcp/src/tools.ts`, `services/mcp/src/context.ts`, `services/mcp/src/resources.ts`, `services/mcp/src/operating-intelligence.ts`, and targeted MCP control-plane tests.
- Out of scope: forcing every recall mission to use a profile, changing backend recall scoring/promotion semantics, schema migrations, production deploy, deleting runtime candidates, or enabling unsafe auto-promotion by default.
- Accepted plan: keep profile-less recall missions allowed, but make MCP mutation responses and `operator.report.verify discovery_run` explicitly warn that recall missions without `profileId`/applied policy are manual-review-only and should not be reported as recallPolicy/minPromotionScore configured; include recall candidate status counts so pending recall review is visible.
- Blueprint context checked: `.aidp/blueprint.md` Operator/admin control plane, Discovery acquisition, PostgreSQL source-of-truth; `.aidp/contracts/mcp-control-plane.md` report verification and recall candidate promotion guards.
- Required proof: targeted MCP unit proof for recall mission create/acquire guidance and discovery report warnings; `pnpm --filter @newsportal/mcp typecheck`; `pnpm lint:ts`; `git diff --check --` touched paths; runtime MCP rebuild and health check.
- Proof passed: yes; second-pass similarity audit found the recall lane had the same profile/policy ambiguity as graph missions. `discovery.recall_missions.create/update/acquire` now warn when no `profileId`/applied recall policy exists and point clients to read-back. `operator.report.verify reportKind=discovery_run` now includes recall mission policy flags, recall candidate status counts, pending recall warnings, and avoids pulling unrelated latest profiles/missions/recall missions when specific entity filters are supplied. Updated MCP instructions/resources/nextReadBack to cover graph and recall missions. `pnpm unit_tests:ts -- mcp-control-plane` passed 347/347; `pnpm --filter @newsportal/mcp typecheck`, `pnpm lint:ts`, and `git diff --check -- .aidp/work.md services/mcp/src/tools/discovery/write-tools.ts services/mcp/src/tools.ts services/mcp/src/context.ts services/mcp/src/resources.ts services/mcp/src/operating-intelligence.ts tests/unit/ts/mcp-control-plane.test.ts` passed; MCP container was rebuilt with `docker compose --env-file .env.dev -f infra/docker/compose.yml -f infra/docker/compose.dev.yml up --build -d mcp`; `/health` returned ok and `docker-mcp-1` is healthy.
- Cleanup status: compose stack remains running for product testing after the MCP rebuild; no destructive cleanup was run.

### Active discovery no-profile guidance bugfix

- ID: NEWSPORTAL-MCP-DISCOVERY-NO-PROFILE-GUIDANCE-BUGFIX-1
- Parent capability: MCP strict typed control-plane facade and ongoing operating intelligence.
- Scope: `.aidp/work.md`, `services/mcp/src/tools/discovery/write-tools.ts`, `services/mcp/src/tools.ts`, `services/mcp/src/context.ts`, `services/mcp/src/resources.ts`, `services/mcp/src/operating-intelligence.ts`, and targeted MCP control-plane tests.
- Out of scope: forcing every discovery mission to use a profile, changing backend discovery ranking/promotion semantics, schema migrations, production deploy, reviewing/deleting current runtime candidates, or enabling unsafe auto-promotion by default.
- Accepted plan: keep profile-less manual discovery allowed, but make MCP mutation responses and report verification explicitly warn that missions without `profileId`/applied policy are manual-review-only and should not be reported as threshold-configured/auto-promote capable; add pending-candidate warnings to `operator.report.verify discovery_run`.
- Blueprint context checked: `.aidp/blueprint.md` Operator/admin control plane, Discovery acquisition, PostgreSQL source-of-truth; `.aidp/contracts/mcp-control-plane.md` report verification and read-after-write guidance.
- Required proof: targeted MCP unit proof for no-profile discovery mission response and `operator.report.verify` warnings; `pnpm --filter @newsportal/mcp typecheck`; `pnpm lint:ts`; `git diff --check --` touched paths; runtime MCP rebuild and health check.
- Proof passed: yes; MCP now keeps profile-less manual discovery allowed but returns explicit `mcpWarnings`/read-back guidance for `discovery.missions.create/run` when no `profileId`/applied policy exists, and `operator.report.verify reportKind=discovery_run` warns when missions are manual-review-only or still have pending candidates. Updated discovery instructions/resources/read-back note to tell clients to pass `payload.profileId` when auto-promotion/profile thresholds are expected. `pnpm unit_tests:ts -- mcp-control-plane` passed 346/346; `pnpm --filter @newsportal/mcp typecheck`, `pnpm lint:ts`, and `git diff --check -- .aidp/work.md services/mcp/src/tools/discovery/write-tools.ts services/mcp/src/tools.ts services/mcp/src/context.ts services/mcp/src/resources.ts services/mcp/src/operating-intelligence.ts tests/unit/ts/mcp-control-plane.test.ts` passed; MCP container was rebuilt with `docker compose --env-file .env.dev -f infra/docker/compose.yml -f infra/docker/compose.dev.yml up --build -d mcp`; `/health` returned ok and `docker-mcp-1` is healthy.
- Cleanup status: compose stack remains running for product testing after the MCP rebuild; no destructive cleanup was run.

### Active read-id alias/prefix sweep

- ID: NEWSPORTAL-MCP-READ-ID-ALIAS-PREFIX-SWEEP-BUGFIX-2
- Parent capability: MCP strict typed control-plane facade and ongoing operating intelligence.
- Scope: `.aidp/work.md`, `services/mcp/src/tools/discovery/read-tools.ts`, `services/mcp/src/tools/sequences-tools.ts`, `services/mcp/src/tools/content-tools.ts`, and targeted MCP control-plane tests.
- Out of scope: accepting missing IDs as guessed latest entities, broad write/destructive ID prefix handling, schema migrations, production deploy, deleting runtime rows, or changing backend/API persistence semantics.
- Accepted plan: scan MCP read tools that still require one canonical id field; for read-only tools, accept common report aliases such as `id`/`entityId` and unique UUID prefixes where the source table is known; keep missing ID errors explicit and keep mutating/destructive tools strict except for already-intentional narrow helper tools.
- Blueprint context checked: `.aidp/blueprint.md` PostgreSQL source-of-truth, Public API vs admin/operator API, Operator/admin control plane; `.aidp/contracts/mcp-control-plane.md` tool/resource contract and read-after-write/report verification guidance.
- Required proof: targeted MCP unit proof for discovery/sequence/content read aliases and prefixes; `pnpm --filter @newsportal/mcp typecheck`; `pnpm lint:ts`; `git diff --check --` touched paths; runtime MCP rebuild and health check.
- Proof passed: yes; scan found read-only discovery profile/class/candidate/recall/source/score reads, sequence/sequence-run reads, article/content-item/web-resource reads with the same canonical-id-only risk. Added shared MCP alias + unique UUID-prefix resolver for read-back tools, kept missing-id errors explicit, and left mutating/destructive tools strict. `pnpm unit_tests:ts -- mcp-control-plane` passed 345/345; `pnpm --filter @newsportal/mcp typecheck`, `pnpm lint:ts`, and `git diff --check -- .aidp/work.md services/mcp/src/tools/shared.ts services/mcp/src/tools/discovery/read-tools.ts services/mcp/src/tools/sequences-tools.ts services/mcp/src/tools/content-tools.ts tests/unit/ts/mcp-control-plane.test.ts` passed; MCP container was rebuilt with `docker compose --env-file .env.dev -f infra/docker/compose.yml -f infra/docker/compose.dev.yml up --build -d mcp`; `/health` returned ok and `docker-mcp-1` is healthy.
- Cleanup status: compose stack remains running for product testing after the MCP rebuild; no destructive cleanup was run.

### Active stage scope

- Current MCP client compatibility bugfix scope: `.aidp/work.md`, `packages/control-plane/src/channels.ts`, `services/mcp/src/tools/channels-tools.ts`, `services/mcp/src/tools/templates-tools.ts`, `services/mcp/src/tools/discovery/read-tools.ts`, `services/mcp/src/operating-intelligence.ts`, `infra/scripts/lib/mcp-http-testkit.mjs`, and targeted MCP control-plane/scenario tests.
- Current MCP client compatibility bugfix out of scope: changing discovery search/ranking, channel provider persistence semantics beyond a safe activation toggle, schema migrations, production deploy, deleting product-test runtime rows, or broad admin UI changes.
- Current MCP client compatibility bugfix accepted plan: use `mcp_request_log` and API logs to classify the three reported failures; add alias-tolerant but still strict entity id parsing for system interest reads; add a safe channel activation/deactivation path or clearer partial-update guard so clients do not need to guess full provider payloads; harden discovery candidate listing against backend 500 from malformed/truncated filters; scan nearby MCP read/write tools for the same alias/partial-payload pattern; prove invalid-path behavior returns MCP `-32602` or guarded structured errors rather than backend 500/422.
- Current MCP client compatibility bugfix blueprint context checked: `.aidp/blueprint.md` Operator/admin control plane, PostgreSQL source-of-truth, Public API vs admin/operator API, Source model vs provider adapters, Discovery acquisition; `.aidp/contracts/mcp-control-plane.md`.
- Current MCP client compatibility bugfix required proof: DB/request-log reproduction evidence; targeted MCP unit proof for system interest aliases, channel partial activation/deactivation guidance, and discovery candidates invalid filter handling; `pnpm --filter @newsportal/mcp typecheck`; `pnpm --filter @newsportal/control-plane typecheck` if touched; `pnpm lint:ts`; `git diff --check --` touched paths; runtime MCP rebuild or explicit no-rebuild reason.
- Current MCP client compatibility bugfix proof passed: yes; request-log/API-log read-back showed the reported `discovery.candidates.list` 500s were caused by short mission UUID prefixes (`f3dbf7b8`, `909f0ed3`, `aaaf6dc4`) reaching the API UUID cast; `channels.update` failures were partial `channelId/isActive` payload guesses; `system_interests.read` failure was an `entityId` vs `interestTemplateId` alias mismatch. Implemented unique UUID-prefix resolution for discovery mission filters and channel read/activation, alias-tolerant system interest/LLM template read-back IDs, dedicated `channels.set_active`, channel-update hinting, and a small MCP HTTP diagnostics guard for invalid request URLs. `pnpm unit_tests:ts -- mcp-control-plane` passed 342/342; `pnpm --filter @newsportal/mcp typecheck`, `pnpm --filter @newsportal/control-plane typecheck`, `node --check infra/scripts/lib/mcp-http-testkit.mjs`, `pnpm lint:ts`, and `git diff --check --` touched paths passed; MCP container was rebuilt; MCP/API health checks passed; targeted compose `pnpm test:mcp:compose -- --scenario=template-interest-channel-flows --scenario=discovery-operator-flows --skip-build` passed with artifact `/tmp/newsportal-mcp-http-deterministic-2b65a0f2-48f5-4bdc-adfe-7f9889a3aa96.json`; recent MCP request-log read-back showed covered template/channel/discovery flows succeeded, with only expected guarded bulk-plan stale/confirm failures and no backend 500/422-like errors in the targeted run.
- Current MCP client compatibility bugfix proof residual: full default `pnpm test:mcp:compose -- --skip-build` still fails in the pre-existing `protocol-discovery` SSE diagnostic path while parsing an empty `text/event-stream` body as JSON; targeted scenarios covering this fix passed, and the remaining SSE harness issue is parked outside this bugfix.
- Current MCP client compatibility bugfix cleanup status: compose stack remains running for product testing after rebuilding MCP; no destructive cleanup was run.

- Current MCP discovery run read-back guard scope: `.aidp/work.md`, `packages/contracts/src/mcp-schemas.ts`, `services/mcp/src/context.ts`, `services/mcp/src/resources.ts`, `services/mcp/src/operating-intelligence.ts`, `services/mcp/src/tools.ts`, `services/mcp/src/tools/discovery/write-tools.ts`, `services/workers/app/task_engine/executor.py`, `services/workers/app/discovery_execution_runtime.py`, and targeted MCP/Python unit proof.
- Current MCP discovery run read-back guard out of scope: changing discovery search/ranking algorithms, schema migrations, production deploy, deleting the product-test missions/candidates, or changing backend/admin discovery semantics.
- Current MCP discovery run read-back guard accepted plan: verify the reported local discovery run through DB-backed evidence; add MCP-only confirmation for interactive missions with `maxHypotheses > 5`; keep backend payload clean by stripping MCP-only confirmation before SDK calls; enrich discovery run report verification with sequence run/task, hypothesis status, candidate status, and warnings; clarify discovery run instructions/resources/nextReadBack so clients report queued/running/completed truthfully; ensure worker cancellation/timeouts mark the active child run and hypothesis failed instead of leaving stale running state.
- Current MCP discovery run read-back guard blueprint context checked: `.aidp/blueprint.md` Operator/admin control plane, Discovery acquisition, PostgreSQL source-of-truth, Async pipeline routing risk; `.aidp/contracts/discovery-agent.md`; `.aidp/contracts/mcp-control-plane.md`.
- Current MCP discovery run read-back guard required proof: targeted MCP unit proof for wide-run guard and discovery report read-back; targeted Python unit proof for task-engine cancellation and discovery hypothesis cancellation; `pnpm --filter @newsportal/contracts typecheck`; `pnpm --filter @newsportal/mcp typecheck`; `pnpm lint:ts`; DB read-back summarizing the reported local run state; runtime MCP/worker rebuild; targeted MCP compose proof; `git diff --check --` touched paths.
- Current MCP discovery run read-back guard proof passed: yes; DB read-back showed the reported OpenCode run first failed one malformed `discovery.profiles.create` at MCP `-32602`, then created profile `outsourcing_demand_discovery`, three 15-hypothesis missions, and three parent API sequence runs that later failed on `execute_hypotheses` timeout with noisy/low-yield child results; local stale child runs/hypotheses left by the old cancellation behavior were marked `failed` with a repair note and read-back confirmed `stale_running_children=0`; `pnpm unit_tests:ts -- mcp-control-plane` passed 339/339 including wide-run confirmation and async read-back hints; `PYTHONPATH=. python -m unittest tests.unit.python.test_task_engine tests.unit.python.test_discovery_orchestrator` passed 32 tests including cancellation status repair; `pnpm --filter @newsportal/contracts typecheck`, `pnpm --filter @newsportal/mcp typecheck`, `pnpm lint:ts`, `pnpm lint:py`, and `git diff --check --` touched paths passed; runtime `worker` and `mcp` containers were rebuilt/recreated; MCP/API health checks passed; targeted `pnpm test:mcp:compose -- --scenario=discovery-operator-flows --skip-build` passed with artifact `/tmp/newsportal-mcp-http-deterministic-325c83b6-ac51-4c52-b5b5-6bf0e9ae948a.json`.
- Current MCP discovery run read-back guard cleanup status: compose stack remains running for product testing; stale local runtime rows from the pre-fix discovery run were marked failed rather than deleted; no destructive cleanup was run.

- Current MCP list-field normalization sweep scope: `.aidp/work.md`, `packages/contracts/src/mcp-schemas.ts`, `services/mcp/src/context.ts`, `services/mcp/src/tools.ts`, `services/mcp/src/tools/shared.ts`, `services/mcp/src/tools/discovery/write-tools.ts`, `services/mcp/src/tools/sequences-tools.ts`, `infra/scripts/lib/mcp-http-scenarios.mjs`, and `tests/unit/ts/mcp-control-plane.test.ts`.
- Current MCP list-field normalization sweep out of scope: loosening object payload envelopes, accepting wrong aliases, schema migrations, production deploy, changing discovery/selection semantics, or silently accepting unsupported enum values.
- Current MCP list-field normalization sweep accepted plan: audit list-like MCP write fields; let schemas accept JSON arrays, newline strings, or comma-separated strings for bounded list-like inputs; normalize those fields at MCP handler edge before SDK/backend calls; keep enum fields strict with MCP `-32602`; prove through targeted unit tests and live MCP compose.
- Current MCP list-field normalization sweep blueprint context checked: `.aidp/blueprint.md` Operator/admin control plane and PostgreSQL source-of-truth boundaries; `.aidp/contracts/mcp-control-plane.md` strict typed facade and backend-422 prevention contract.
- Current MCP list-field normalization sweep required proof: `pnpm --filter @newsportal/contracts typecheck`; `pnpm --filter @newsportal/mcp typecheck`; `pnpm unit_tests:ts -- mcp-control-plane`; `pnpm lint:ts`; `node --check infra/scripts/lib/mcp-http-scenarios.mjs`; `git diff --check --` touched paths; runtime MCP rebuild; targeted `pnpm test:mcp:compose -- --scenario=discovery-operator-flows --skip-build`.
- Current MCP list-field normalization sweep proof passed: yes; contracts and MCP typechecks passed; `pnpm unit_tests:ts -- mcp-control-plane` passed 338/338 and now covers comma/newline list normalization plus invalid enum `-32602` before backend fetch; `pnpm lint:ts` and scenario syntax check passed; `docker compose --env-file .env.dev -f infra/docker/compose.yml -f infra/docker/compose.dev.yml up --build -d mcp` rebuilt the MCP runtime; targeted MCP compose passed with artifact `/tmp/newsportal-mcp-http-deterministic-df9fb693-4efe-4c2e-9c47-afdb8fbdd899.json`; recent MCP request log showed discovery write/read flow succeeded with 0 failures and `backend_422_like_errors=0`.
- Current MCP list-field normalization sweep cleanup status: compose stack remains running for product testing; rebuild recreated `api` and `mcp` due compose dependencies; no destructive cleanup was run.

- Current MCP system-interest list-field bugfix scope: `.aidp/work.md`, `packages/contracts/src/mcp-schemas.ts`, `apps/admin/src/lib/server/admin-templates.ts`, `services/mcp/src/context.ts`, `services/mcp/src/resources.ts`, `services/mcp/src/tools/templates-tools.ts`, `tests/unit/ts/admin-template-sync.test.ts`, and local dev DB read-back/repair for the malformed `AI implementation failures and adoption barriers` test interest.
- Current MCP system-interest list-field bugfix out of scope: schema migrations, production deploy, deleting/recreating the interest, changing final selection semantics, changing LLM review policy, or destructive runtime cleanup.
- Current MCP system-interest list-field bugfix accepted plan: reproduce from MCP error/DB read-back; make MCP schemas accept system-interest string arrays as well as newline strings; make admin/control-plane parsing split comma-separated token/kind fields and candidate cues where appropriate; add targeted regression proof; repair the existing local malformed interest/profile/criterion JSON arrays; restart/prove runtime MCP/admin code if needed.
- Current MCP system-interest list-field bugfix blueprint context checked: `.aidp/blueprint.md` Operator/admin control plane, System selection vs personalization, PostgreSQL source-of-truth vs derived state; `.aidp/contracts/mcp-control-plane.md` strict typed facade and read-after-write/report verification contract.
- Current MCP system-interest list-field bugfix required proof: targeted TS unit parser proof; `pnpm --filter @newsportal/contracts typecheck`; `pnpm --filter @newsportal/mcp typecheck`; `pnpm --filter @newsportal/admin typecheck`; `pnpm lint:ts`; DB read-back showing split `allowed_content_kinds`, `must_not_have_terms`, profile cues, and compiled criterion; compose/restart proof if live containers must pick up code.
- Current MCP system-interest list-field bugfix proof passed: yes; DB read-back confirmed the created `AI implementation failures and adoption barriers` interest, its selection profile, criterion, and compiled criterion now use split arrays for `allowed_content_kinds`, `must_not_have_terms`, and candidate cues; `pnpm --filter @newsportal/contracts typecheck`, `pnpm --filter @newsportal/mcp typecheck`, `pnpm --filter @newsportal/admin typecheck`, `pnpm unit_tests:ts -- admin-template-sync` (337/337), `pnpm lint:ts`, `node --check infra/scripts/lib/mcp-http-scenarios.mjs`, and `git diff --check -- .aidp/work.md packages/contracts/src/mcp-schemas.ts apps/admin/src/lib/server/admin-templates.ts services/mcp/src/context.ts services/mcp/src/resources.ts services/mcp/src/tools/templates-tools.ts tests/unit/ts/admin-template-sync.test.ts apps/admin/src/pages/bff/admin/channels/bulk/shared.ts tests/unit/ts/admin-bulk-channel-import.test.ts` passed; rebuilt runtime containers with `docker compose --env-file .env.dev -f infra/docker/compose.yml -f infra/docker/compose.dev.yml up --build -d admin mcp`; health checks for admin and MCP passed; `pnpm test:mcp:compose -- --scenario=template-interest-channel-flows --skip-build` passed with artifact `/tmp/newsportal-mcp-http-deterministic-9b91e559-1776-4163-99bb-b6c4763219e0.json`; recent MCP request log proved `system_interests.create/update/read/list/archive/delete` all succeeded and `backend_422_like_errors=0` after the compose run.
- Current MCP system-interest list-field bugfix cleanup status: local dev DB test interest was repaired in place; existing backfill job `076145b9-7ce6-40e9-a0e8-d796a026b197` remains running and reached `5500/10970` at last read-back; compose stack remains running for product testing.

- Current discovery page bugfix scope: `.aidp/work.md`, `services/relay/src/migrations.ts`, local replay of existing `database/migrations/0042_discovery_core_schema_repair_replay.sql`, and admin/API runtime proof for `/discovery`.
- Current discovery page bugfix out of scope: discovery policy/yield tuning, schema/migrations, auth/session policy, source promotion semantics, MCP token behavior, production deploy, destructive runtime cleanup, or broad refactors.
- Current discovery page bugfix accepted plan: reproduce the `/discovery` render failure through the local admin runtime; confirm the API/schema drift behind `/maintenance/discovery/summary`; prevent schema-scoped migration smoke from falling through to public tables; replay the existing idempotent discovery schema repair migration locally; then prove the route renders and static checks pass.
- Current discovery page bugfix blueprint context checked: `.aidp/blueprint.md` UI/BFF boundary, Public API vs admin/operator API, Discovery capability, Auth/session boundary; `.aidp/contracts/discovery-agent.md` admin/operator proof expectations; `.aidp/contracts/auth-session-boundary.md` mutating admin BFF guard context.
- Current discovery page bugfix required proof: failing/reproducing evidence for `/discovery`; local schema repair proof; proof that migration smoke no longer mutates public schema; passing route/browser or curl proof after repair; `pnpm test:migrations:smoke`; `pnpm lint:ts`; `pnpm typecheck`; `git diff --check -- .aidp/work.md services/relay/src/migrations.ts`.
- Current discovery page bugfix proof passed: yes; admin/API logs showed `/maintenance/discovery/summary` failed with `psycopg.errors.UndefinedTable: relation "discovery_hypothesis_classes" does not exist`; `schema_migrations` listed all migrations as applied while `to_regclass` showed missing discovery core tables; local replay of `database/migrations/0042_discovery_core_schema_repair_replay.sql` initially restored public discovery tables, then `pnpm test:migrations:smoke` reproduced the table removal because schema-scoped migrations used `search_path = temp_schema, public`; `services/relay/src/migrations.ts` was patched to use only the requested temp schema for schema-scoped runs; `pnpm test:migrations:smoke` passed after the patch; replaying `0042_discovery_core_schema_repair_replay.sql` restored `discovery_hypothesis_classes`, `discovery_source_profiles`, `discovery_source_interest_scores`, `discovery_portfolio_snapshots`, and `discovery_feedback_events` in public; `curl -sS -i http://127.0.0.1:8000/maintenance/discovery/summary` returned 200; a temporary allowlist admin sign-in rendered `http://127.0.0.1:4322/discovery` with `Adaptive Discovery Agent`, `Dual-path discovery control plane`, `Dashboard`, `Profiles`, `Missions`, and `Recall`; `pnpm lint:ts`, `pnpm typecheck`, and `git diff --check -- .aidp/work.md services/relay/src/migrations.ts` passed. `pnpm typecheck` retained existing Astro hints but 0 errors.
- Current discovery page bugfix cleanup status: no destructive cleanup run; temporary proof admin user was deleted by the proof script; existing local compose stack remains running.

- Current bugfix scope: `.aidp/work.md`, `apps/admin/src/pages/channels/import.astro`, `apps/admin/src/components/BulkChannelImport.tsx`, `apps/admin/src/pages/bff/admin/channels/bulk/shared.ts`, `tests/unit/ts/admin-bulk-channel-import.test.ts`.
- Current bugfix out of scope: auth/session policy changes, admin role/allowlist changes, APP_SECRET/token signing semantics, schema, migrations, deploy/production settings, MCP bearer-token issuance or storage.
- Current bugfix accepted plan: reproduce by code inspection; the bulk import island runs preflight through `fetch` and final import through `form.submit()`, which bypasses the AdminShell submit listener that injects `adminActionToken`; pass route-scoped action tokens from the server-rendered page into the island, include the import token as a hidden field for form submission, attach the preflight token to JSON preflight fetches, and preserve admin action token fields through the bulk import custom payload reader before the shared guard validates them.
- Current bugfix blueprint context checked: `.aidp/blueprint.md` UI/BFF boundary, Public API vs admin/operator API, Auth/session boundary; `.aidp/contracts/auth-session-boundary.md` mutating admin BFF action-token expectations.
- Current bugfix required proof: targeted TS unit proof for bulk import/action-token rendering and payload-reader guard contract; `pnpm lint:ts`; `pnpm typecheck`; `git diff --check -- .aidp/work.md apps/admin/src/pages/channels/import.astro apps/admin/src/components/BulkChannelImport.tsx apps/admin/src/pages/bff/admin/channels/bulk/shared.ts tests/unit/ts/admin-bulk-channel-import.test.ts`.
- Current bugfix proof passed: yes; `pnpm unit_tests:ts -- admin-bulk-channel-import`, `pnpm lint:ts`, `pnpm typecheck`, and `git diff --check -- .aidp/work.md apps/admin/src/pages/channels/import.astro apps/admin/src/components/BulkChannelImport.tsx apps/admin/src/pages/bff/admin/channels/bulk/shared.ts tests/unit/ts/admin-bulk-channel-import.test.ts` passed. `pnpm typecheck` retained existing Astro hints but 0 errors.
- Current hardening scope: `.aidp/work.md`, `apps/admin/src/components/admin-form-submit.ts`, `apps/admin/src/components/AdminConfirmAction.tsx`, `apps/admin/src/components/AdminConfirmSubmitButton.tsx`, `tests/unit/ts/admin-action-kit.test.ts`.
- Current hardening accepted plan: replace duplicate `requestSubmit()`/native `form.submit()` fallback logic with a shared helper; when `requestSubmit()` exists, preserve current behavior; when only native `form.submit()` remains, dispatch a cancelable bubbling `submit` event first so the `AdminShell` submit listener can inject `adminActionToken`.
- Current hardening required proof: targeted TS unit proof for fallback event dispatch behavior; `pnpm lint:ts`; `pnpm typecheck`; `git diff --check -- .aidp/work.md apps/admin/src/components/admin-form-submit.ts apps/admin/src/components/AdminConfirmAction.tsx apps/admin/src/components/AdminConfirmSubmitButton.tsx tests/unit/ts/admin-action-kit.test.ts`.
- Current hardening proof passed: yes; `pnpm unit_tests:ts -- admin-action-kit`, `pnpm lint:ts`, `pnpm typecheck`, and `git diff --check -- .aidp/work.md apps/admin/src/components/admin-form-submit.ts apps/admin/src/components/AdminConfirmAction.tsx apps/admin/src/components/AdminConfirmSubmitButton.tsx tests/unit/ts/admin-action-kit.test.ts` passed. `pnpm typecheck` retained existing Astro hints but 0 errors.

- Current MCP reindex hardening scope: `.aidp/work.md`, `.aidp/contracts/mcp-control-plane.md`, `services/mcp/src/context.ts`, `services/mcp/src/resources.ts`, `services/mcp/src/tools.ts`, `services/mcp/src/tools/sequences-tools.ts`, `services/mcp/src/operating-intelligence.ts`, `tests/unit/ts/mcp-control-plane.test.ts`, `infra/scripts/lib/mcp-http-scenarios.mjs`.
- Current MCP reindex hardening out of scope: changing worker selection semantics, schema/migrations, production reindex execution, admin UI semantics unless needed for MCP proof.
- Current MCP reindex hardening accepted plan: use the user-provided MCP Reindex Intent Hardening plan; restrict reindex MCP schema/handler to supported index/jobKind enums; make `backfill` defaults match admin UI selection replay; clarify tool descriptions/resources/instructions so historical/current-interest selection replay maps to `maintenance.reindex.request jobKind=backfill` and content-analysis backfill is not treated as selection replay; add structured read-back hints/warnings; detect stale selected/pass_through rows with no system-criterion `interest_filter_results`; cover invalid arguments/defaults/descriptions and scenario static behavior with targeted tests.
- Current MCP reindex hardening blueprint context checked: `.aidp/blueprint.md` Runtime-поверхности, Operator/admin control plane, System selection vs personalization, PostgreSQL vs derived state; `.aidp/contracts/mcp-control-plane.md` Tool/resource contract and proof expectations.
- Current MCP reindex hardening required proof: `pnpm unit_tests:ts -- mcp-control-plane`; `node --check infra/scripts/lib/mcp-http-scenarios.mjs`; `pnpm lint:ts`; `pnpm typecheck`; `git diff --check -- .aidp/work.md .aidp/contracts/mcp-control-plane.md services/mcp/src/context.ts services/mcp/src/resources.ts services/mcp/src/tools.ts services/mcp/src/tools/sequences-tools.ts services/mcp/src/operating-intelligence.ts tests/unit/ts/mcp-control-plane.test.ts infra/scripts/lib/mcp-http-scenarios.mjs`.
- Current MCP reindex hardening proof passed: yes; `pnpm unit_tests:ts -- mcp-control-plane` passed 331/331 TS tests including invalid reindex enum rejection, backfill default option storage, metadata disambiguation, and content-analysis warning coverage; `node --check infra/scripts/lib/mcp-http-scenarios.mjs` passed; `pnpm lint:ts` passed; `pnpm typecheck` passed with existing Astro hints and 0 errors; `pnpm test:mcp:compose` passed with artifact `/tmp/newsportal-mcp-http-deterministic-2fdf5fce-9440-4d2c-a5cf-57973111b373.json`; `git diff --check -- .aidp/work.md .aidp/contracts/mcp-control-plane.md services/mcp/src/context.ts services/mcp/src/resources.ts services/mcp/src/tools.ts services/mcp/src/tools/sequences-tools.ts services/mcp/src/operating-intelligence.ts tests/unit/ts/mcp-control-plane.test.ts infra/scripts/lib/mcp-http-scenarios.mjs` passed.
- Current MCP reindex hardening proof note: the first compose attempt intentionally exposed that a full historical backfill over the already-populated local DB (`4609` articles) can exceed the sequence task timeout; the compose scenario now proves HTTP backfill lifecycle with a single existing `docId`, while unit coverage separately proves omitted `backfill` defaults.
- Current MCP reindex hardening cleanup status: compose stack was already running and remains running after `pnpm test:mcp:compose`; the failed wide backfill attempt left a local runtime `reindex_jobs` row in `running` state from the proof attempt, but no repository source cleanup is required.

- Current reindex cancellation scope: `.aidp/work.md`, `database/migrations/*`, `packages/control-plane/src/*`, `apps/admin/src/pages/reindex.astro`, `apps/admin/src/components/LiveReindexJobsSection.tsx`, `apps/admin/src/lib/live-updates.ts`, `apps/admin/src/lib/server/live-updates.ts`, `apps/admin/src/pages/bff/admin/reindex.ts`, `services/api/app/content_analysis_backfill.py`, `services/mcp/src/tools/sequences-tools.ts`, `services/workers/app/reindex_processor.py`, `services/workers/app/reindex_backfill.py`, `services/workers/app/reindex_backfill_runtime.py`, targeted TS/Python tests and relevant proof harnesses if needed.
- Current reindex cancellation out of scope: production deployment, hard-killing BullMQ/worker processes, broad queue redesign, changing selection semantics beyond replay cancellation, notification policy, auth/session policy, or unrelated MCP/admin changes.
- Current reindex cancellation accepted plan: implement same-lane `cancellation_key` supersession for new reindex/content-analysis requests, add manual admin Cancel for queued/running jobs, add `cancel_requested`/`cancelled` reindex statuses with cooperative worker exits between batches, repair stale `running` rows when sequence evidence already failed, and expose truthful cancelling/cancelled state in admin live jobs.
- Current reindex cancellation blueprint context checked: `.aidp/blueprint.md` Runtime-поверхности, Операционная схема, PostgreSQL vs derived state, UI/BFF boundary, Public API vs admin/operator API, Relay vs workers, Async pipeline routing risk; `.aidp/engineering.md` async/state/database discipline; `.aidp/verification.md` migration, relay/queue, worker, admin UI proof expectations.
- Current reindex cancellation required proof: targeted TS unit proof for control-plane/admin/MCP cancellation semantics and UI cancellability; targeted Python unit proof for API/worker cancellation and timeout handling; `pnpm test:migrations:smoke` if feasible; `pnpm lint:ts`; `pnpm typecheck`; relevant compose proof if runtime remains available; `git diff --check --` for touched paths.
- Current reindex cancellation proof passed: yes; targeted TS proof passed with `node --import tsx --test tests/unit/ts/reindex-control-plane.test.ts tests/unit/ts/admin-live-updates.test.ts tests/unit/ts/admin-reindex-live-updates.test.ts tests/unit/ts/mcp-control-plane.test.ts`; targeted Python proof passed with `PYTHONPATH=. python -m unittest tests.unit.python.test_reindex_backfill_progress tests.unit.python.test_api_discovery_management tests.unit.python.test_interest_auto_repair`; `pnpm lint:ts`, `pnpm lint:py`, `pnpm typecheck`, `pnpm test:migrations:smoke`, and `git diff --check --` for touched paths passed. `pnpm typecheck` retained existing Astro hints but 0 errors.
- Current reindex cancellation cleanup status: no new cleanup obligation; existing local compose stack and stale runtime rows were pre-existing audit evidence, and no destructive runtime cleanup was run.

- In scope: `.aidp/work.md`, `.aidp/contracts/mcp-control-plane.md`, `docs/product/operator/mcp/README.md`, `packages/control-plane/src/index.ts`, `packages/control-plane/src/channel-bulk-onboarding.ts`, `apps/admin/src/pages/bff/admin/channels/bulk/shared.ts`, `services/mcp/src/context.ts`, `services/mcp/src/resources.ts`, `services/mcp/src/prompts.ts`, `services/mcp/src/tools/channels-tools.ts`, `infra/scripts/lib/mcp-http-scenarios.mjs`, and targeted bulk-channel proof files if existing harnesses require them.
- Out of scope: token issuance UI changes, auth/session policy changes, schema, migrations, deploy/production settings, and storing real MCP token secrets in repository files.
- Allowed paths: `.aidp/work.md`, `.aidp/contracts/mcp-control-plane.md`, `docs/product/operator/mcp/README.md`, `packages/control-plane/src/index.ts`, `packages/control-plane/src/channel-bulk-onboarding.ts`, `apps/admin/src/pages/bff/admin/channels/bulk/shared.ts`, `services/mcp/src/context.ts`, `services/mcp/src/resources.ts`, `services/mcp/src/prompts.ts`, `services/mcp/src/tools/channels-tools.ts`, `infra/scripts/lib/mcp-http-scenarios.mjs`, and targeted bulk-channel proof files if existing harnesses require them.
- Accepted plan summary: stage 14 implements the user-provided MCP Bulk Source Onboarding plan: move admin bulk-channel planning/execution into the shared control-plane package, keep the admin import flow on that shared contract, add MCP `channels.bulk_onboard.plan/apply/verify`, classify per-source create/update/duplicate/mismatch/override states, require stale-plan and overwrite confirmation guards, and prove mixed-source onboarding through MCP compose.
- Blueprint context checked: `.aidp/blueprint.md` Runtime-поверхности, Operator/admin control plane capability, Runtime boundary, Product docs vs AIDP runtime; `.aidp/contracts/mcp-control-plane.md` Transport/Auth proof expectations.
- Required proof: `pnpm --filter @newsportal/control-plane typecheck`; `pnpm --filter @newsportal/admin typecheck`; `pnpm --filter @newsportal/mcp typecheck`; `node --check infra/scripts/lib/mcp-http-scenarios.mjs`; `pnpm lint:ts`; `pnpm test:mcp:compose`; `git diff --check --` for touched paths.
- Proof passed: yes; `pnpm --filter @newsportal/control-plane typecheck`, `pnpm --filter @newsportal/admin typecheck`, `pnpm --filter @newsportal/mcp typecheck`, `node --check infra/scripts/lib/mcp-http-scenarios.mjs`, `pnpm lint:ts`, `pnpm test:mcp:compose`, and `git diff --check -- .aidp/work.md .aidp/contracts/mcp-control-plane.md docs/product/operator/mcp/README.md packages/control-plane/src/index.ts packages/control-plane/src/channel-bulk-onboarding.ts apps/admin/src/pages/bff/admin/channels/bulk/shared.ts services/mcp/src/context.ts services/mcp/src/resources.ts services/mcp/src/tools/channels-tools.ts infra/scripts/lib/mcp-http-scenarios.mjs` passed. MCP compose artifact: `/tmp/newsportal-mcp-http-deterministic-83bdc9ef-2e08-4cca-9f01-2337bbf8271a.json`; markdown artifact: `/tmp/newsportal-mcp-http-deterministic-83bdc9ef-2e08-4cca-9f01-2337bbf8271a.md`.
- Admin sign-in form-only proof completed: `rg -n "Admin operations|Manage sources, templates, and moderation without losing your place|Dedicated CRUD flows|Safer operations|Context preserved" apps/admin/src/pages/sign-in.astro` returned no matches; `pnpm lint:ts` passed; `pnpm typecheck` passed with existing Astro hints and 0 errors; `git diff --check -- apps/admin/src/pages/sign-in.astro .aidp/work.md` passed.
- Web card-grid page-size proof completed: `rg -n "DEFAULT_PAGE_SIZE|WEB_CARD_GRID_PAGE_SIZE|pageSize:" apps/web/src/pages/index.astro apps/web/src/pages/matches.astro apps/web/src/pages/following.astro apps/web/src/lib/view-helpers.ts` confirmed the three 3-column card-grid pages use `WEB_CARD_GRID_PAGE_SIZE`; `pnpm lint:ts` passed; `pnpm typecheck` passed with existing Astro hints and 0 errors.
- Non-native admin confirmation proof completed: `rg -n "window\\.confirm|window\\.alert|window\\.prompt|\\bconfirm\\(|\\balert\\(|\\bprompt\\(" apps packages services infra` returned no real app/runtime matches; broader scan only finds intentional test XSS payload strings plus the newly documented AIDP rule. `pnpm lint:ts`, `pnpm typecheck`, and `git diff --check --` passed.
- MCP token admin UX proof completed: `pnpm unit_tests:ts -- mcp-control-plane` passed 323/323 TS tests including revoked-only delete guard and audit row coverage; `pnpm lint:ts` passed; `pnpm typecheck` passed with existing Astro hints and 0 errors.
- Runtime availability proof completed: `pnpm dev:mvp:internal:no-build` completed against the existing local compose stack; `curl -sS http://127.0.0.1:4322/api/health` returned admin `status:"ok"`; `curl -sS -I http://127.0.0.1:4322/automation/mcp` returned `302` to `/sign-in?next=%2Fautomation%2Fmcp` as expected without an admin session.
- Cleanup status: compose stack is intentionally left running so the admin change can be tried locally; no destructive cleanup was run.
- Product total-live proof completed: `pnpm test:product:total-live:compose -- --skip-diagnostics --skip-stack-build` run id `38128383` passed with `runtimeVerdict=pass`, `finalVerdict=pass`; artifact `/tmp/newsportal-product-total-live-38128383.json` and `.md`; strict nested mega-flow artifact `/tmp/newsportal-product-mega-flow-695b3c2b.json` passed with `runtimeVerdict=pass`, `yieldVerdict=pass`, `finalVerdict=pass`.
- Product total-live coverage evidence: strict A/B/C live selected product proof passed; providers/channel auth/website-admin/automation-admin/MCP/Web viewport/Web UI audit/relay phase 3/4/5/ingest/normalize-dedup/interest compile/criterion compile/cluster-match-notify/embed/reindex-backfill/LLM-budget-stop required commands passed; RSS and Website provider evidence passed; API and Email IMAP deterministic fixtures passed and external live lanes were recorded as `not_applicable_with_reason` because no real external target is available.
- Product total-live live-selected evidence: Example A selected `Boulevard: Staff Product Designer, Platform` from `We Work Remotely — Programming`; Example B selected `How GitHub uses eBPF to improve deployment safety` from `GitHub Blog`; Example C selected `Why Tokyo is the most important tech destination of 2026` from `TechCrunch — Startups`; all had `finalDecision=selected` and `matchedFilterCount=1`.
- Product total-live fixes applied during proof: UI button audit seed now retries Postgres deadlock/serialization conflicts, waits for worker-stable article state before deterministic selection seeding, re-marks the admin moderation fixture as a recent failure immediately before the admin route check, and viewport proof waits longer with diagnostics for criteria processing; reindex-backfill smoke now isolates criterion scope during the reindex replay so cardinality proof remains deterministic in a live populated DB.
- Product total-live proof not run: full diagnostic-mode `pnpm test:product:total-live:compose` was not run after the required-live pass; diagnostic lanes remain separately classifiable by the harness and can be run when the long live-internet matrix is desired.
- Product mega-flow proof completed: latest strict `pnpm test:product:mega-flow:compose` run id `093f6007` passed with `runtimeVerdict=pass`, `yieldVerdict=pass`, `finalVerdict=pass`; artifact `/tmp/newsportal-product-mega-flow-093f6007.json` and `.md`; child live discovery artifact `/tmp/newsportal-product-mega-flow-discovery-6cbd0bec.json` passed for Examples A/B/C; child MCP artifact `/tmp/newsportal-mcp-http-deterministic-215cb065-494e-452d-b2e7-b268a50e102d.json` passed.
- Product mega-flow live-selected evidence: Example A selected live current-window article `Show HN: AI voice screens for hiring managers to save time` from `Hacker News — Ask HN: Who is hiring?`; Example B selected live article `How GitHub uses eBPF to improve deployment safety` from `GitHub Blog`; Example C selected live article `Why Tokyo is the most important tech destination of 2026` from `TechCrunch — Startups`. All three had `finalDecision=selected` and `matchedFilterCount=1`.
- Product mega-flow coverage evidence: A/B/C admin-managed profile truth passed; live graph+recall discovery passed for all three; RSS, Website, API and Email IMAP provider evidence passed; deterministic rejected/filter/duplicate buckets passed; strict live-selected article evidence passed; sequence resource/article/enrichment/digest/cancel/fail/retry buckets passed; Web/Admin/MCP surfaces passed.
- Separate required discovery proofs completed: `pnpm test:discovery:examples:compose` run id `84c5ef4c` passed with `runtimeVerdict=pass`, `yieldVerdict=pass`, `finalVerdict=pass`; `pnpm test:discovery:yield:compose` run id `5f14543e` passed with 3/3 passing runs for Examples A, B and C.
- Static/local proof completed: `node --check infra/scripts/test-product-mega-flow.mjs`, `node --check infra/scripts/lib/product-mega-flow-proof.mjs`, `pnpm unit_tests:ts` (318/318), targeted `PYTHONPATH=. python -m unittest tests.unit.python.test_final_selection tests.unit.python.test_selection_profiles` (22/22), `pnpm lint:ts`, and `pnpm typecheck` passed; `typecheck` retained existing Astro hints but 0 errors.
- Runtime proof completed: `pnpm test:discovery:mega:compose` run id `b57b55db` wrote `/tmp/newsportal-live-discovery-domain-matrix-b57b55db.json` and `.md`; all 27 child runs completed; `runtimeVerdict=pass`, `yieldVerdict=weak`, `finalVerdict=yield_weak`.
- Runtime proof residuals: pass 3/3 for `weworkremotely.com`, `infoq.com`, `github.blog`, `sam.gov`; weak 0/3 for `hnrss.org` with `target_domain_generation_problem`; weak 0/3 for `remoteok.com`, `blog.cloudflare.com`, `ted.europa.eu`, `contractsfinder.service.gov.uk` with `target_domain_review_policy_problem`.
- Full complex proof run: `pnpm test:discovery:admin:compose` passed; `pnpm test:discovery:examples:compose` run id `1eb94981` passed; `pnpm test:discovery:mega:compose` run id `5f4ce70f` completed all 27 child runs with `runtimeVerdict=pass`, `yieldVerdict=weak`, `finalVerdict=yield_weak`; `pnpm test:discovery:yield:compose` run id `c82c5137` passed 3/3 example repeats; `pnpm test:mcp:compose` passed with artifact `/tmp/newsportal-mcp-http-deterministic-d335de54-4f3e-45c2-a716-5fdc2a5bf2f6.json`; `pnpm test:channel-auth:compose` initially exposed localhost crawl-policy cache contamination, then passed after cleanup and a pre-clean test isolation patch; `pnpm test:providers:compose`, `pnpm test:web:viewports`, and `pnpm test:website:compose` passed.
- Cleanup status: compose stack stopped with `pnpm dev:mvp:internal:down`; final `docker ps --format '{{.Names}}'` was empty; current proof artifacts include `/tmp/newsportal-product-total-live-38128383.*`, `/tmp/newsportal-product-mega-flow-695b3c2b.*`, `/tmp/newsportal-product-mega-flow-discovery-ad8f7e3c.*`, `/tmp/newsportal-product-mega-flow-093f6007.*`, `/tmp/newsportal-product-mega-flow-discovery-6cbd0bec.*`, `/tmp/newsportal-live-discovery-examples-84c5ef4c.*`, `/tmp/newsportal-live-discovery-yield-proof-5f14543e.*`, and `/tmp/newsportal-mcp-http-deterministic-215cb065-494e-452d-b2e7-b268a50e102d.*`.

### Recent completed work

- 2026-05-12 closeout: `NEWSPORTAL-OUTSOURCING-DEMAND-DISCOVERY-PRODUCT-TEST-1` is archived in `.aidp/history.md`; heartbeat `continue-tuned-outsourcing-discovery` is paused; product-test DB evidence is retained; compact restore snapshot remains at `docs/product/operator/snapshots/2026-05-12-operational-restore-config/`; one-off `/private/tmp/newsportal*.mjs` helper scripts were removed; no DB cleanup/delete/archive/reset, git stage, commit, or push was performed.

- Current resilient discovery v3 live calibration stage scope: `.aidp/work.md`, `package.json`, `infra/scripts/test-live-discovery-calibration.mjs`, `infra/scripts/lib/discovery-live-calibration.mjs`, and targeted TS unit tests for calibration policies.
- Current resilient discovery v3 live calibration stage out of scope: social/API/email ingress, production/shared DB execution, paid provider use without configured keys, automatic threshold/prompt/policy mutation, and git commit/stage/push.
- Current resilient discovery v3 live calibration accepted plan: add a separate bounded live web/RSS/Atom/website calibration contour with diagnostic, strict acceptance and soak commands; create run-scoped v3 targets/runs, execute them through `discovery.v3.run`, read back coverage/hypotheses/endpoints/contracts/claims/negative-evidence/provider-health/LLM decisions, emit JSON/Markdown artifacts, create replay-fixture pointers, and classify tuning recommendations without applying them.
- Current resilient discovery v3 live calibration blueprint context checked: `.aidp/blueprint.md` Discovery acquisition, PostgreSQL source-of-truth, Runtime surfaces, Task engine/worker boundary; `.aidp/contracts/discovery-agent.md`; `.aidp/contracts/test-access-and-fixtures.md`; `.aidp/verification.md` discovery live/external-provider proof notes.
- Current resilient discovery v3 live calibration required proof: targeted TS unit proof for case validation, verdict policy, tuning recommendation mapping and replay fixture preservation; `node --check` for new harness scripts; `pnpm lint:ts`; `pnpm typecheck`; `git diff --check`; run live compose calibration/acceptance if local provider/network conditions allow, otherwise record explicit residual.
- Current resilient discovery v3 live calibration proof passed: yes for source-level harness and a bounded live guard smoke; `node --check infra/scripts/lib/discovery-live-calibration.mjs`, `node --check infra/scripts/test-live-discovery-calibration.mjs`, `pnpm unit_tests:ts -- discovery-live-calibration` (full TS suite 345/345), `pnpm lint:ts`, `pnpm check:env-sync`, `pnpm typecheck`, `pnpm lint:py`, `pnpm test:migrations:smoke`, and `git diff --check` passed. A first live smoke exposed that sequence runs can terminate as `completed`, so the harness terminal status set was corrected; rerun with `DISCOVERY_ENABLED=1 DISCOVERY_V3_LIVE_EXECUTION=1 DISCOVERY_SEARCH_PROVIDERS=ddgs node infra/scripts/test-live-discovery-calibration.mjs --mode=calibration --cases=negative_evidence_provider_health` passed 3/3 guard iterations with runtime/quality/final verdict `pass`, artifact `/tmp/discovery-live-calibration-085e31ea.json` and `.md`, replay suite `98ca7ed5-4b58-49ee-a922-66df82dbd232`, eval run `08908764-e5e8-4952-afd0-7ddd8800f89f`.
- Current resilient discovery v3 live calibration residual gates: the full `pnpm test:discovery:live-calibration:compose`, strict `pnpm test:discovery:live-acceptance:compose`, and manual/nightly `pnpm test:discovery:live-soak:compose` commands were added but not run end-to-end in this pass because they intentionally execute multiple real web/RSS/Atom/website flows; use the guard-smoke artifact above as the route proof and run the full/strict suites for product polishing before threshold/prompt/policy tuning.

- Current resilient discovery v3 cutover source scope: `database/migrations/0052_resilient_discovery_rebuild.sql`, `.aidp/contracts/discovery-agent.md`, `.aidp/contracts/independent-recall-discovery.md`, `docs/product/operator/discovery/resilient-discovery.md`, `docs/product/README.md`, `infra/scripts/relay/test-migrations.ts`, `services/workers/app/discovery_v3_*.py`, `services/api/app/discovery_v3_api.py`, `services/api/app/routes/discovery_routes.py`, `packages/sdk/src/index.ts`, `services/mcp/src/tools/discovery/v3-tools.ts`, `services/mcp/src/tools/discovery/index.ts`, `services/mcp/src/resources.ts`, `services/mcp/src/prompts.ts`, `apps/admin/src/pages/discovery.astro`, and targeted Python/API/admin/MCP/migration tests.
- Current resilient discovery v3 cutover out of scope: applying the destructive `0052` migration to a shared/runtime database, deleting old worker runtime modules, deleting old MCP/admin source files that are no longer exported, production deploy, real social-provider scraping, or disabling old runtime data without explicit operator approval.
- Current resilient discovery v3 cutover accepted plan: land the source-level v3 domain model and guards before runtime deletion: chunked legacy archive migration, provider registry/health, bounded Constructive Skeptic loop, source evidence contracts/probation, negative evidence, hidden-signal claims and controls, eval replay tables/policies, v3 repository/graph/coverage/hypothesis/orchestrator skeleton, v3 API routes, v3 MCP tools/resources/prompts, SDK v3 methods, and a v3 admin operator workspace showing why-found/why-not-promoted/missing-evidence, contracts, claims, cooldowns and provider health.
- Current resilient discovery v3 cutover proof passed: yes for source-level gates; `PYTHONPATH=. python -m unittest tests.unit.python.test_discovery_v3_policy tests.unit.python.test_discovery_v3_stability tests.unit.python.test_api_discovery_v3_cutover` passed 24/24; `pnpm test:migrations:smoke` passed and verified v3 discovery tables/constraints plus retained ingestion tables; `pnpm --filter @newsportal/sdk typecheck`, `pnpm --filter @newsportal/mcp typecheck`, `pnpm --filter @newsportal/admin typecheck`, `pnpm lint:py`, `pnpm lint:ts`, and `git diff --check` passed.
- Current resilient discovery v3 cutover continuation proof passed: yes; legacy SDK discovery mission/class/profile/recall/candidate methods were removed in favor of v3 target/run/endpoint/contract/claim/provider-health/eval methods; old MCP discovery read/write tool exports were retired and v3 tool/resource/prompt expectations now drive tests; old admin discovery BFF POST now requires the signed discovery action token and returns `410 Gone` instead of proxying old mission/recall routes; `operator.report.verify` accepts v3 `targetIds` and reports queued/running runs, manual-review endpoints and probation contracts. Proof passed with `pnpm unit_tests:ts -- discovery-admin mcp-control-plane mcp-http-doc-parity mcp-http-live-diagnostics` (full TS suite 352/352), `PYTHONPATH=. python -m unittest tests.unit.python.test_discovery_v3_policy tests.unit.python.test_discovery_v3_stability tests.unit.python.test_api_discovery_v3_cutover` (24/24), `pnpm --filter @newsportal/sdk typecheck`, `pnpm --filter @newsportal/mcp typecheck`, `pnpm --filter @newsportal/admin typecheck`, `pnpm lint:ts`, `pnpm lint:py`, `pnpm test:migrations:smoke`, and `git diff --check`.
- Current resilient discovery v3 promotion integration proof passed: yes; v3 endpoint promotion now delegates through the endpoint-aware `PostgresSourceRegistrarAdapter` instead of API-local raw SQL, and the registrar creates `source_channels`, `source_channel_runtime_state`, `outbox_events`, `discovery_source_contracts`, endpoint registration state and `discovery_actions` in one promotion path. Endpoint promotion config starts with `trustStage=probation`, `coverageContribution=0.25`, `downstreamWeight=0.3`, operator config, tags and the Source Evidence Contract; contract evaluation also syncs trust/evidence contract state back into `source_channels.config_json.discovery`. Proof passed with `PYTHONPATH=. python -m unittest tests.unit.python.test_discovery_fetchers_website_probe tests.unit.python.test_discovery_v3_policy tests.unit.python.test_discovery_v3_stability tests.unit.python.test_api_discovery_v3_cutover` (29/29), `pnpm lint:py`, `pnpm test:migrations:smoke`, and `git diff --check`.
- Current resilient discovery v3 coverage/runtime-guard proof passed: yes; deterministic coverage now dedupes source identity before counting strong sources, stores duplicate counts, uses `coverageContribution` for partial probation coverage, and keeps probation sources out of strong coverage. API refresh now uses the same coverage engine instead of a separate row-count implementation. The v3 orchestrator now filters planned hypotheses through negative-evidence cooldowns and provider-health circuit breakers before inserting them, degrading provider budgets instead of treating provider failures as hypothesis failures. Proof passed with `PYTHONPATH=. python -m unittest tests.unit.python.test_api_discovery_v3_cutover tests.unit.python.test_discovery_v3_stability tests.unit.python.test_discovery_v3_policy tests.unit.python.test_discovery_fetchers_website_probe` (31/31), `pnpm lint:py`, and `git diff --check`.
- Current resilient discovery v3 hidden-claim follow-up proof passed: yes; confirmed hidden-signal claims with control comparison now generate only direct `web_search` follow-up hypotheses, while hidden claims without control comparison generate no direct follow-up. The v3 orchestrator loads confirmed claims for the target and appends claim-backed direct-source hypotheses before runtime guard filtering, preserving the rule that hidden/social evidence cannot directly promote `source_channels`. Proof passed with `PYTHONPATH=. python -m unittest tests.unit.python.test_discovery_v3_stability tests.unit.python.test_api_discovery_v3_cutover tests.unit.python.test_discovery_v3_policy` (28/28) and `pnpm lint:py`.
- Current resilient discovery v3 replay-eval proof passed: yes; fixture replay evaluation now measures source precision/recall/noise/reject recall/cost plus hidden-claim precision/recall, and `POST /maintenance/discovery/eval-suites/{eval_suite_id}/run` records a completed `discovery_eval_runs` row from stored eval case fixtures instead of a placeholder queued row. Proof passed with `PYTHONPATH=. python -m unittest tests.unit.python.test_discovery_v3_stability tests.unit.python.test_api_discovery_v3_cutover tests.unit.python.test_discovery_v3_policy tests.unit.python.test_discovery_fetchers_website_probe` (34/34), `pnpm lint:py`, and `git diff --check`.
- Current resilient discovery v3 self-healing/task-plugin/execution-bridge proof passed: yes; added bounded self-healing diagnosis/repair rows so provider/probe failures, coverage stagnation, endpoint-sweep misses, hidden-signal noise, duplicate pressure and stale sources map to queued `discovery_repairs` without treating provider failures as hypothesis failures. Added v3 task-engine plugins for URL/domain clustering, source-directory extraction with outbound-domain follow-up hypotheses, endpoint sweep with localized patterns, endpoint scoring and action decision, registered alongside existing plugins for non-destructive source-level cutover. Added a network-free fixture execution bridge that turns referee-accepted hypotheses into clustered domains, source-directory follow-ups, endpoint candidates, scored actions and negative evidence for no-result branches; the orchestrator now persists fixture bridge endpoints/edges/negative evidence when run fixtures are supplied, otherwise it explicitly leaves live provider execution disabled until operator approval. Proof passed with `PYTHONPATH=. python -m unittest tests.unit.python.test_task_engine_discovery_plugins tests.unit.python.test_task_engine_pipeline_plugins tests.unit.python.test_api_sequence_agent tests.unit.python.test_discovery_v3_stability tests.unit.python.test_api_discovery_v3_cutover tests.unit.python.test_discovery_v3_policy tests.unit.python.test_discovery_fetchers_website_probe` (73/73), `pnpm lint:py`, `pnpm test:migrations:smoke`, and `git diff --check`.
- Current resilient discovery v3 MCP/SDK scenario cutover proof passed: yes; extended SDK and MCP v3 coverage for endpoint/domain/action/contract/claim/negative-evidence/provider-health/identity/eval reads and writes, replaced MCP HTTP discovery operator scenarios and doc-parity expectations with v3 targets/runs/endpoints/contracts/claims/provider-health/eval flows, and removed legacy discovery mission/profile/class/recall/candidate tool references from the exercised MCP/SDK scenario surfaces. Proof passed with `pnpm unit_tests:ts -- mcp-control-plane mcp-http-doc-parity mcp-http-live-diagnostics` (352/352), `pnpm --filter @newsportal/sdk typecheck`, `pnpm --filter @newsportal/mcp typecheck`, `pnpm lint:ts`, `node --check infra/scripts/lib/mcp-http-scenarios.mjs`, `node --check infra/scripts/lib/mcp-http-doc-parity.mjs`, `PYTHONPATH=. python -m unittest tests.unit.python.test_task_engine_discovery_plugins tests.unit.python.test_task_engine_pipeline_plugins tests.unit.python.test_api_sequence_agent tests.unit.python.test_discovery_v3_stability tests.unit.python.test_api_discovery_v3_cutover tests.unit.python.test_discovery_v3_policy tests.unit.python.test_discovery_fetchers_website_probe` (73/73), `pnpm lint:py`, `pnpm test:migrations:smoke`, `git diff --check`, and `rg -n "discovery\\.(profiles|classes|missions|recall|candidates|source_profiles|source_interest_scores|feedback|costs)" infra/scripts/lib/mcp-http-scenarios.mjs infra/scripts/lib/mcp-http-doc-parity.mjs services/mcp/src/tools/discovery packages/sdk/src/index.ts` returning no matches.
- Current resilient discovery v3 MCP prompt/resource cleanup proof passed: yes; MCP session/yield/tuning/review prompts now describe v3 targets, coverage, endpoints, contracts, claims, provider health, negative evidence, eval replay and probation instead of old mission/recall/profile planning, and operator resources now route discovery onboarding through target coverage plus endpoint contract review. `operator.report.verify` schema no longer advertises old discovery `profileIds`/`missionIds`/`recallMissionIds`; discovery health guidance now reports rejected/duplicate endpoints instead of recall candidates. Proof passed with `pnpm unit_tests:ts -- mcp-control-plane mcp-http-doc-parity mcp-http-live-diagnostics` (352/352), `pnpm --filter @newsportal/mcp typecheck`, `pnpm lint:ts`, `node --check infra/scripts/lib/mcp-http-scenarios.mjs`, `node --check infra/scripts/lib/mcp-http-doc-parity.mjs`, `git diff --check`, and `rg -n "discovery\\.mission|discovery\\.recall|discovery\\.candidate|discovery\\.profile|profiles, missions|recall missions|recall candidates|mission/candidate|profileId|missionId|recallMissionId" services/mcp/src/prompts.ts services/mcp/src/resources.ts services/mcp/src/context.ts services/mcp/src/tools.ts tests/unit/ts/mcp-control-plane.test.ts` returning no matches.
- Current resilient discovery v3 API dependency decoupling proof passed: yes; FastAPI route registration already uses the v3 discovery router, and `services/api/app/route_deps.py` no longer requires legacy discovery mission/recall/candidate/profile/class functions to construct the API app context. Old Python legacy modules are still present for the explicit approval-gated cleanup phase, but they are no longer part of the route dependency registry. Proof passed with `PYTHONPATH=. python -m unittest tests.unit.python.test_api_discovery_v3_cutover tests.unit.python.test_api_sequence_agent` (13/13), `pnpm lint:py`, and `rg -n "discovery_(mission|recall|candidate|profile|class|source|portfolio|feedback|cost)" services/api/app/route_deps.py services/api/app/routes/discovery_routes.py` returning no matches.
- Current resilient discovery v3 local runtime cutover proof passed: yes; after the recorded operator approval, local `schema_migrations` was checked, `pnpm test:migrations:smoke` passed, `pnpm db:migrate` applied `0052_resilient_discovery_rebuild.sql`, and DB read-back verified old mission/candidate/recall tables gone, retained ingestion tables present, v3 tables/views present, and `discovery_legacy_archive_batches` populated for legacy archive batches. Runtime proof promoted a v3 RSS endpoint through `PostgresSourceRegistrarAdapter`, created `source_channels`, `source_channel_runtime_state`, `outbox_events`, `discovery_source_contracts`, endpoint registration state and `discovery_actions`, then contract evaluation moved trust from `probation/0.25/0.3` to `active/1.0/1.0`. Bounded live execution is now available behind explicit run summary/env enablement; provider failures update `discovery_provider_health` and repairs, not hypothesis failure evidence. Old admin mission/recall/candidate/profile/source pages were removed, the admin compose acceptance was rewritten for v3 targets/endpoints/contracts/claims/negative-evidence/provider-health/eval surfaces, and `pnpm test:discovery:admin:compose` passed. A v3 domain list runtime bug found by MCP compose was fixed by filtering `discovery_domain_inventory` with `first_seen_target_id`; regression test added. Final proof passed with `PYTHONPATH=. python -m unittest tests.unit.python.test_api_discovery_v3_cutover`, `pnpm lint:py`, `pnpm lint:ts`, `pnpm typecheck`, `node --check infra/scripts/test-discovery-admin-flow.mjs`, `pnpm test:discovery:admin:compose`, `pnpm test:mcp:compose -- --scenario=discovery-operator-flows` (artifact `/tmp/newsportal-mcp-http-deterministic-6698b273-37d8-4357-8c8a-8599d1402f06.*`), and `git diff --check`.
- Current resilient discovery v3 approval-gated cleanup proof passed: yes; after the local runtime cutover proof, the remaining active legacy discovery runtime references were removed from FastAPI registration and status constants, old mission/recall/candidate/profile/class/source-quality API helper modules were deleted, unused legacy worker planning/execution/evaluation/recall/candidate/source-quality/policy/cost modules were deleted, old unit tests tied to removed legacy modules were retired, and compose/MCP/product discovery proof harnesses were moved to v3 targets/runs/endpoints/contracts/claims/provider-health/eval read-backs. `services/api/app/main.py` now registers only the v3 discovery route family; `services/api/app/route_deps.py` remains decoupled from legacy discovery functions; MCP discovery HTTP scenarios no longer exercise old mission/recall/candidate tools; live discovery/domain/yield/product proof formatters now report v3 endpoints instead of legacy candidates. Proof passed with `python -m ruff check services/api/app/main.py services/api/app/status_constants.py`, `PYTHONPATH=. python -c "import services.api.app.main as m; print('ok', len(m.app.routes))"`, `pnpm lint:py`, `pnpm lint:ts`, `pnpm typecheck`, `PYTHONPATH=. python -m unittest discover -s tests/unit/python -p 'test_*.py'` (319/319), `pnpm unit_tests:ts -- mcp-control-plane discovery-admin admin-operator-surfaces mcp-http-doc-parity mcp-http-live-diagnostics` (340/340), `pnpm test:migrations:smoke`, `DISCOVERY_ENABLED=1 PYTHONPATH=. python -m infra.scripts.workers.smoke discovery-enabled`, `pnpm test:discovery:admin:compose`, `pnpm test:discovery:examples:compose` (artifact `/tmp/newsportal-live-discovery-examples-bb566d77.json` after UUID fix also used by nonregression), `pnpm test:discovery:yield:compose` (artifact `/tmp/newsportal-live-discovery-yield-proof-33d895b7.json`), `pnpm test:mcp:http:discovery` (artifact `/tmp/newsportal-mcp-http-deterministic-2677c8d6-16dd-4cc7-a7be-7b145ca5c657.json`), `pnpm test:discovery:domains:compose` (artifact `/tmp/newsportal-live-discovery-domain-matrix-2ee67265.json`), `pnpm test:discovery:mega:compose` (artifact `/tmp/newsportal-live-discovery-domain-matrix-8fa52eae.json`), `pnpm test:discovery:nonregression:compose` (artifact `/tmp/newsportal-discovery-nonregression-1b21b83e.json`), `git diff --check`, and repository scans showing no active `discovery_missions`/`discovery_candidates`/`discovery_recall_*` table references under `services`, `tests`, `apps`, `infra/scripts`, or `packages`; only negative assertions remain for removed legacy BFF exports.
- Current resilient discovery v3 cutover residual gates: production deployment, shared/prod database migration, and any physical removal of now-unreferenced legacy Python helper files outside the v3 route/plugin/export path remain separate approval-gated cleanup/deploy tasks. Social providers remain `monitor_only`/`needs_config`; website auto-promotion remains disabled by default.
- Current resilient discovery v3 intelligence completion proof passed: yes; added additive `0053_discovery_v3_intelligence_layer.sql`, discovery LLM task templates/decision logging, schema-validating cached/fallback LLM gateway, autopilot profiles/config simplification, runtime LLM graph expansion, bounded Explorer -> Constructive Skeptic -> Repairer -> Verification Skeptic -> Referee integration in the orchestrator, multi-generation discovery loop, search fanout adapter/provider votes, bounded source-directory extraction, source expansion/replacement planning, API/SDK/MCP/admin surfaces for simple target creation, explain/diagnose, LLM decisions and source expand/replace. Proof passed with `PYTHONPATH=. python -m unittest tests.unit.python.test_discovery_v3_intelligence`, `PYTHONPATH=. python -m unittest tests.unit.python.test_discovery_v3_policy tests.unit.python.test_discovery_v3_stability tests.unit.python.test_api_discovery_v3_cutover tests.unit.python.test_discovery_v3_intelligence` (43/43), `pnpm --filter @newsportal/sdk typecheck`, `pnpm --filter @newsportal/mcp typecheck`, `pnpm --filter @newsportal/admin typecheck`, `pnpm typecheck`, `pnpm test:migrations:smoke`, `pnpm lint:py`, `pnpm lint:ts`, `pnpm unit_tests:ts -- mcp-control-plane` (340/340), `pnpm test:discovery:admin:compose`, `pnpm test:mcp:http:discovery` (artifact `/tmp/newsportal-mcp-http-deterministic-97cfd164-fdf4-45c0-a64b-c2b976da887e.json`), `pnpm test:discovery:nonregression:compose` (artifacts `/tmp/newsportal-live-discovery-examples-38be8843.json` and `/tmp/newsportal-discovery-nonregression-14b76295.json`), and `git diff --check`.
- Last completed item this session: `NEWSPORTAL-REINDEX-SUPERSESSION-CANCEL-CAPABILITY-1`.
- Outcome: added reindex job supersession keys, cooperative queued/running cancellation, stale-running migration repair, worker batch-boundary cancellation handling, admin BFF Cancel action, live cancelling counts, and Recent Jobs Cancel controls; targeted proof, lint, typecheck, and migration smoke passed.
- Cleanup status: no destructive cleanup was run; existing local runtime rows/compose state were treated as pre-existing audit evidence.
- Last archived item: `NEWSPORTAL-RELEASE-READY-COMPLIANCE-CAPABILITY-STAGE-32`.
- Outcome: added the repo-owned non-deploy `pnpm release:verify` gate, production image content smoke and release verification wiring, then repaired deterministic MVP proof seed state so the full release gate completes against consistent canonical document state.
- Cleanup status: `pnpm release:verify` ran product-local cleanup/down; final `docker ps --format '{{.Names}}'` was empty; no commit/stage/branch action was performed.

### Known next candidates

- Current resilient discovery v3 extended live-polish stage:
  - ID: NEWSPORTAL-DISCOVERY-V3-EXTENDED-LIVE-POLISH-STAGE-1
  - Item status: active.
  - Lifecycle mode: normal.
  - Work route: capability.
  - Route phase: implementation/proof.
  - Parent capability: Resilient Discovery V3 capability completion and live calibration.
  - Planning/spec source/status: external-spec accepted-for-this-item from the user-provided Discovery V3 Extended Live Examples Plan.
  - Scope: add a separate extended live-polish case set for security advisories, primary/open data, report/research libraries, localized PL/DE procurement, regulatory/policy watches, and vendor ecosystem expansion; add root scripts; extend role/query/classification support only where needed for those live examples; add targeted TS/Python proof and bounded live diagnostic/acceptance evidence when feasible.
  - Out of scope: production/shared DB migration, paid provider live usage, social/API/email live ingress, website auto-promotion, automatic threshold/policy edits without replay evidence, git commits/staging.
  - Blueprint context checked: Discovery live search/LLM budget, Source model vs provider adapters, PostgreSQL source-of-truth, Worker/selection/discovery work, and test/runtime boundaries.
  - Risk: medium; external live web/RSS/website nondeterminism is bounded by repeat gates and report-only tuning artifacts.
  - Approval required: no for local source/test harness edits and bounded local compose proof; yes for production/shared DB changes, paid providers, deployment, destructive cleanup, or social/API/email live ingress.
  - Required proof: `pnpm unit_tests:ts -- discovery-live-calibration`; targeted Python discovery tests for endpoint classification/hypothesis role support; `pnpm lint:py`; `pnpm lint:ts` or targeted JS syntax/lint if full lint is too broad; `git diff --check`; at least targeted extended live diagnostic proof if local compose/network permits.
  - Proof passed: yes for the extended live-polish source/harness changes and bounded live diagnostic proof. Added extended case packs and scripts for security advisories, primary/open data, report/research libraries, localized PL/DE procurement, regulatory/policy watches, and vendor ecosystem expansion; added role-specific query/classification support for regulatory policy and localized procurement; made live reports read all endpoint pages and include endpoint examples with why-found/why-not-promoted/missing-evidence; added settled-state rereads for large endpoint sets; fixed seed role selection to respect explicit `sourceRoleTargets`. Static/unit proof passed with `node --check infra/scripts/lib/discovery-live-calibration.mjs`, `node --check infra/scripts/test-live-discovery-calibration.mjs`, `PYTHONPATH=. python -m unittest tests.unit.python.test_discovery_v3_policy tests.unit.python.test_discovery_v3_stability` (36/36), `pnpm unit_tests:ts -- discovery-live-calibration` (348/348), `pnpm lint:py`, `pnpm lint:ts`, and `git diff --check`.
  - Live proof passed: yes; targeted PL/DE procurement diagnostic passed with artifact `/tmp/discovery-live-calibration-68a8ecb6.*`; targeted regulatory policy diagnostic passed after endpoint pagination/stabilization with artifact `/tmp/discovery-live-calibration-f3c49024.*`; targeted report/research diagnostic passed after explicit role-target seed selection with artifact `/tmp/discovery-live-calibration-52ce10e8.*`. Final full extended diagnostic `pnpm test:discovery:live-polish:compose -- --repeat=1` passed with artifact `/tmp/discovery-live-calibration-7a9df566.json` and `.md`, runtime/quality/final verdict `pass`, all six extended cases passing 1/1, replay suite `59dfd7b1-14f8-4ef4-ab0b-1d4a33d9fac6`, replay eval run `03fcc548-4a76-4940-a386-804a39f6fe8a`.
  - Extended acceptance proof passed: yes; the full readiness sweep passed targeted Python discovery suites (`tests.unit.python.test_discovery_v3_policy`, `test_discovery_v3_stability`, `test_api_discovery_v3_cutover`, `test_discovery_v3_intelligence`, `test_task_engine_discovery_plugins`, `test_task_engine_pipeline_plugins`, 75/75), `pnpm unit_tests:ts -- discovery-live-calibration mcp-control-plane discovery-admin mcp-http-doc-parity mcp-http-live-diagnostics` (348/348), `pnpm test:migrations:smoke`, `pnpm lint:py`, `pnpm lint:ts`, `pnpm typecheck`, and `git diff --check`. Core strict live acceptance `pnpm test:discovery:live-acceptance:compose` passed with artifact `/tmp/discovery-live-calibration-6b339a65.*`, runtime/quality/final verdict `pass`, all six core cases 3/3, replay suite `803edc0f-a56c-4507-a8a7-a0728f03582d`, replay eval run `e5bb730e-3f24-4cf9-a78e-4bb548770b96`. Extended strict live acceptance `pnpm test:discovery:live-polish-acceptance:compose` passed with artifact `/tmp/discovery-live-calibration-7e2e7ae9.*`, runtime/quality/final verdict `pass`, all six extended cases 3/3, replay suite `865bc6ec-d33e-4d26-98c5-aae5f9b43913`, replay eval run `01e3704c-42a7-4195-bc28-9a1e95ad794a`.
  - Extended soak proof passed: yes; `pnpm test:discovery:live-polish-soak:compose` passed with artifact `/tmp/discovery-live-calibration-118b9fe4.*`, runtime/quality/final verdict `pass`, all six extended cases 5/5, replay suite `892a353a-9370-4ff4-b01b-9274a20a7075`, replay eval run `22837663-9eb3-4f56-91e1-2bac9acbcb42`. Root-cause buckets remained evidence-only tuning signals: `provider_health_event` 30 and `coverage_not_improving` 5 for localized PL/DE procurement, consistent with review-only website endpoints and circuit-breaker telemetry rather than hypothesis failure.
  - Admin/MCP/nonregression proof passed: yes; `pnpm test:discovery:admin:compose` passed v3 operator workspace acceptance for targets/endpoints/contracts/claims/negative-evidence/provider-health/eval guard actions; `pnpm test:mcp:http:discovery` passed with artifact `/tmp/newsportal-mcp-http-deterministic-9aeb9e3e-4ad1-4530-9704-adb2f2270f6c.*`; `pnpm test:discovery:nonregression:compose` passed with artifacts `/tmp/newsportal-discovery-nonregression-51122f2d.*` and `/tmp/newsportal-live-discovery-examples-e4a603e6.*`.
  - Residual gates: no local readiness gate from the requested sweep remains failing. Production/shared DB migration, paid provider live usage, social/API/email live ingress, production deploy, destructive cleanup, and any policy/threshold/prompt tuning remain separate approval-gated work. Provider-health events remain a tuning/observability track; website auto-promotion remains disabled, and website endpoints stay review/manual-review only.
  - Cleanup status: local compose stack may remain running from the proof commands; no destructive cleanup, git stage, commit, or push was performed.
- Optional next candidate: narrow profile/policy tuning for consistently weak matrix domains, then rerun `pnpm test:discovery:mega:compose`.
- Current resilient discovery v3 live calibration proof passed: yes for runtime/live web-RSS-Atom-website execution; full calibration command `pnpm test:discovery:live-calibration:compose` completed with artifact `/tmp/discovery-live-calibration-00e72023.json` and `/tmp/discovery-live-calibration-00e72023.md`, runtime verdict `pass`, final verdict `pass`, replay suite `2992e13d-0a52-4713-bd34-134ea3f35da4`, replay eval run `06bea808-4cce-48fb-8b3b-0d452a472972`. The full 6-flow pass showed `bootstrap_rss_atom` 3/3, `existing_source_expansion` 3/3, `replacement_discovery` 3/3, `source_directory_extraction` 3/3, and `negative_evidence_provider_health` 3/3; `gap_procurement_web` remained quality-weak at 0/3 with root causes `provider_health_event`, `review_policy_problem`, and `coverage_not_improving`. Earlier live diagnostic failures in this route exposed and were fixed for UUID JSON serialization in coverage snapshots, invalid `source_expansion` endpoint signal mode, unbounded source expand/replace run payloads, provider-health FK leakage through synthetic `fetchers`, missing endpoint novelty causing duplicate rejection, missing seed URL/domain simple-target support, RSS probe evidence mapping for `is_valid_rss`/`sample_entries`, and RSS auto-promotion/contract creation. Targeted post-fix calibration artifacts `/tmp/discovery-live-calibration-3108b7cc.*` and `/tmp/discovery-live-calibration-73ca4b6f.*` showed bootstrap RSS/Atom passing 3/3 with probation/partial coverage while procurement remained the tuning target. Proof also passed with `PYTHONPATH=. python -m unittest tests.unit.python.test_discovery_v3_stability tests.unit.python.test_discovery_v3_policy tests.unit.python.test_api_discovery_v3_cutover tests.unit.python.test_task_engine_discovery_plugins` (52/52), `pnpm unit_tests:ts -- discovery-live-calibration` (345/345), `pnpm lint:py`, and `git diff --check`.
- Current resilient discovery v3 live calibration residual gates: strict `pnpm test:discovery:live-acceptance:compose` was not run after the latest full calibration because the known `gap_procurement_web` weak result would intentionally fail the MVP quality gate. Next tuning should focus only on procurement web discovery: better procurement-specific seed URLs/domains/query templates, missing-evidence explanations from website probes, provider-health noise classification, and website manual-review thresholds, while keeping website auto-promotion disabled and proving any threshold/prompt/policy change through the recorded replay eval path before claiming quality improvement.
- Current resilient discovery v3 live procurement polish proof passed: yes; procurement-specific live tuning added official procurement seed URLs/domains for SAM.gov, TED Europa, and Find a Tender, expanded procurement query templates, made seed URL/domain hypotheses inherit the missing procurement role instead of defaulting to technical change, classified official procurement opportunity/search paths as procurement endpoints, preserved URL-validation evidence when merging website probe evidence, and allowed operator-supplied procurement seed URLs to become `manual_review` candidates with explicit missing evidence while still preventing website auto-promotion. Targeted `pnpm test:discovery:live-calibration:compose -- --cases=gap_procurement_web` passed with artifact `/tmp/discovery-live-calibration-8ee4915d.json` and `/tmp/discovery-live-calibration-8ee4915d.md`, runtime/quality/final verdict `pass`, `gap_procurement_web` 3/3, three reviewable procurement website endpoints per iteration, replay suite `eecaecde-2ffb-4b39-8abc-5443c30e478e`, and eval run `74b8a7a9-d6d9-4352-bc8a-68d4cacdc2d1`.
- Current resilient discovery v3 live acceptance proof passed: yes; full `pnpm test:discovery:live-calibration:compose` after procurement tuning passed with artifact `/tmp/discovery-live-calibration-20dc43f9.json` and `/tmp/discovery-live-calibration-20dc43f9.md`, runtime/quality/final verdict `pass`, all six case families 3/3, replay suite `6ef0948f-c7ed-4f6a-8484-08ae75cb0799`, eval run `0c3fbb60-955e-4417-8868-b8a97a903278`. Strict `pnpm test:discovery:live-acceptance:compose` initially hit only local Docker socket sandbox denial, then passed after approved compose access with artifact `/tmp/discovery-live-calibration-d4b1905f.json` and `/tmp/discovery-live-calibration-d4b1905f.md`, runtime/quality/final verdict `pass`, all six case families 3/3, replay suite `9b90e70a-d6fa-41ba-8c9c-c42218916002`, eval run `9195d13a-6752-426c-b93f-c2fa348c0221`. Proof passed with `PYTHONPATH=. python -m unittest tests.unit.python.test_discovery_v3_stability tests.unit.python.test_discovery_v3_policy` (35/35), `pnpm unit_tests:ts -- discovery-live-calibration` (345/345), `pnpm lint:py`, and `git diff --check`.
- Current resilient discovery v3 live acceptance residual gates: provider-health events still appear in every live run and should remain a circuit-breaker/tuning track rather than hypothesis failure evidence; duplicate pressure remains visible in RSS/bootstrap and replacement flows, so the next polish should be identity dedupe/novelty budget tuning backed by the replay suites above. Website auto-promotion remains disabled; procurement endpoints are reviewable only and carry missing evidence rather than source-channel promotion.

- Current outsourcing-demand discovery product-test quality audit update:
  - User follow-up asked whether LLM template types and system interests are duplicate/confusing after the product flow produced web-visible articles but quality review showed many broad technology/business items.
  - Lifecycle mode: normal. Work route: capability. Product-test DB retention/no-cleanup still applies.
  - MCP-only product-ops proof completed: audited current public items and template/interest configuration through MCP read-back; no raw SQL mutations, no cleanup/deletes.
  - Initial quality audit artifact `/tmp/newsportal-mcp-quality-audit-540f318a.json`: 57 recent public items classified heuristically as 3 strong buyer signals, 4 probable project signals, 9 weak context signals, and 41 reject/noise items.
  - Precision tuning artifact `/tmp/newsportal-mcp-precision-tuning-dc25c381.json`: created stricter buyer-intent interests/template and expanded geography including GCC/MENA/global while excluding Russia, Belarus, North Korea, and China; result was 70 recent public items with 6 strong, 9 probable, 13 weak, and 42 noisy items.
  - Legacy tightening artifact `/tmp/newsportal-mcp-tighten-legacy-interests-00674b9a.json`: tightened 10 broad/legacy duplicated interests via MCP and reindexed; result was 21 recent public items, 37 selected articles, 1586 rejected articles, with 5 strong, 5 probable, 5 weak, and 6 noisy items.
  - Duplicate audit artifact `/tmp/newsportal-mcp-template-duplicate-audit-27cc3c10.json`: confirmed system interests and LLM prompt templates are distinct types, but retained active copies exist from repeated product-test runs; active counts were 56 interests and 7 LLM templates, with 8 interest duplicate-name groups and 1 LLM duplicate-name group.
  - Added read-only MCP operation `templates.duplicates.audit` in `services/mcp/src/tools/templates-tools.ts` because duplicate/template audit was missing as a first-class MCP product operation.
  - Native MCP proof artifact `/tmp/newsportal-mcp-native-template-duplicate-audit-f8aadb61.json`: `templates.duplicates.audit` returned 56 active interests, 7 active LLM templates, 8 interest duplicate-name groups, 1 LLM duplicate-name group, 2 proof-only bridge/high-recall interests, and recommended archive-not-delete consolidation.
  - Proof passed: `pnpm --filter @newsportal/mcp typecheck`; `pnpm unit_tests:ts -- mcp-control-plane` (349/349) including `templates.duplicates.audit`; `git diff --check`; docker compose rebuilt/restarted the `mcp` service with the new tool; read-only MCP call to `templates.duplicates.audit` passed.
  - Cleanup status: no destructive cleanup, delete, archive, git stage, or commit was performed. DB, articles, selected rows, templates, interests, channels, and proof artifacts remain retained.
  - Follow-up product-test continuation created Codex heartbeat `continue-tuned-outsourcing-discovery` every 45 minutes; goal is quality-first 50-60 tuned recent articles, not count-only filling.
  - MCP consolidation/tuning artifact `/tmp/newsportal-mcp-tuned-yield-2c81008d.json`: archived 40 duplicate/proof-only interests and 4 duplicate LLM templates through MCP archive (no delete), created 5 canonical interests, ran discovery/reindex, but over-tightened to 0 public items.
  - Recovery/tuning artifacts: `/tmp/newsportal-mcp-tighten-legacy-interests-25b7e44e.json` and `/tmp/newsportal-mcp-criteria-compile-recover-61428c28.json` confirmed reindex completed but public items stayed 0; `/tmp/newsportal-mcp-zero-selection-diagnose-115306a8.json` showed system criteria were present but `places` and `short_tokens_required` hard gates blocked all selection.
  - Recall recovery artifact `/tmp/newsportal-mcp-broad-hold-recall-d32f39e1.json`: relaxed hard geo/short-token gates through MCP and reindexed; public items recovered to 103 recent, but quality audit showed 26 strong/probable, 23 weak, and 54 noise, so this was too broad.
  - Precision/noise artifact `/tmp/newsportal-mcp-precision-noise-002f921c.json`: added noise exclusions through MCP and reindexed; result was 29 recent public items, 6 strong/probable, 9 weak, 14 noise. This improved precision but confirmed the shortage should be solved by source expansion, not by loosening filters.
  - Quality source expansion artifact `/tmp/newsportal-mcp-quality-source-expansion-c14798ec.json`: onboarded/applied 23 public-sector/procurement/modernization sources through MCP, observed fetch evidence on 13 new/updated channels, launched discovery source expansion/direct scan, reindexed, and verified 38 recent public items with 10 strong/probable, 7 weak, 21 noise. Discovery run remained queued, so later heartbeat should check/promote/repair discovered endpoints and continue source discovery.
  - Heartbeat crumb-source iteration artifact `/tmp/newsportal-mcp-crumb-source-expansion-bd36031b.json`: updated the heartbeat prompt to encode the domain-specific crumb-source model; onboarded/applied 44 additional low-yield/high-signal public-sector/procurement/channel/security/regional tender sources through MCP, launched discovery expansion, waited for fetch evidence, reindexed, and verified 70 recent public items with 15 strong/probable, 16 weak, and 39 noise. This improves raw and candidate volume but still does not meet the quality-first 50-60 article target.
  - Heartbeat strict crumb-source iteration artifact `/tmp/newsportal-mcp-crumb-source-expansion-2-5a4d6d8d.json`: corrected an MCP client contract error (`system_interests.list` `pageSize` must be <= 100), then through MCP archived 20 active duplicate ODPT interests and 3 duplicate LLM templates without deletion, created 5 canonical strict buyer-demand interests plus 1 strict multilingual LLM reviewer, onboarded/applied 34 of 49 additional official/procurement/security/public-sector/regional sources, launched source expansion/direct discovery, waited for fetch evidence, and completed reindex job `59a51bea-b85f-4e95-b44c-aabc5dd5ba49`. Result: 78 recent public items, but only 16 strong/probable and 8 weak under stricter buyer/project scoring; noisy items remain 54, proving that quality target is still source-limited rather than filter-limited.
  - Production-like web-selection correction artifacts: `/tmp/newsportal-mcp-strict-evidence-groups-bb122a8e.json`, `/tmp/newsportal-mcp-strict-evidence-groups-3110e8ed.json`, `/tmp/newsportal-mcp-archive-legacy-odpt-13af6776.json`, and `/tmp/newsportal-mcp-archive-legacy-odpt-33bf9a58.json`. User clarified that web must always show only actually selected items, not a broad audit pool. Investigation through MCP `content_items.explain` showed the 78 web-visible items were legitimately `final_selection_results.selected`, but selection was polluted by 25 still-active historical ODPT interests and by final-selection aggregation counting stale `interest_filter_results` from disabled criteria. Fixed runtime selection path so strict configured candidate-signal profiles keep zero-hit explain evidence and `upsert_final_selection_result` aggregates only enabled criteria. Through MCP archived 25 legacy ODPT interests without deletion, leaving only 5 current strict canonical interests active, then replayed selection. Result after reindex job `17e08828-9f1f-4854-b00f-45b8fc9b58f0`: active interests 5, public web count 0, final-selection selected count 0, rejected count 2823; web proof rendered `0 content items in the system-selected collection` and `No content yet`. This is production-like behavior: no noisy articles are displayed as selected.
  - Current residual risk: the product flow is now under-yielding rather than over-showing; next iterations should repair/promote high-quality sources and discover more official procurement/public-sector/compliance implementation feeds until real strict-selected articles appear, without broadening selection just to hit 50-60.
  - Heartbeat 2026-05-09 MCP/runtime continuation: added read-only MCP `system_interests.compile_status.list` to prove active interest criteria/profile compilation instead of inferring from worker index logs. Proof artifact `/tmp/newsportal-mcp-centroid-issue-readback-7ed9352e.json` showed 8 active interests, 8 compiled active criteria, 8 active selection profiles, blockerCount 0, stale pass-through 0.
  - Runtime defect found and fixed: MCP backfill job `21bbc4a9-bb46-47d6-a2dd-a03fcec910a1` failed after 100/4452 articles with `list object has no attribute get`; added traceback persistence to reindex failures, reran, and artifact `/tmp/newsportal-mcp-backfill-after-compile-readback-55d3c681.json` traced it to Gemini review JSON parsing a provider array response. Fixed `services/workers/app/gemini.py` to coerce array-shaped provider JSON into a review object with shape warning. Proof passed with `PYTHONPATH=. python -m unittest tests.unit.python.test_gemini tests.unit.python.test_reindex_backfill_progress tests.unit.python.test_interest_auto_repair` (40/40, approved local fake-provider port), `git diff --check`, and rebuilt worker.
  - Post-fix MCP backfill artifact `/tmp/newsportal-mcp-backfill-after-compile-readback-ffa02ad8.json`: job `d32fc951-ff69-44f0-a718-1bc4aaa574ff` completed 4453/4453 articles with no runtime failure; selection remained quality-strict with 0 selected/web-visible and 6 gray-zone holds, proving the blocker moved from runtime crash to selection policy/source quality.
  - MCP quality tuning iteration artifacts: `/tmp/newsportal-mcp-grayzone-quality-audit-634c6bd0.json` identified candidate-signal holds and noisy/generic fallback evidence; `/tmp/newsportal-mcp-candidate-evidence-tune-72e30da2.json` updated 8 active interests through MCP with configured positive/negative candidate evidence groups and broad/disabled review policy, then reindexed via job `6f8e3bf4-fb66-42f6-b5bc-ad0da6fafbb8`. Read-back `/tmp/newsportal-mcp-tuned-backfill-readback-a2f1c06b.json` completed 4454/4454, produced 113 selected rows and 51 web-visible content items, but audit showed many weak SMB training/advice/vendor items, so this was treated as recall proof, not final success.
  - MCP precision tightening iteration artifacts: `/tmp/newsportal-mcp-noise-tighten-4e5f21ee.json` added noise exclusions through MCP for training/workshop/advice/vendor/HR/workplace/non-tech grant pages and launched reindex job `97e0807a-86be-4c32-a9e6-e2a4931a4672`; read-back `/tmp/newsportal-mcp-tightened-readback-df8a255c.json` completed 4455/4455 and reduced output to 24 selected rows, 10 web-visible content items, stalePassThroughCount 0. Remaining visible items include a few strong/probable signals (Business.gov.au cyber resilience grant, Home Office biometrics modernisation supplier engagement, GDS One Login rollout) plus residual weak/noisy AI/advice/vendor-channel items.
  - Heartbeat source-pool continuation artifact `/tmp/newsportal-mcp-quality-source-continue-c603df3b.json`: through MCP onboarded/applied 46 source-channel updates from 39 public-sector/procurement/channel/regional sources, observed fetch evidence for all 46 channel ids, completed reindex job `863a1f39-072c-4596-9570-0f54a820a189`, and verified production-like selection stayed strict: 26 selected rows, 11 web-visible content items, stalePassThroughCount 0. Heuristic article audit found 4 strong/probable, 2 weak, and 5 noisy/unknown visible items; this improved source pool evidence but not the 50-60 quality target.
  - Local SMB/community source iteration artifacts `/tmp/newsportal-mcp-local-smb-community-735d9b0e.json`, `/tmp/newsportal-mcp-local-smb-final-readback-ee995c9f.json`, and `/tmp/newsportal-mcp-local-smb-postcomplete-36e8e020.json`: based on user review that the best crumbs likely live in local small-business/chamber/community/MSP/Hacker News style feeds, created/updated a narrow MCP system interest for local SMB contractor-seeking asks, created an LLM reviewer template, repaired degraded `rss`/`website`/`web_search` providers through MCP, created target `938a2a22-e21d-4d56-965a-1c2346cc2bd0`, planned 42 community/local sources, applied 33 channels, observed fetch evidence on 16, and executed hidden/direct discovery sequences. Hidden/direct discovery completed but produced no new claims/endpoints (`no_new_evidence` diagnosis); Reddit/X/custom social providers are disabled, so social seeds remained monitor-only and were not promoted as article sources. Reindex job `56ab8ee3-2d9f-48c0-bd9e-cef0b4aea58b` completed 4875/4875 with 426 criterion LLM reviews; post-completion read-back still showed 11 web-visible content items, 26 selected rows, 475 hold/gray-zone final decisions, stalePassThroughCount 0, and no missing interest-filter results. This confirms the next product gap is not display gating but discovery/source execution quality for community/social crumbs and a hold-review path for borderline community items.
  - Heartbeat queued expansion/hold audit artifact `/tmp/newsportal-mcp-hold-expand-9ec58220.json`: through MCP `articles.residuals.list` inspected the hold bucket and executed 8 queued `source_expand` discovery runs for local-SMB target `938a2a22-e21d-4d56-965a-1c2346cc2bd0` via MCP-created sequences. All 8 runs completed but diagnosed `no_new_evidence` and produced 0 endpoints. Selection remained unchanged at 26 selected rows, 475 gray-zone/hold rows, 5153 rejected rows, 11 web-visible content items, and stalePassThroughCount 0. First-page hold audit found 35 weak candidates and 15 likely-noise rows, mostly SBA contracting/grant/disaster-assistance pages held with `candidate_signal_hold`; no strong local buyer-ask candidate was found, so broadening hold policy would reduce quality. Current discovery warning remains that additional discovery runs are still queued/running.
  - Current residual state: product chain is now proven end-to-end through MCP with DB retained and web showing only selected items, but the quality-first 50-60 article goal is not met. Latest quality setting is precision-heavy: 11 web-visible items and 475 held/gray-zone rows, below target. The next product improvement should be first-class MCP/social/community provider configuration or better source-expansion execution for forum/community feeds, plus source-specific exclusions for generic SBA navigation/grant pages; it should not loosen selected-web filters or promote hold rows without stronger buyer/project evidence.
  - SMB/community technology-feed expansion update 2026-05-09: user asked for additional no-auth/feed surfaces where small/mid-business outsourcing signals may appear, especially technology/community/problem-solving sites. External source research identified HNRSS/Hacker News query feeds, Google News search RSS, Stack Overflow/Server Fault tag feeds, Atlassian/Webflow/Zapier/Shopify/Odoo/WordPress community surfaces, and SMB/MSP/channel/security/regional business publications as relevant crumb-source pools. Product operations remained MCP-first; web research was only used to identify public feed surfaces.
  - MCP max-funnel product artifact `/tmp/newsportal-mcp-smb-max-funnel-e0df18b3.json`: through MCP created/updated 6 ODPT funnel system interests plus one independent SMB max-funnel LLM reviewer, created target `68316100-0947-4aa2-a8c4-017bc5a85dec`, planned 83 sources and applied 68 channels across Google News RSS, Stack Exchange technology tags, Atlassian/Webflow/community surfaces, SMB/MSP/channel/security/regional sources, evaluated 20 source priors, launched 24 expansion runs and direct discovery run `3dad34d1-522d-4bf0-a995-069ea9c158fe`, and queued reindex job `2bccd56d-ca51-42bc-8b0d-ff4bb1a1edfd` (still running when artifact was written). Read-back at artifact time: 30 selected, 503 gray-zone, 5944 rejected, stalePassThroughCount 0. This was treated as funnel expansion, not final success, because selected rows were still weak/quality-risk.
  - Product defect found and fixed during HNRSS onboarding: bulk-onboarding row identity stripped query strings, so query-backed RSS sources such as HNRSS and Google News search RSS could be incorrectly treated as duplicates even when the query defined a distinct rare-signal feed. Fixed `packages/control-plane/src/channel-bulk-onboarding.ts` so row identity preserves semantic query params for HNRSS, Google News RSS search, and Reddit search RSS while still stripping tracking/count/pagination params. Added TS regression coverage in `tests/unit/ts/admin-bulk-channel-import.test.ts`.
  - Second product defect found and fixed: `channels.bulk_onboard.plan` could correctly detect a fetchUrl match/update, but `channels.bulk_onboard.apply` reparsed the original row without the matched `channelId`, so override/update rows could be created as duplicate channels. Fixed apply to hydrate actionable update rows with the planned `channelId` and to require `confirm=true` for actionable override-updates. Added TS regression coverage proving HNRSS fetchUrl matches update without inserting `source_channels`.
  - HNRSS MCP proof artifacts: first proof `/tmp/newsportal-mcp-hn-query-feeds-ce748ddc-d7cb-4192-a875-33eb1b8f9d52.json` proved the query-identity fix by planning 8 HNRSS query feeds with duplicate=0, but because the apply defect still existed in the then-running MCP image, it created duplicate retained channel rows for some existing exact fetch URLs. No cleanup/delete/archive was performed per product-test retention rule. After the apply fix and MCP rebuild, repeat bulk planning against retained duplicates correctly stopped as ambiguous rather than silently choosing one row.
  - HNRSS expansion/read-back artifact `/tmp/newsportal-mcp-hn-query-expand-6be5d78e-d53e-453f-b472-795ebe9707f0.json`: through MCP verified 8 retained HNRSS query channels, acquisitionSucceeded=8, two channels had useful article yield so far (`HN managed IT MSP` 98 articles and `HN CRM ERP consultant` 52 articles), and queued 8 `discovery.sources.expand_existing` runs for target `68316100-0947-4aa2-a8c4-017bc5a85dec` (`a8f850f2-0dac-4e6d-909f-6abd07437762`, `a3a9a2d0-bb41-461a-ac4e-727de03169af`, `3a21694f-1cba-488a-9162-d32eee47c7e7`, `0a650d0b-da58-49a0-b014-fb17e4a7ecda`, `662a10f2-3a91-4e5a-88ca-0ef7dd88ce98`, `2c22b3e3-8d8f-44ac-937d-895f3b6692ec`, `e59b3b48-f571-4289-af3a-215bf34b0a70`, `b6940bc6-0c9a-415a-8215-dabf14306cc8`). Selection report at this read-back showed selected=45, gray_zone=547, rejected=6883, stalePassThroughCount=0. This is progress toward source-pool breadth, not a completed 50-60 quality article proof.
  - Heartbeat read-back artifact `/tmp/newsportal-mcp-heartbeat-readback-12deb290-7c19-453d-a0ab-a6024d129008.json`: read-only MCP check after the HNRSS expansion queue showed all 8 retained HNRSS channels still found and acquisitionSucceeded=8; target/report read-back still shows the expansion/discovery queue not drained yet (25 queued, 0 running, 0 completed in the report sample), sourcePriorsListed=0 for the target, endpointCount=0 on the first page, selection counts selected=45, gray_zone=547, rejected=6884, stalePassThroughCount=0. No cleanup/delete/archive was performed; next heartbeat should let worker/discovery drain the queued runs before claiming yield.
  - Heartbeat read-back artifact `/tmp/newsportal-mcp-heartbeat-readback-35d54027-929d-4945-8e28-0d340381a928.json`: after another 45-minute window, the same HNRSS expansion/discovery report still showed queued=25, running=0, completed=0 for the target sample, so discovery expansion has not drained yet. HNRSS channel read-back remains healthy enough for observation (foundChannels=8, acquisitionSucceeded=8). Selection counts moved to selected=45, gray_zone=547, rejected=6910, stalePassThroughCount=0; all selected rows are still weak verification in operator health, so this is not the 50-60 high-quality proof.
  - Heartbeat source-prior artifact `/tmp/newsportal-mcp-heartbeat-priors-0e5cc7b0-6e33-4155-8504-850ddce51626.json`: evaluated 8 retained HNRSS query channels through MCP `discovery.source_priors.evaluate`; none were applied because all returned `priorState=no_prior`, mainly due to low `sourceRoleFit` (0.1732) and sub-threshold semantic fit despite good trust/fetch health. This correctly preserved the guardrail that priors do not force monitoring/probation when evidence is weak. Selection after the prior pass remained independent: selected=46, gray_zone=550, rejected=7031, stalePassThroughCount=0. No cleanup/delete/archive was performed.
  - Systemic history sweep 2026-05-09 found the real queued-run blocker: Discovery V3 API/MCP start-like operations created `discovery_runs` rows but did not dispatch the corresponding `discovery.v3.run` Sequence Runner job, so workers that consume `q.sequence` had nothing executable to drain. Retained queued-run proof artifacts: `/tmp/newsportal-mcp-drain-hnrss-expansions-8368abc2-809c-4366-9663-ccb3fb510d63.json` moved the first 8 retained HNRSS expansion runs to completed, and `/tmp/newsportal-mcp-drain-target-queued-runs-892ff848-5fec-4e30-a449-000162340032.json` moved one target batch to completed; target read-back artifact `/tmp/newsportal-mcp-heartbeat-readback-0551f5c4-23b0-49b7-886f-32886420a2ad.json` shows those tracked runs queued=0, running=0, completed=25, selection selected=46, gray_zone=550, rejected=7114, stalePassThroughCount=0.
  - Global MCP queue audit artifact `/tmp/newsportal-mcp-global-discovery-queue-audit-665fce35-14b9-40f0-af7f-b6ac861033f6.json` showed this was broader retained state, not just one target: 205 old queued discovery runs and 1 old running run existed without `summary_json.sequenceDispatch`. This is retained evidence from the pre-fix behavior, not a cleanup target; it must be continued through a bounded MCP repair path.
  - Systemic fix implemented: `services/api/app/discovery_v3_api.py` now ensures the default Discovery V3 Runner sequence and attaches a pending `sequence_runs` dispatch for `create_run`, `expand_source`, and `replace_source_candidates`; run responses and `summary_json.sequenceDispatch` expose dispatch success or failure. It also adds bounded `dispatch_queued_runs` for retained queued rows, exposed through API `/maintenance/discovery/runs/dispatch-queued` and MCP `discovery.runs.dispatch_queued`, so old queued rows can be sent to `q.sequence` without deletion/archive/cleanup. Contract updates in `.aidp/contracts/discovery-agent.md` and `.aidp/contracts/mcp-control-plane.md` now require start-like discovery tools to dispatch executable sequence work or report dispatch failure, not merely insert a run row.
  - Adjacent MCP contract defect found and fixed: `discovery.sources.expand_existing` and `discovery.sources.replace_candidates` previously accepted loose payloads that could reach the backend as 422 errors. `services/mcp/src/tools/discovery/v3-tools.ts` now requires a strict source action payload with `targetId` at the MCP boundary and rejects unsupported fields before backend fetch. Live proof artifact `/tmp/newsportal-mcp-discovery-run-dispatch-proof-17011b27-e050-4bb2-babc-a8a4087fbf67.json` created discovery run `d09284bc-6631-4d82-be2d-afff84468097`, dispatched sequence run `f1a6367f-3fe5-4338-a633-43c1ea1dbf2c`, both completed, and proved invalid source expansion is now rejected at MCP schema boundary.
  - Domain-neutrality sweep found and removed remaining runtime domain-specific logic from `services/mcp/src/tools/templates-tools.ts`, `services/workers/app/final_selection.py`, and `services/workers/app/selection_runtime.py`; outsourcing/HNRSS/SMB/domain words remain allowed in tests, retained artifacts, and AIDP product-test state only. Runtime code search over `services`, `packages`, `apps`, and `database` found no remaining domain strings after the patch. Source priors remain source-observation only and do not affect article selection/ranking/escalation/web visibility/counts.
  - Live MCP proof for the new retained-queue repair path: `/tmp/newsportal-mcp-dispatch-queued-proof-19f497ef-81e4-4024-b9b1-24f5e0551603.json` called `discovery.runs.dispatch_queued` with `limit=3` for target `68316100-0947-4aa2-a8c4-017bc5a85dec`, matched/dispatched 3 old queued runs, created sequence dispatch metadata for all 3, and moved them to running; follow-up read-back `/tmp/newsportal-mcp-dispatch-proof-poll-dc41b801-0fa1-4c1b-9dfc-50f5a6bc179e.json` verified all 3 sequence runs and discovery runs completed. Global audit `/tmp/newsportal-mcp-global-discovery-queue-audit-9712e568-258b-4123-bef3-bc7e951bea61.json` then showed queued=202, running=1, completed=86, failed=0, cancelled=0. Next heartbeat should continue bounded dispatch, not launch an unbounded 202-run burst.
  - Heartbeat 2026-05-09 14:16Z continuation: MCP global audit `/tmp/newsportal-mcp-global-discovery-queue-audit-98fed827-9399-41c9-9366-3591d136133c.json` confirmed retained queue state at queued=202, running=1, completed=86. Two bounded MCP `discovery.runs.dispatch_queued` batches then ran through artifacts `/tmp/newsportal-mcp-bounded-dispatch-readback-a0460c82-26ad-4f1a-8516-4775ff13691f.json` and `/tmp/newsportal-mcp-bounded-dispatch-readback-e53296aa-903a-4ccd-af3d-d3d323f1901a.json`; both batches matched/dispatched/completed 10/10 retained queued runs with failureCount=0. Final global audit `/tmp/newsportal-mcp-global-discovery-queue-audit-16f85af9-7b37-4cf8-9eee-73c2e7eae1d8.json` shows queued=182, running=1, completed=106, failed=0, cancelled=0. Target read-back `/tmp/newsportal-mcp-heartbeat-readback-65854e41-ae31-4b0e-aa6c-f12b8887389c.json` still shows tracked HNRSS expansion runs queued=0/running=0/completed=25, HNRSS found/acquisitionSucceeded=8/8, endpointCount=0, sourcePriorsListed=0, selection unchanged at selected=46, gray_zone=550, rejected=7127, stalePassThroughCount=0. This is queue-continuation progress, not final 50-60 quality article success.
  - Proof passed for the systemic sweep: `PYTHONPATH=. python -m unittest tests.unit.python.test_worker_hard_filters tests.unit.python.test_final_selection tests.unit.python.test_api_discovery_v3_cutover` passed 37/37; `pnpm unit_tests:ts -- mcp-control-plane` passed 356/356; `pnpm --filter @newsportal/mcp typecheck`, `pnpm --filter @newsportal/sdk typecheck`, `pnpm lint:ts`, `pnpm lint:py`, and `git diff --check` passed for touched paths. Runtime was rebuilt with API/MCP/worker; API `/health`, MCP `/health`, and compose health for API/MCP/worker/fetchers all passed. Cleanup status: no DB cleanup/delete/archive was performed; old queued/error/duplicate rows remain retained evidence.
  - Proof passed for the HNRSS/source-identity fixes: `pnpm unit_tests:ts` (354/354), `pnpm lint:ts`, `pnpm --filter @newsportal/control-plane typecheck`, `pnpm --filter @newsportal/mcp typecheck`, `git diff --check -- packages/control-plane/src/channel-bulk-onboarding.ts tests/unit/ts/admin-bulk-channel-import.test.ts`; MCP service rebuilt/restarted and is healthy, with worker/fetchers also healthy. Cleanup status: no DB cleanup/delete/archive was performed; retained duplicate HNRSS rows from the pre-fix apply remain as evidence/residual state for the product test.
  - H100 hypothesis mapping/discovery pass 2026-05-09: read the user-provided 100 outsourcing-demand hypotheses and implemented them as 12 compact MCP system-interest families instead of 100 separate active interests to avoid unnecessary selection/discovery fanout. MCP artifact `/tmp/newsportal-mcp-h100-interests-discovery-7bd53213-cff6-42cf-846e-5ddebacdc3e8.json` created/updated 12 interests, onboarded 12 public RSS/no-auth source channels, created 12 discovery targets, and started 12 discovery runs. Read-back artifact `/tmp/newsportal-mcp-h100-compile-readback-37da6fc1-d404-474a-a1c1-46c7903a1b38.json` verified 27/27 active interests compiled with blockerCount=0. First live follow-up exposed a generic MCP/API gap: per-run `providerExecutionEnabled` was supported by the worker but rejected by the API schema, so `services/api/app/discovery_v3_api.py` and `services/mcp/src/tools/discovery/v3-tools.ts` now expose bounded operator-approved provider execution on `discovery.runs.start`, `discovery.sources.expand_existing`, and `discovery.sources.replace_candidates`.
  - H100 live discovery continuation proof: artifact `/tmp/newsportal-mcp-h100-live-runs-23c4134e-06bd-47ca-b555-f598c13878c2.json` proved the new flag was persisted but exposed a second generic bug: existing dry-run queued hypotheses deduped later live runs into `no_hypotheses_executed`. Fixed `services/workers/app/discovery_v3_repository.py` so a later approved run can reuse existing queued/failed dedupe-matched hypotheses for execution without deleting or cloning retained rows. After worker rebuild, artifact `/tmp/newsportal-mcp-h100-live-runs-fff35a76-2140-4d56-a547-aa95983a561c.json` verified 12/12 H100 runs completed with `executionMode=bounded_live_provider_execution`, 96 executed hypotheses, 96 endpoints observed in run summaries, 64 sampled endpoint rows, 11 registered endpoints and 11 probation contracts in discovery-yield read-back. Selection remained strict and independent: selected=46, gray_zone=550, rejected=7180, stalePassThroughCount=0, web-visible contentItems total=24. Provider/source residual: web_search/rss/website remain degraded for some flows and searchResultCount was 0 in this bounded pass; most yield came from seed endpoint probes, so this is discovery/source-pool progress, not the 50-60 high-quality article success gate.
  - H100 proof passed: `PYTHONPATH=. python -m unittest tests.unit.python.test_api_discovery_v3_cutover`, `PYTHONPATH=. python -m unittest tests.unit.python.test_discovery_v3_stability.DiscoveryV3StabilityTests.test_insert_hypotheses_reuses_unexecuted_dedupe_match_for_later_run`, full `PYTHONPATH=. python -m unittest tests.unit.python.test_discovery_v3_stability`, and `pnpm unit_tests:ts -- mcp-control-plane` passed. Runtime API/MCP/worker/fetchers/relay stack was rebuilt/restarted as needed, with no DB reset, cleanup, delete, or archive.
  - Fresh hypothesis/channel diagnosis 2026-05-09: MCP read-back shows the new H100/SMB hypotheses are not blocked by interest compilation or LLM budget; `operator.system.health` reports 27/27 active interests compiled, LLM review enabled, 1105 LLM reviews in 24h, and stalePassThroughCount 0. The bottleneck is split across acquisition and strict independent selection. Active channels are 441 (312 rss, 129 website), but 162 need attention and the last 24h show roughly 23.7k fetch failures versus 432 new-content fetches. Channel funnel artifact `/tmp/newsportal-mcp-channel-funnel-probe-1e3aa4bd-d781-48b3-9af3-839d17a131fe.json` shows HNRSS query feeds are healthy enough to observe 208 stored items, Google News query feeds observe 234 items, Shopify/Odoo community websites observe hundreds of items, but Stack Overflow technology feeds are all hard-failing as RSS (12/12, 0 stored, mostly 404/403), Product Hunt product URLs are all hard-failing as RSS/XML (12/12, 0 stored), GitHub/StackExchange/ATS API sources are not present as producing source channels, and several website community surfaces have recurring upstream-precondition failures. Residual selection read-back shows the strict profile is working but low-yielding: total residuals 7837, selected 46, gray/hold 550, rejected 7241; query buckets include `small business` selected 14 / hold 415 / rejected 706, `Shopify` selected 0 / hold 11 / rejected 565, `contractor` selected 2 / hold 377 / rejected 382, and `developer` selected 6 / hold 21 / rejected 785. This means many hypothesis/channel items either never become content due to provider/adapter mismatch, or they become generic/noisy/problem-discussion items without enough buyer/project evidence to pass independent article selection. Additional finding: current `operator.report.verify` calls require explicit `entityIds: {}`; old report-only calls without entityIds now fail schema validation, so follow-up scripts must pass `entityIds` and MCP docs/tests should keep this contract explicit.
  - Channel validation/alternatives product-flow rerun 2026-05-09: runtime was rebuilt with the retained DB, no reset/cleanup/delete/archive. Health passed for API/MCP/web/nginx and the full product flow was driven through MCP after scoped token bootstrap. Artifact `/private/tmp/newsportal-product-flow-39fae247-fdb.json` proved `tools/list` saw 129 tools, read server/discovery/channel guides, read admin/system/selection/discovery/channel state, planned alternatives for failure-kind channels, found 50 alternative inputs and 22 RSS/feed candidates, applied only 2 validated feed-probe candidates through `channels.bulk_onboard.plan -> apply -> verify` (1 created channel `0454bf33-44b4-4f1a-845b-128b5225bd01`, 1 updated channel `f6022dde-9fcb-4b3a-851b-69c0ffcb1b35`, acquisitionSucceeded=1 at immediate verify), then dispatched 12 retained queued discovery runs through MCP `discovery.runs.dispatch_queued` with failureCount=0. Read-back artifact `/private/tmp/newsportal-product-flow-readback-4a2ff063-3d7.json` after worker activity showed web/public `content_items.list` remains 24, retained queued discovery runs fell from 112 to 100 with 1 running and 0 failed in the sampled read-back, fetch_runs total 32887, 144 endpoints still require evidence review, and 23 promoted sources remain in contract probation. Residual selection summary shows 46 selected final-selection rows but only 24 public content items, so the earlier over-display bug is not reproducing; the current gap remains source acquisition/quality plus strict independent selection, not web gating.
  - Immediate bounded continuation after operator approval: updated heartbeat automation `continue-tuned-outsourcing-discovery` to run every 45 minutes with the current MCP-only, no-cleanup, strict-selection/channel-alternatives/queued-dispatch instructions. Artifact `/private/tmp/newsportal-bounded-product-flow-b4b0763b-6fc.json` ran another MCP pass: public content stayed 24, retained queued discovery runs fell from 100 to 88, 12 queued runs dispatched with failureCount=0, alternatives planned 50 inputs/34 candidates/3 feed-probe candidates. The pass exposed an operator-runner argument error (`channels.alternatives.start` rejected `maxSocialItems=0` because the MCP schema requires positive integers); corrected follow-up artifact `/private/tmp/newsportal-alt-correct-05e9f7f2-186.json` omitted the zero field, started 5 bounded replacement runs, dispatched 5 more queued runs with failureCount=0, and left public content at 24 with queue at 83/running 1. Because one batch candidate was blocked by retained duplicate RSS URLs, final per-source artifact `/private/tmp/newsportal-alt-single-apply-a8c4d5e5-864.json` applied only non-ambiguous ready sources: 1 new feed channel created, `https://www.cio.com/feed/` and `https://itbrief.co.nz/feed` skipped as ambiguous retained duplicates. No DB cleanup/delete/archive was performed.
  - Heartbeat continuation 2026-05-09T20:31Z: MCP-only artifact `/private/tmp/newsportal-heartbeat-flow-7a82735e-82a.json` read retained state and ran another bounded pass. Runtime health remained healthy; selected/public content was 25 before and after the pass, so web still shows only independently selected content and no selection loosening occurred. `channels.alternatives.plan` inspected 50 inputs and 8 candidates, with 3 feed-probe candidates; one non-ambiguous ready feed was applied as an update, while `https://www.cio.com/feed/` and `https://itbrief.co.nz/feed` were again skipped because retained duplicates make them ambiguous. `channels.alternatives.start` created 5 bounded replacement runs, and `discovery.runs.dispatch_queued` dispatched 12 retained queued runs with failureCount=0, reducing retained queued runs from 83 to 71 with 1 running and 0 failed in the sample. Discovery read-back still reports 4 endpoints requiring evidence review and 1 probation source for the target; source priors remain absent and separate from selection. No DB cleanup/delete/archive was performed.
  - Heartbeat continuation 2026-05-09T21:16Z: MCP-only artifact `/private/tmp/newsportal-heartbeat-flow-7c39e92c-d7a.json` ran another bounded pass on the retained DB. Runtime remained healthy. Public/web selected content stayed 25 before and after, so no noisy content was promoted to web. `channels.alternatives.plan` inspected 50 inputs and 20 candidates, found 2 feed-probe candidates, and applied one non-ambiguous ready feed update; `https://itbrief.co.nz/feed` was skipped because retained duplicate RSS rows make the update ambiguous. `channels.alternatives.start` created 5 bounded replacement runs, and `discovery.runs.dispatch_queued` dispatched 12 retained queued runs with failureCount=0, reducing retained queued runs from 71 to 59 with 1 running and 0 failed in the sample. Discovery report still warns that 4 endpoints need evidence review and 1 promoted source is still in probation; source priors remain absent and independent from selection. No DB cleanup/delete/archive was performed.
  - Heartbeat continuation 2026-05-09T22:01Z / local 2026-05-10: MCP-only artifact `/private/tmp/newsportal-heartbeat-flow-0fc6db30-ade.json` ran another bounded retained-DB pass. Runtime health remained healthy. Public/web selected content stayed 25 before and after, preserving strict independent selection. `channels.alternatives.plan` inspected 50 inputs and 3 feed-probe candidates; all 3 non-ambiguous candidates were applied as channel updates through bulk onboarding, with no skipped candidates. `channels.alternatives.start` created 5 bounded replacement runs, and `discovery.runs.dispatch_queued` dispatched 12 retained queued runs with failureCount=0, reducing retained queued runs from 59 to 47 with 1 running and 0 failed in the sample. Discovery read-back still warns that 4 endpoints require evidence review and 1 promoted source remains in probation; source priors remain absent and separate from selection. No DB cleanup/delete/archive was performed.
  - Heartbeat continuation 2026-05-09T22:46Z / local 2026-05-10: MCP-only artifact `/private/tmp/newsportal-heartbeat-flow-be8d78bf-0bd.json` ran another bounded retained-DB pass. Runtime health remained healthy. Public/web selected content stayed 25 before and after, so strict independent selection was preserved. `channels.alternatives.plan` inspected 50 inputs and 34 candidates, with 4 feed-probe candidates; 2 non-ambiguous ready candidates were applied through bulk onboarding (1 created channel, 1 updated channel), while `https://www.cio.com/feed/` and `https://itbrief.co.nz/feed` were skipped again because retained duplicate RSS rows make them ambiguous. `channels.alternatives.start` created 5 bounded replacement runs, and `discovery.runs.dispatch_queued` dispatched 12 retained queued runs with failureCount=0, reducing retained queued runs from 47 to 35 with 1 running and 0 failed in the sample. Discovery report only warns that some runs are still queued/running; source priors remain absent and separate from selection. No DB cleanup/delete/archive was performed.
  - Heartbeat continuation 2026-05-09T23:31Z / local 2026-05-10: MCP-only artifact `/private/tmp/newsportal-heartbeat-flow-fce28740-5d2.json` ran another bounded retained-DB pass. Runtime health remained healthy. Public/web selected content stayed 25 before and after, preserving strict independent selection and confirming this is still a source/yield problem rather than web over-display. `channels.alternatives.plan` inspected 50 inputs and 24 candidates, with 4 feed-probe candidates; 2 non-ambiguous ready candidates were applied as channel updates through bulk onboarding, while `https://www.cio.com/feed/` and `https://itbrief.co.nz/feed` were skipped because retained duplicate RSS rows still make them ambiguous. `channels.alternatives.start` created 5 bounded replacement runs, and `discovery.runs.dispatch_queued` dispatched 12 retained queued runs with failureCount=0, reducing retained queued runs from 35 to 23 with 1 running and 0 failed in the sample. Discovery and selection verification returned no warnings; source priors remained absent and separate from selection. No DB cleanup/delete/archive was performed.
  - Heartbeat continuation 2026-05-10T00:16Z / local 2026-05-10: the first local runner attempt only read MCP state and stopped before writes because discovery target rows are snake_case (`target_id`), then the corrected MCP pass normalized target IDs and ran the bounded flow. The corrected runner applied MCP feed alternatives, started bounded replacement runs, and dispatched a retained queued batch before hitting a local script-only wait-helper misuse (`waitFor` signature), so a separate MCP read-back artifact `/private/tmp/newsportal-heartbeat-readback-9b8ed0c7-4bbf.json` verified the DB-backed outcome. Public/web selected content increased from the previous 25 to 30; final selection read-back shows selected=52, gray_zone=574, rejected=7566, stalePassThroughCount=0, but only 30 content items are web-visible, so the 50-60 web-visible quality target is not reached. Retained queued runs dropped from 23 to 11, with 1 running and 0 failed; completed runs are 483. `channels.alternatives.plan` still finds 50 problematic inputs and 13 feed-probe candidates; endpoints=10162, hidden claims=19, source priors=0, provider health rows=10. Discovery and selection verification returned no warnings. No DB cleanup/delete/archive/reset was performed; the local runner helper issue is not a product-state mutation and should be fixed in the next ad hoc runner by using a plain timeout or the correct wait helper signature.
  - Heartbeat continuation 2026-05-10T01:01Z / local 2026-05-10: MCP-only artifact `/private/tmp/newsportal-heartbeat-flow-b8182c34-25df.json` read AIDP/runtime state, admin/discovery/system health, selected content, residuals, provider health, claims and source priors, then ran another retained-DB pass without cleanup/delete/archive/reset. Runtime health stayed green. Public/web selected content stayed 30 before and after; final selection remained independent with selected=52, gray_zone=574, rejected=7595, stalePassThroughCount=0, so the 50-60 web-visible target is still not reached. The retained queued run backlog drained from 11 to 0 with 1 running and 0 failed; completed discovery runs reached 544. `channels.alternatives.plan` inspected 50 problematic inputs but found only 5 candidates and 0 validated feed-probe candidates to apply, so no channel rows were created/updated this pass. Important product/operator observation: calling `channels.alternatives.start` with `maxCandidates=5` still created 50 replacement runs because the tool starts runs for all planned input channels rather than the capped candidate set. This is not a data-cleanup issue, but it is a boundedness/control-plane gap; next heartbeat should avoid `channels.alternatives.start` and use only `channels.alternatives.plan` plus strict feed-probe `bulk_onboard.plan/apply/verify` until the tool is fixed. Channel verification warned that 2 channels still have provider-shape blockers. Hidden claims remain 19 and source priors remain 0.
  - Heartbeat continuation 2026-05-10T01:46Z / local 2026-05-10: MCP-only artifact `/private/tmp/newsportal-heartbeat-flow-7aaeee2f-c749.json` followed the updated automation guardrail and did not call `channels.alternatives.start`. Runtime/API/MCP health stayed green. Product ops read admin/discovery/system health, selected content, residuals, fetch/channel state, endpoints, claims, provider health and source priors through MCP; `channels.alternatives.plan` inspected 50 problematic inputs and returned 50 candidates with 1 validated feed-probe candidate, which was applied through strict `channels.bulk_onboard.plan -> apply -> verify` as 1 channel update. No retained queued discovery rows were left to dispatch (queued 0 before/after, dispatch matched/dispatched 0), with 1 running row and 0 failed rows still visible. Public/web selected content stayed 30 before and after; final selection stayed independent with selected=52, gray_zone=574, rejected=7602, stalePassThroughCount=0, so the 50-60 recent web-visible quality target remains unmet. Hidden claims remain 19, source priors remain 0, provider health rows remain 10, and endpoints remain 10162. Discovery, selection, and channel verification returned no warnings. No DB cleanup/delete/archive/reset was performed.
  - Heartbeat continuation 2026-05-10T02:31Z / local 2026-05-10: MCP-only artifact `/private/tmp/newsportal-heartbeat-flow-2ac94a88-4c57.json` again followed the no-`channels.alternatives.start` guardrail. Runtime/API/MCP health stayed green. Product ops read admin/discovery/system health, selected content, residuals, fetch/channel state, endpoints, claims, provider health and source priors through MCP; `channels.alternatives.plan` inspected 50 problematic inputs and returned 35 candidates but 0 validated feed-probe candidates, so no channel rows were created or updated. No retained queued discovery rows were left to dispatch (queued 0 before/after, dispatch matched/dispatched 0), with 1 running row and 0 failed rows still visible. Public/web selected content stayed 30 before and after; final selection stayed independent with selected=52, gray_zone=575, rejected=7615, stalePassThroughCount=0, so the 50-60 recent web-visible quality target remains unmet. Hidden claims remain 19, source priors remain 0, provider health rows remain 10, and endpoints remain 10162. Discovery, selection, and channel verification returned no warnings. No DB cleanup/delete/archive/reset was performed.
  - Heartbeat continuation 2026-05-10T03:16Z / local 2026-05-10: MCP-only artifact `/private/tmp/newsportal-heartbeat-flow-ffc35415-6eed.json` again followed the no-`channels.alternatives.start` guardrail. Runtime/API/MCP health stayed green. Product ops read admin/discovery/system health, selected content, residuals, fetch state, endpoints, claims, provider health and source priors through MCP; an optional runner attempt to list channels via `channels.read` failed schema validation because that tool is detail-only and requires `channelId`, so future scripts should use the proper channel-health/list surface instead. `channels.alternatives.plan` inspected 50 problematic inputs and returned 12 candidates with 2 validated feed-probe candidates; one ITnews feed update was applied through strict `channels.bulk_onboard.plan -> apply -> verify`, while `https://www.cio.com/feed/` was skipped because retained duplicate RSS rows make bulk onboarding ambiguous. No retained queued discovery rows were left to dispatch (queued 0 before/after, dispatch matched/dispatched 0), with 1 running row and 0 failed rows still visible. Public/web selected content stayed 30 before and after; final selection stayed independent with selected=52, gray_zone=575, rejected=7625, stalePassThroughCount=0, so the 50-60 recent web-visible quality target remains unmet. Hidden claims remain 19, source priors remain 0, provider health rows remain 10, and endpoints remain 10162. Discovery, selection, and channel verification returned no warnings. No DB cleanup/delete/archive/reset was performed.
  - Heartbeat continuation 2026-05-10T04:01Z / local 2026-05-10: MCP-only artifact `/private/tmp/newsportal-heartbeat-flow-2861238f-5df0.json` again followed the no-`channels.alternatives.start` guardrail. Runtime/API/MCP health stayed green. Product ops read admin/discovery/system health, selected content, residuals, fetch state, endpoints, claims, provider health and source priors through MCP; `channels.alternatives.plan` inspected 50 problematic inputs and returned 10 candidates but 0 validated feed-probe candidates, so no channel rows were created or updated. No retained queued discovery rows were left to dispatch (queued 0 before/after, dispatch matched/dispatched 0), with 1 running row and 0 failed rows still visible. Public/web selected content stayed 30 before and after; final selection stayed independent with selected=52, gray_zone=575, rejected=7634, stalePassThroughCount=0, so the 50-60 recent web-visible quality target remains unmet. Hidden claims remain 19, source priors remain 0, provider health rows remain 10, and endpoints remain 10162. Discovery, selection, and channel verification returned no warnings. No DB cleanup/delete/archive/reset was performed.
  - Heartbeat continuation 2026-05-10T04:46Z / local 2026-05-10: MCP-only artifact `/private/tmp/newsportal-heartbeat-flow-d096dc7d-7c1d.json` again followed the no-`channels.alternatives.start` guardrail. Runtime/API/MCP health stayed green. Product ops read admin/discovery/system health, selected content, residuals, fetch state, endpoints, claims, provider health and source priors through MCP; `channels.alternatives.plan` inspected 50 problematic inputs and returned 50 candidates but 0 validated feed-probe candidates, so no channel rows were created or updated. No retained queued discovery rows were left to dispatch (queued 0 before/after, dispatch matched/dispatched 0), with 1 running row and 0 failed rows still visible. Public/web selected content stayed 30 before and after; final selection stayed independent with selected=52, gray_zone=575, rejected=7638, stalePassThroughCount=0, so the 50-60 recent web-visible quality target remains unmet. Hidden claims remain 19, source priors remain 0, provider health rows remain 10, and endpoints remain 10162. Discovery, selection, and channel verification returned no warnings. No DB cleanup/delete/archive/reset was performed.
  - Heartbeat continuation 2026-05-10T05:31Z / local 2026-05-10: MCP-only artifact `/private/tmp/newsportal-heartbeat-flow-4f032a81-96f7.json` again followed the no-`channels.alternatives.start` guardrail. Runtime/API/MCP health stayed green. Product ops read admin/discovery/system health, selected content, residuals, fetch state, endpoints, claims, provider health and source priors through MCP; `channels.alternatives.plan` inspected 50 problematic inputs and returned 46 candidates with 1 validated feed-probe candidate, which was applied through strict `channels.bulk_onboard.plan -> apply -> verify` as 1 channel update. No retained queued discovery rows were left to dispatch (queued 0 before/after, dispatch matched/dispatched 0), with 1 running row and 0 failed rows still visible. Public/web selected content stayed 30 before and after; final selection stayed independent with selected=52, gray_zone=575, rejected=7644, stalePassThroughCount=0, so the 50-60 recent web-visible quality target remains unmet. Hidden claims remain 19, source priors remain 0, provider health rows remain 10, and endpoints remain 10162. Discovery, selection, and channel verification returned no warnings. No DB cleanup/delete/archive/reset was performed.
  - Heartbeat continuation 2026-05-10T06:16Z / local 2026-05-10: MCP-only artifact `/private/tmp/newsportal-heartbeat-flow-5f5281e6-b996.json` again followed the no-`channels.alternatives.start` guardrail. Runtime/API/MCP health stayed green. Product ops read admin/discovery/system health, selected content, residuals, fetch state, endpoints, claims, provider health and source priors through MCP; `channels.alternatives.plan` inspected 50 problematic inputs and returned 32 candidates with 2 validated feed-probe candidates. One feed update was applied through strict `channels.bulk_onboard.plan -> apply -> verify`; `https://www.cio.com/feed/` was skipped because retained duplicate RSS rows make bulk onboarding ambiguous. No retained queued discovery rows were left to dispatch (queued 0 before/after, dispatch matched/dispatched 0), with 1 running row and 0 failed rows still visible. Public/web selected content stayed 30 before and after; final selection stayed independent with selected=52, gray_zone=575, rejected=7673, stalePassThroughCount=0, so the 50-60 recent web-visible quality target remains unmet. Hidden claims remain 19, source priors remain 0, provider health rows remain 10, and endpoints remain 10162. Discovery, selection, and channel verification returned no warnings. No DB cleanup/delete/archive/reset was performed.
  - Source bottleneck/deep-alternatives runtime proof 2026-05-10 local: after implementing the source-bottleneck read model, deep feed alternatives, bounded `channels.alternatives.start`, and polite polling changes, changed services were rebuilt with `docker compose --env-file .env.dev -f infra/docker/compose.yml -f infra/docker/compose.dev.yml up --build -d fetchers mcp admin nginx`; no DB cleanup/delete/archive/reset was performed. MCP health returned `{"service":"mcp","status":"ok"}` and compose showed admin/api/fetchers/mcp/nginx/postgres/redis/relay/web/worker healthy. Scoped MCP read-back confirmed the new tools are published: `channels.bottlenecks.summary/list/explain`, `channels.alternatives.plan`, `channels.bulk_onboard.plan/apply/verify`, and `operator.report.verify`. DB-backed bottleneck summary: 444 total/active channels, 245 technical bottlenecks, 172 working_noisy, 17 working_low_yield; repair lanes were 189 monitor_quality, 169 access_or_auth_required, 70 deep_alternatives, 10 none, 6 polite_retry. A bounded repair pass selected 5 deep-alternative channels, found 20 candidates/17 feed candidates with 3 validated feed-probe source rows, then applied only actionable bulk-onboard rows through MCP: 1 RSS channel update, 2 duplicate candidates skipped, verify found 1/1 requested channel and acquisitionSucceeded=1. Selection read-back remained strict and independent: `content_items.list` total 30 while residuals show selected final-selection rows=52, hold=575, rejected=7730; the 52-vs-30 difference is canonical/public-content projection/dedupe/gating, not web over-display. No domain hardcode was added in this proof.
  - Example C/reference-bundle calibration follow-up 2026-05-10 local: inspected `docs/product/operator/examples/EXAMPLES.md` Example C, `docs/product/operator/examples/DISCOVERY_MODE_TESTING.md` proof-backed Example C profile, `docs/product/data-scripts/outsource*.json`, `docs/product/data-scripts/outsource_balanced_templates.md`, and `infra/scripts/lib/outsource-example-c.bundle.mjs`. Key finding: the historical working setup was a calibrated funnel, not broad discovery alone: source roles/provider shapes, 5 signal-family interests, 3 LLM scopes, profile policy, positive cues, near-miss negative cues, content-kind allowances, and adapter-required source classes were aligned. Added generic MCP/operator guidance without domain hardcode: `operator.funnel.calibrate` prompt, `newsportal://guide/scenarios/funnel-calibration` resource, server instruction notes for reference-bundle calibration, discovery/system-interest scenario guidance, MCP README notes, and resilient-discovery reference-bundle section. Proof passed with `pnpm --filter @newsportal/mcp typecheck`, `pnpm unit_tests:ts -- mcp-control-plane` (367/367), `pnpm lint:ts`, and `git diff --check -- services/mcp/src/context.ts services/mcp/src/prompts.ts services/mcp/src/resources.ts docs/product/operator/mcp/README.md docs/product/operator/discovery/resilient-discovery.md`. Live MCP read-back also passed through a scoped read token without product DB cleanup/delete/archive/reset: `prompts/list` published `operator.funnel.calibrate`, `resources/list` published `newsportal://guide/scenarios/funnel-calibration`, `prompts/get` returned reference-bundle calibration guidance, and `resources/read` returned the funnel-calibration playbook.
  - Active calibration-run follow-up 2026-05-10 local: lifecycle mode `normal`, work route `capability`, item status `active`, route phase `MCP product calibration/proof`, parent capability `Resilient Discovery V3 capability completion and outsourcing-demand product test`. Planning source/status: AIDP-native accepted-for-this-item from the user's request to run the Example C/reference-bundle calibration and do the needed MCP product operations. Scope: read Example C/manual JSON assets, read current MCP interests/templates/channels/bottlenecks/residuals/content/discovery state, compare funnel gaps, apply only safe configuration/discovery/channel changes through MCP/admin config, then verify counts and report claims through MCP. Out of scope: raw SQL product mutations, DB cleanup/delete/archive/reset, domain-specific runtime hardcode, production/shared DB changes, paid/social/API adapter implementation. Risk: medium because this touches retained local product-test configuration and live discovery queues; approval required: no for bounded local MCP product ops already requested by the user, yes for destructive cleanup, production/shared changes, paid providers, or new adapters. Cleanup status: DB evidence must be retained.
  - Clarification/pivot 2026-05-10 local: user clarified that Example C/manual JSON assets are intended to finish the system and extract missing generic funnel-design rules, not to obtain immediate outsourcing test results. Stopped the ad hoc calibration runner; before it was stopped it had already applied only bounded MCP configuration writes for 5 ODPT calibrated system interests and 3 ODPT LLM templates, with no discovery/backfill/source-onboarding continuation and no cleanup/delete/archive/reset. Pivoted implementation to generic, domain-independent MCP/operator guidance: reference bundles are calibration evidence, first produce a portable funnel spec, separate acquisition/source-health/candidate-recovery/final-selection layers, and mutate domain config only when the operator explicitly asks for a product test or calibration run. Retained DB evidence remains intact.
  - Proof after clarification/pivot 2026-05-10 local: `pnpm --filter @newsportal/mcp typecheck`, `pnpm unit_tests:ts -- mcp-control-plane` (367/367), `pnpm lint:ts`, and `git diff --check -- services/mcp/src/context.ts services/mcp/src/prompts.ts services/mcp/src/resources.ts docs/product/operator/mcp/README.md docs/product/operator/discovery/resilient-discovery.md .aidp/work.md` passed. Rebuilt local MCP/nginx with `docker compose --env-file .env.dev -f infra/docker/compose.yml -f infra/docker/compose.dev.yml up --build -d mcp nginx`; scoped MCP read-back through a temporary read token confirmed `operator.funnel.calibrate` is in `prompts/list`, `newsportal://guide/scenarios/funnel-calibration` is in `resources/list`, `prompts/get` contains the portable funnel spec/no-domain-mutation guidance, and `resources/read` contains `portableFunnelSpec` plus `whenToMutate`. Temporary read token was revoked after proof; product-test DB evidence was not cleaned/deleted/archived/reset.
  - Ad hoc MCP-guided calibration runner 2026-05-10 local: the runner was corrected to use current MCP guidance/read-back only, not Example C as a benchmark. It reads `operator.funnel.calibrate`, `newsportal://guide/scenarios/funnel-calibration`, current interests/templates/residuals/content/source-bottleneck/discovery state, and applies only bounded MCP product operations. A first runner pass updated 3 calibrated system interests to allow `document`/`entity`, applied 5 validated channel repairs, but mistakenly queued `maintenance.reindex.request` with `systemFeedOnly=true`; this replayed only 55 articles and temporarily exposed a false low-yield state of 3 web-visible / 4 selected rows. The runner was patched in `/private/tmp/newsportal-mcp-guided-calibration-runner.mjs` to require `NEWSPORTAL_MCP_TOKEN` and to omit `systemFeedOnly` for full selection replay. No DB cleanup/delete/archive/reset was performed.
  - Reindex timeout system fix 2026-05-10 local: corrective full replay without `systemFeedOnly` initially failed at 5,250/8,641 articles because the migration-owned `Default Reindex` sequence still had `maintenance.reindex timeoutMs=1800000` (30 minutes). This was a systemic retained-DB/full-calibration bug, not an outsourcing-domain tuning issue. Fixed the live sequence through MCP `sequences.update` to `timeout_ms=7200000` and added/applied `database/migrations/0054_reindex_backfill_timeout_tuning.sql` for future environments. Proof: `pnpm db:migrate` applied 0054; `pnpm test:migrations:smoke` passed with 55 migrations and active article/resource sequence graphs; `git diff --check -- database/migrations/0054_reindex_backfill_timeout_tuning.sql` passed. No cleanup/delete/archive/reset was performed.
  - Full MCP replay and calibration proof 2026-05-10 local: after the timeout fix, `maintenance.reindex.request jobKind=backfill` completed 8,647/8,647 articles with 233,469 criteria matches and 54 criterion LLM reviews. MCP read-back after completion showed strict selection, not quantity inflation: 7 selected rows / 3 web-visible unique content items immediately after full replay, then 9 selected rows / 4 web-visible unique content items after validated channel repairs fetched new content. Residuals after the final read-back: selected=9, hold=21, rejected=8674, technicalFiltered=6245, semanticNoMatch=2288, missingInterestFilterResults=0, stalePassThroughCount=0. This did not reach the 50-60 quality target; it proves the current funnel is precision-heavy and still source-yield constrained.
  - Acquisition repair pass 2026-05-10 local: `channels.bottlenecks.summary` after full replay showed 496 active channels, 245 technical bottlenecks, 231 working_noisy, 7 working_low_yield, with repair lanes 238 monitor_quality, 159 access_or_auth_required, 80 deep_alternatives, 13 none, 6 polite_retry. A bounded `channels.alternatives.plan` on 20 deep-alternative channels found 60 candidates and 7 valid feed-probe RSS candidates; strict `channels.bulk_onboard.plan/apply/verify` applied 6 non-ambiguous repairs (4 CIO updates plus 2 ARN Australia new RSS channels) and skipped ambiguous `https://www.cio.com/feed/`. Channel verification found 6/6 requested, acquisitionSucceeded=4. Bounded `channels.alternatives.start` then created 1 replacement discovery run for a CIO source and skipped 2 inputs with no bounded candidate; the run completed. Later verification showed 499 active channels, 245 technical bottlenecks, 232 working_noisy, 9 working_low_yield, 4 web-visible selected items, and no cleanup/delete/archive/reset.
  - MCP alternatives zero-social contract fix 2026-05-10 local: while continuing the retained product test, `channels.alternatives.start` correctly created a backend replacement run with `max_social_items=0` when the field was omitted, but MCP schema validation rejected explicit `maxSocialItems=0` as not positive. Fixed the MCP boundary to keep `maxCandidates/maxDepth/maxHypotheses/maxSearchResults/maxDomains/maxEndpoints` positive-only while accepting `maxSocialItems` as a non-negative disable value. Proof: `pnpm unit_tests:ts -- mcp-control-plane` passed 367/367 with regression coverage, `pnpm --filter @newsportal/mcp typecheck` passed, `pnpm lint:ts` passed, `git diff --check -- services/mcp/src/tools/channels-tools.ts tests/unit/ts/mcp-control-plane.test.ts` passed, local MCP was rebuilt with `docker compose --env-file .env.dev -f infra/docker/compose.yml -f infra/docker/compose.dev.yml up --build -d mcp`, `/health` returned ok, and a live MCP call with explicit `maxSocialItems=0` was accepted. No product-test DB cleanup/delete/archive/reset was performed.
  - MCP channel expansion pass 2026-05-10 local: at user request, ran 7 MCP `channels.bulk_onboard.plan/apply/verify` source-expansion sessions to add a larger crumb-source pool without changing selection filters or code. Inputs were 300 feed-shaped RSS sources across Google News regional searches, HNRSS community searches, and StackOverflow tag feeds for outsourcing-demand hypotheses; all writes went through MCP strict bulk onboarding. Result: active/total channels increased from 500 to 792, with 292 new RSS channels created and 8 existing channels updated. Per-batch verify found all requested rows in each batch; newly created rows use polite/adaptive polling (`pollIntervalSeconds=21600`, `maxPollIntervalSeconds=86400`, `maxItemsPerPoll=20`). `channels.bottlenecks.summary` after onboarding reported 792 active channels, 663 RSS / 129 website, healthy bucket 305, technical bottlenecks still 245, deep_alternatives 72, and monitor_quality 242. No source priors or source health were used for article selection/ranking/web visibility, and no product-test DB cleanup/delete/archive/reset was performed.
  - MCP SMB channel expansion pass 2026-05-10 local: at user request, ran 10 additional MCP `channels.bulk_onboard.plan/apply/verify` sessions focused on small and mid-market business demand signals. Inputs were generated as operator/product-test configuration, not runtime code: Google News regional RSS searches across US/Canada/Australia/New Zealand/Europe/GCC/other allowed markets, plus HNRSS and StackOverflow feeds for SMB technology pain, grants/vouchers, ERP/CRM/ecommerce/cloud/cyber/compliance, local-business verticals, agency overflow, no-code limits, and implementation/migration rescue patterns. Strict MCP onboarding created 500 new RSS channels and updated 0 existing rows. `admin.summary.get` and `channels.list` read-back show active/total channels increased from 792 to 1292. Newly created rows use polite/adaptive polling (`pollIntervalSeconds=21600`, `maxPollIntervalSeconds=86400`, `maxItemsPerPoll=20`). `operator.report.verify reportKind=channel_onboarding` over the first 250 created IDs found rows and provider counts consistent with 1163 RSS plus 129 website channels. Observation: `channels.bottlenecks.summary` read-back appears capped at 1000 rows in this high-channel-count state even though `channels.list`/admin totals show 1292; this should be treated as a source-bottleneck read-model scaling gap to fix later, not as a channel creation failure. No selection/ranking/web visibility filters were changed, and no product-test DB cleanup/delete/archive/reset was performed.
  - Source-bottleneck read-model scaling bugfix 2026-05-10 local: fixed the post-expansion bug where `channels.bottlenecks.summary/list` capped the shared read model at 1000 rows while `admin.summary.get` and `channels.list` showed 1292 channels. Removed the hardcoded `limit 1000` from `packages/control-plane/src/channel-bottlenecks.ts` and added MCP regression coverage that `operator.report.verify reportKind=source_bottleneck` does not use the capped SQL. Proof: `pnpm unit_tests:ts -- mcp-control-plane` passed 367/367, `pnpm --filter @newsportal/control-plane typecheck` passed, `pnpm --filter @newsportal/mcp typecheck` passed, `pnpm lint:ts` passed, `git diff --check -- packages/control-plane/src/channel-bottlenecks.ts tests/unit/ts/mcp-control-plane.test.ts` passed. Rebuilt local MCP with `docker compose --env-file .env.dev -f infra/docker/compose.yml -f infra/docker/compose.dev.yml up --build -d mcp`; `/health` returned ok; live MCP read-back now aligns: adminActive=1292, channelsTotal=1292, bottleneckTotal=1292, bottleneckActive=1292, bottleneckListTotal=1292, byProvider RSS=1163 and website=129. No product-test DB cleanup/delete/archive/reset was performed.
  - Expanded-pool fetch/ingestion soak 2026-05-10 local: after expanding to 1292 active channels, let the running fetcher/article pipeline process the new source pool without changing selection/ranking/web filters. Baseline MCP read-back at 13:51Z: active_channels=1292, overdue_channels=576, processed_total=9188, fetch_new_content_24h=377, contentTotal=4, residual selected=9, hold=21, rejected=9158, technicalFiltered=6590, semanticNoMatch=2427. After a 3-minute natural scheduler soak, MCP read-back at 13:54Z: overdue_channels=520, processed_total=9295, fetch_new_content_24h=404, contentTotal=4, residual selected=9, hold=21, rejected=9265, technicalFiltered=6680, semanticNoMatch=2444, missingInterestFilterResults=3. Interpretation: the expanded pool is ingesting (+107 processed articles during the short soak) but strict independent selection has not yet produced additional web-visible content; remaining bottleneck is quality/yield, not source count or web over-display. Updated heartbeat automation `continue-tuned-outsourcing-discovery` to focus the next 45-minute wakeup on fetch/ingestion yield read-back for the 1292-channel pool before adding more sources. No product-test DB cleanup/delete/archive/reset was performed.
  - Heartbeat continuation 2026-05-10T10:00Z: MCP-only artifact `/private/tmp/newsportal-heartbeat-flow-e77ab11b.json` read retained state through MCP, including selected/public content, residuals, source bottlenecks, discovery queues, targets, endpoints, claims, provider health, source priors, and operator report verification. Public/web-visible content remained 30 and selected final-selection rows remained 52 before/after, so the 50-60 web-visible quality target is still unmet and selection was not loosened. Source bottleneck read-back reported 492 active channels, 246 technical bottlenecks, 217 working_noisy, 17 working_low_yield; repair lanes were 234 monitor_quality, 160 access_or_auth_required, 79 deep_alternatives, 12 none, 7 polite_retry. `channels.alternatives.plan` ran against 5 deep-alternative channels and found 5 validated feed-probe candidates; bulk onboarding applied 4 non-ambiguous RSS channel updates through MCP and verified channel onboarding. No bounded `channels.alternatives.start` replacement run was needed because enough feed candidates were available. Queue dispatch ran bounded with limit=8; no DB cleanup/delete/archive/reset was performed.
  - MCP SMB/serial-entrepreneur expansion pass 2026-05-10 local: user asked to add another 1000 channels for small-business and serial-entrepreneur/search-fund signals through discovery/MCP. Product operations used strict `channels.bulk_onboard.plan/apply/verify`; no SQL, no selection/ranking/web-visible changes, and no cleanup/delete/archive/reset. The initial current-state read-back before this pass showed `channels.list` total 2326. Candidate generation focused on feed-shaped public sources: Google News regional RSS searches across allowed countries, HNRSS founder/community searches, and StackOverflow tag feeds for SMB software pain, digital adoption, grants/vouchers, ERP/CRM/ecommerce/cloud/cyber/compliance, agency overflow, implementation/migration rescue, search funds, ETA, roll-ups, post-exit founders, venture studios, and family-business succession. MCP contract guards blocked two local client mistakes before successful writes: `channels.list` requires `page/pageSize`, and `channels.bulk_onboard.apply` requires `planFingerprint`; the runner was corrected to honor those contracts. The final successful runner was stopped after discovering its local response parser was not reading the apply-created counters correctly; DB-backed MCP read-back showed the pass had already over-added channels, increasing total/active channels from 2326 to 8460 (+6134), all retained per product-test rules. `channels.bottlenecks.summary` aligned with `channels.list`: 8460 total/active channels, 8331 RSS and 129 website, 7442 healthy, 721 working_noisy, 9 working_low_yield, 288 technical bottlenecks, repair lanes 7442 none, 730 monitor_quality, 184 access_or_auth_required, 87 deep_alternatives, 17 polite_retry. Residual/content read-back remained strict and independent: 4 web-visible content items, 9 selected rows, 21 hold rows, 10410 rejected rows, 7470 technicalFiltered, 2799 semanticNoMatch, missingInterestFilterResults 0. Interpretation: the source-pool expansion exceeded the requested +1000 because of local apply-result parsing, but selection/web visibility stayed correctly independent; next work should let fetch/ingestion soak and monitor scheduler pressure before adding any more channels.
  - Controlled MCP real-search SMB zone expansion 2026-05-10 local: user explicitly requested another 1000 new channels from real search for small/mid-business across very different zones. Ran a corrected bounded MCP-only expansion that counted progress only by DB-backed `channels.list` after every batch and applied only strict `ready_create` rows from `channels.bulk_onboard.plan` with the returned `planFingerprint`. Candidate pool had 5105 feed-shaped real-search sources: Google News RSS search feeds across North America, Oceania, EU, GCC/MENA, ASEAN, India, Japan, South Korea, Taiwan, Africa, and Latin America, plus HNRSS/StackOverflow query/tag feeds. Queries covered SMB verticals and local communities: dentists, vet clinics, accounting/law firms, construction, property management, retail/restaurants/hotels, small manufacturers, logistics, nonprofits, family businesses, chambers, merchant associations, regional development agencies, startup hubs, search funds, ETA, angel networks, venture studios, and software needs such as client portals, ERP/CRM/ecommerce, QuickBooks/Xero/HubSpot/Shopify/Odoo integrations, workflow automation, cyber/compliance, legacy replacement, API integration, vendor search, and paid contractor asks. Excluded disallowed Russia/Belarus/North Korea/China source zones. Result: exactly 40 MCP batches, 1000 attempted sources, 1000 strict ready-create rows, channel total increased from 8460 to 9460 (+1000) with no overrun. Post-pass read-back: 9460 active channels, 9331 RSS and 129 website, 288 technical bottlenecks, 1451 working_noisy, 9 working_low_yield. Selection/web visibility stayed independent and was not loosened: contentTotal 4, selected rows 9, hold 32, rejected 12419, technicalFiltered 8908, semanticNoMatch 3370, missingInterestFilterResults 0. No cleanup/delete/archive/reset was performed.
  - Deep MCP calibration pass 2026-05-10 local: user requested deep calibration after the audit showed channel count grew from ~250 to 9460 while selected/web-visible articles did not. Read-only MCP audit found acquisition/ingestion working (`processed_total` over 14k and rising) but strict selection blocked nearly all new material: dominant buckets were `technical_filter`/`semantic_filter`, strong duplicate pressure from Google News/search feeds, and `places` as a dominant hard-gate in active criteria diagnostics. Through MCP only, updated 23 active ODPT/H100 system interests to remove `places` as a hard gate, keeping disallowed-country negative terms and source-policy geography as the control layer; source priors/source health still do not influence selection/ranking/web visibility/counts. Added 3 precise bridge interests: `ODPT DeepCal: SMB paid contractor and vendor ask`, `ODPT DeepCal: Search fund ETA post-acquisition systems need`, and `ODPT DeepCal: Grant or mandate creates implementation vendor pull`, each with balanced/hold/optional-high-value LLM profile and explicit buyer/vendor/project terms. Queued full MCP backfill `f49e793c-4f18-4683-b7aa-c1773a9d6101` with reason `odpt-deep-calibration-202605101837`; enqueue proof warned not to claim success before completion. After ~15 minutes the job was still running but alive with options progress `processedArticles=1400/15100`; worker container was healthy and operator health showed reindex/sequences still running, not failed. Mid-run read-back already showed expected recall movement into hold, not selected inflation: active interests compiled 30/30, contentTotal remained 4, selected remained 9, hold rose from 42 to 67, rejected rose with processed volume, and missingInterestFilterResults fluctuated while replay was in progress. Next heartbeat should monitor job `f49e793c-4f18-4683-b7aa-c1773a9d6101` until completed/failed before making final yield claims. No cleanup/delete/archive/reset was performed.
  - Deep calibration backfill failure/read-back 2026-05-10 local: MCP heartbeat monitoring showed job `f49e793c-4f18-4683-b7aa-c1773a9d6101` failed at exactly the active Default Reindex task timeout window with `Task reindex was cancelled before completion`, after processing `12100/15100` articles. This is a capacity/timeout blocker for the enlarged retained DB and 30 active selection profiles, not evidence that calibration failed semantically. Partial read-back before failure showed calibration effect: contentTotal/web-visible increased from 4 to 10, selected rows from 9 to 19, hold rows from 42 to 201, stalePassThroughCount stayed 0, and missingInterestFilterResults returned to 0 at the read-back moment. Scheduler health improved separately: overdue_channels dropped to 237, fetch_new_content_24h rose to 2941, and source_bottleneck showed 9460 active channels with 285 technical bottlenecks. Residual target is still below the 50-60 quality goal and all selected rows remain weak verification; next work should choose between a longer reindex timeout/chunked replay or a more selective backfill continuation before claiming final calibration yield. No cleanup/delete/archive/reset was performed.
  - MCP bounded replay guidance and cancellation-key fix 2026-05-11 local: added first-class MCP guidance for enlarged retained DBs where full selected-content replay times out. `maintenance.reindex.request` now exposes bounded `options.docIds`, rejects unsupported runtime fields, and `operator.tuning.recommend` recommends the MCP-only flow `articles.residuals.summary/list -> maintenance.reindex.request docIds -> maintenance.reindex_jobs.list -> operator.report.verify -> operator.effect.verify` for `gray_zone_hold` recall recovery. Updated MCP server instructions/resources so clients understand that source priors/source health never affect selection/ranking/web visibility/counts and that hold replay must be chunked and verified. Product MCP proof: bounded replay job `e1d8ae10-172e-48a0-b083-a046ad01d466` completed 50/50 and moved selection from 19 to 27 selected rows; second chunk `b7c13c79-5cfc-4c2d-a937-c381beaea62c` completed 50/50 but did not increase selected/web-visible count, leaving 27 selected rows and 12 web-visible content items. A third attempted 100-doc chunk exposed a system bug: `reindex_jobs.cancellation_key` stored the full docIds array and exceeded the btree index row limit. Fixed `packages/control-plane/src/reindex.ts`, `services/workers/app/reindex_backfill_runtime.py`, and `services/api/app/content_analysis_backfill.py` to use a stable `sha256` cancellation key while retaining full details in `options_json`. Proof passed: `pnpm unit_tests:ts -- reindex-control-plane mcp-control-plane` (369/369), `PYTHONPATH=. python -m unittest tests.unit.python.test_worker_hard_filters` (10/10), `pnpm --filter @newsportal/mcp typecheck`, `pnpm lint:ts`, `pnpm lint:py`, and `git diff --check` over touched files. Rebuilt `mcp`, `api`, and `worker`; live MCP proof queued 100-doc chunk `9cd4a0f1-85c9-45b4-a46c-26b7659bf5e8` with short `sha256` cancellation key length 107 and completed 100/100. Final read-back after the live fix: active channels 9460, fetch_new_content_24h 4186, selected rows 27, web-visible content items 12, hold 217, rejected 23629, missingInterestFilterResults 0, stalePassThroughCount 0. Interpretation: the system bug is fixed and bounded replay works, but the quality target is still not met; current blocker is semantic quality/source-yield, not acquisition volume or display gating. No product-test DB cleanup/delete/archive/reset was performed.
  - Hold-quality/replay-observability fix 2026-05-11 local: implemented replay progress heartbeats and hold-quality diagnostics for the retained outsourcing-demand product test. `reindex_backfill` now writes progress at start, at least every 5 docs/30 seconds, and completion with `phase`, `currentDocId`, `processedArticles`, `totalArticles`, `criterionLlmReviews`, `lastProgressAt`, `lastHeartbeatAt`, `elapsedSeconds`, and LLM review failure counts; historical replay LLM reviews now soft-fail/timeout instead of making a chunk look stuck. Reindex read-back now exposes `derivedState` including `running_active`, `running_no_progress_yet`, `running_stalled`, `completed`, `failed`, and `completed_with_review_failures`. Added generic config-driven candidate-signal tiers (`context`, `buyer_intent`, `project_intent`) so context-only cues can remain diagnostic without becoming selected or inflating new hold buckets; source priors/source health remain selection-independent. Added MCP read-only tools `articles.holds.summary`, `articles.holds.list`, `articles.holds.explain`, `operator.report.verify reportKind=selection_hold_quality`, and updated MCP guidance/resources plus `operator.tuning.recommend` to prefer 25-doc chunks and max 50 when LLM reviews may run. Proof passed: `PYTHONPATH=. python -m unittest tests.unit.python.test_final_selection tests.unit.python.test_reindex_backfill_progress` (26/26), broader targeted Python suite (76/76), `pnpm unit_tests:ts -- mcp-control-plane` (373/373), `pnpm --filter @newsportal/mcp typecheck`, `pnpm --filter @newsportal/control-plane typecheck`, `pnpm lint:py`, `pnpm lint:ts`, `pnpm typecheck`, and `git diff --check` over touched files. Rebuilt `api`, `mcp`, and `worker`; health checks passed for API/MCP/web. Live MCP proof against retained DB: before the chunk, `articles.holds.summary` showed 210 holds all `unknown`; `maintenance.reindex.request` queued bounded job `f7746091-2eb6-42b0-b6b1-45941a34dd59` for 25 inspected hold docIds. MCP polling showed `running_active` with progress at 0/25, 5/25, 10/25, 15/25, 20/25 and final `completed`, 25/25, 29 criterion LLM reviews, 0 LLM review failures. Post-chunk MCP verification: selected rows stayed 27, web-visible content stayed 12, stalePassThroughCount stayed 0, missingInterestFilterResults stayed 0, holds shrank 210 -> 198 and split into 13 `project_intent_hold` plus 185 still-unreplayed `unknown`; content/source counts continued to rise independently (`active_channels=9460`, `fetch_new_content_24h=4421`). Interpretation: the fix resolves replay observability and exposes the real next bottleneck: old hold rows need tiered bounded replay and/or generic negative-cue tuning, while selected quality must not be increased by source health or context-only evidence. No product-test DB cleanup/delete/archive/reset was performed.

  - Experimental thematic adapter layer 2026-05-11 local: implemented the research-stage adapter/source-role capability requested for rare-signal acquisition gaps. Added API adapter metadata to source channel config, an API adapter registry for official/public adapters (HN Algolia, GitHub Issues Search, Stack Exchange, Greenhouse, Lever, Ashby, Remotive, RemoteOK, We Work Remotely RSS marker) plus bounded research-only public-page marketplace extractors, source-role coverage/read-back, adapter-research and indirect-aggregator MCP tools, operator report verification kinds for source_role_coverage and adapter_research, and MCP guide/context updates that warn RSS volume is not thematic coverage. Source role, adapterKey, researchMode, tosRisk, source health, and source priors remain acquisition diagnostics only and do not influence selection/ranking/escalation/web visibility/counts. Proof passed: pnpm unit_tests:ts (382/382), pnpm typecheck, and pnpm lint. No product-test DB cleanup/delete/archive/reset was performed.
  - Experimental adapter product proof 2026-05-11 local: continued the retained outsourcing-demand product flow through MCP only. `discovery.adapter_research.start` persisted 13 adapter-research endpoint evidence rows: 9 `official_free`, 2 `github_unofficial_public`, 1 `github_unofficial_restricted`, and 1 `closed_access`. Bounded `channels.bulk_onboard.plan/apply/verify` created 12 API adapter channels across `community_search`, `remote_job_board`, and `project_marketplace`; a query-backed API dedupe defect was found and fixed so different adapter search queries are not collapsed as duplicate channels. Live MCP read-back after natural fetch/ingestion showed active channels 9472, API adapter channels 12, source-role coverage now `community_search=7`, `remote_job_board=3`, `project_marketplace=2`; adapter channels were mostly working/noisy with one RemoteOK broken-fetch lane. Selection remained independent and below target: 27 selected rows, 12 web-visible content items, 211 holds, 0 stale pass-through, 0 missingInterestFilterResults. Adapter-origin quality audit showed HN/StackOverflow/GitHub mostly semantic/technical rejects, Remotive produced one `project_intent_hold` (`Operations software assistant`), and marketplace public-page research adapters currently extract many category/navigation pages rather than enough project-detail asks. Added a bounded indirect-aggregator evidence pass through `discovery.indirect_targets.start`: 40 detect-only `site:` queries for Upwork, LinkedIn, Reddit, HN, Shopify/HubSpot/WordPress/Bubble/Zapier communities, and GitHub; no channels or selected content were created by this operation. A read-model gap was fixed so `source_role_coverage` now reports `candidateEndpoints` and `detectOnlyEndpoints`; `indirect_aggregator` moved from missing to pending execution with 40 detect-only endpoints, while `rss_web`, `forum_support`, and `procurement` remain metadata/role coverage gaps for retained channels/endpoints. Proof passed after the latest fixes: `pnpm --filter @newsportal/control-plane typecheck`, `pnpm --filter @newsportal/mcp typecheck`, `pnpm unit_tests:ts -- thematic-discovery mcp-control-plane` (385/385), MCP rebuild healthy. No product-test DB cleanup/delete/archive/reset was performed.
  - Executable indirect/search adapter lane and marketplace/forum extraction 2026-05-11 local: implemented the next missing acquisition layer without domain hardcode. Added search adapter keys `searxng_search`, `brave_search`, `tavily_search`, `exa_search`, `serpapi_google_news_research`, plus `discourse_search`; added generic `searchQuery` adapter config with `directCoverage=false`; normalized search-provider results into normal article acquisition items with `rawPayload.fetcher=api_adapter`, `adapterKey`, `sourceRole`, query metadata, provider rank/url, and no selection influence. Added read-only MCP `discovery.indirect_targets.channels.plan` to materialize detect-only indirect endpoints into `channels.bulk_onboard`-ready API rows, and added `operator.report.verify` kinds `indirect_search_execution` and `marketplace_extraction_quality`. Improved research marketplace/forum extraction to reject category/navigation/profile/login/search/filter/listing-wrapper links, optionally follow bounded same-origin detail links, and preserve `projectDetailConfidence`, `extractionKind`, `rejectedAnchorReason`, `detailFetchAttempted`, and budget/scope metadata. Updated MCP server instructions/resources and `docs/product/operator/discovery/channel-adapter-gap.md` so clients use `source_roles -> indirect_targets -> indirect_targets.channels.plan -> bulk_onboard -> fetch/read-back` and do not treat marketplace/forum acquisition noise as selection evidence. Proof passed: `pnpm --filter @newsportal/mcp typecheck`, `pnpm --filter @newsportal/control-plane typecheck`, `pnpm --filter @newsportal/fetchers typecheck`, `pnpm unit_tests:ts -- api-channel-config api-adapter-registry thematic-discovery mcp-control-plane admin-api-channels schema-registry` (392/392), and `git diff --check`. Rebuilt `mcp` and `fetchers`; local stack healthy. Live MCP proof showed `discovery.indirect_targets.channels.plan` sees 40 detect-only indirect endpoints: with no SearXNG base URL it correctly returns `needs_config`/`searxng_base_url_required`; with a supplied base URL it returns bounded `ready_for_bulk_onboard` API rows with `selectionInfluence=none`. `operator.report.verify reportKind=indirect_search_execution` confirmed 40 detect-only endpoints and no active executable search channels yet; `reportKind=marketplace_extraction_quality` confirmed existing marketplace adapter rows are all rejected and have 0 project-detail evidence, validating the extraction/noise bottleneck. Current retained selection state through MCP remains independent and unchanged: 27 selected rows, 12 web-visible content items, 211 holds, 0 stale pass-through, and no DB cleanup/delete/archive/reset.
  - DDGS indirect-search execution proof 2026-05-11 local: user asked to use DDGS for the search lane while SearXNG is not configured. Added generic `ddgs_search` adapter support without domain hardcode: `discovery.indirect_targets.channels.plan` now defaults to a research-only internal API bridge (`/maintenance/discovery/search/ddgs`) for materializing detect-only indirect endpoints, `fetchers` normalizes DDGS bridge results as ordinary `api_adapter` acquisition items with `directCoverage=false`, MCP/report guidance documents DDGS as research-only and not production-certified direct coverage, and `.env.dev` allows fetchers to call the internal API service through the acquisition URL guard. Proof passed: `pnpm --filter @newsportal/control-plane typecheck`, `pnpm --filter @newsportal/fetchers typecheck`, `pnpm --filter @newsportal/mcp typecheck`, `python -m py_compile services/api/app/routes/discovery_routes.py services/workers/app/task_engine/adapters/web_search.py`, and `pnpm unit_tests:ts -- api-adapter-registry thematic-discovery api-channel-config mcp-control-plane admin-api-channels schema-registry` (394/394). Rebuilt `api`, `mcp`, and `fetchers`; direct API proof returned live DDGS Upwork results including fixed-budget/project-detail snippets. MCP product proof materialized and onboarded 20 `ddgs_search` indirect-aggregator API channels through `discovery.indirect_targets.channels.plan -> channels.bulk_onboard.plan/apply/verify`; `operator.report.verify reportKind=indirect_search_execution` showed 20 active DDGS channels and, after relay was restarted as a missing lifecycle component, 198 DDGS-acquired article/listing items. No DDGS item became selected through source/provider metadata: final read-back showed DDGS `selected=0`, `held=0`, `rejected=198`, global selection still `27` rows and `12` web-visible content items, `stalePassThroughCount=0`. The DDGS run proved the original thematic-search gap is real and now executable, but exposed the next generic bottleneck: good Upwork project asks such as PWA/MVP budget and HubSpot API integration are being false-rejected by over-broad negative cues (`best practices`, `event`) even when article-level project-intent signals are present. Next work should tune negative-cue precision/exception handling through MCP/admin configuration or generic candidate-signal policy so project-intent evidence is routed to hold/LLM review instead of being hard-filtered, without letting source role/provider health affect selection. A temporary scoped MCP read-back token was revoked after proof; no product-test evidence was cleaned/deleted/archived/reset.
  - Selection-quality semantics correction 2026-05-11 local: user removed the quantity goal and clarified that `selected` must be the only web truth, with no internal/public split. Added MCP read-only `operator.selection.precision_audit` and `operator.report.verify reportKind=selection_precision`; MCP guidance now instructs clients to fix noisy selected rows through system interests/templates/candidateSignals and bounded replay, not a second display gate. Runtime selection now rejects no-filter pass-through, requires item-level buyer/project candidate evidence for semantic matches, then tightened that further to require a clean strong item-level match (`buyer_intent`/`project_intent`, multiple positive groups/hits, no noise, no matched-row filter reasons). Added generic article-shape vetoes for repo-internal PR/commit noise, professional-network posts, jobs-only posts, and directory/listicle items. Proof passed: targeted Python selection/operator suites (67/67), `pnpm lint:py`, `git diff --check`, worker rebuild, and MCP bounded replay/read-back. Retained DB evidence shows the fix is taking effect without cleanup: selected rows dropped from 139 to 122 across bounded replay chunks, content/web-visible dropped from 81 to 66, holds rose from 216 to 228, stale pass-through stayed 0, and missingInterestFilterResults stayed 0. Remaining visible items include some real marketplace/forum project asks and residual LinkedIn/marketplace ambiguity that needs more bounded replay/config tuning; quality count is no longer treated as a failure metric.
  - Coverage-first source-family calibration 2026-05-11 local: implemented the no-auto-disable source-retention plan. Added `packages/control-plane/src/source-families.ts` with source-family classification, lifecycle labels, DB-backed coverage summary, coverage-first autoplan, and iteration recommendation. Exposed MCP `discovery.source_families.coverage`, `operator.funnel.autoplan`, `operator.funnel.iteration.recommend`, and `operator.report.verify reportKind=source_family_balance`; MCP guidance/resources/prompts now tell clients to retain working noisy/low-yield/negative-control useful channels and use labeling, measurement, cadence changes, repair, adapter/access lanes, and strict item-level selection instead of automatic deactivation. Hardened the article-yield remediation CLI so Google RSS sources are deprioritized by polling cadence rather than set inactive. Proof passed: `pnpm unit_tests:ts -- source-families thematic-discovery mcp-control-plane` (404/404), `pnpm --filter @newsportal/control-plane typecheck`, `pnpm --filter @newsportal/mcp typecheck`, `pnpm --filter @newsportal/fetchers typecheck`, `pnpm lint:ts`, and `git diff --check`. Rebuilt local MCP with Docker Compose; `http://localhost:4300/health` returned `{"service":"mcp","status":"ok"}`. No product-test DB cleanup/delete/archive/reset was performed.
  - Operational restore snapshot 2026-05-12 local: replaced the earlier broad audit snapshot with a compact restore-oriented JSON snapshot at `docs/product/operator/snapshots/2026-05-12-operational-restore-config/`, then deleted the old `docs/product/operator/snapshots/2026-05-12-system-config-snapshot/` directory and removed temporary helper `/private/tmp/newsportal_export_snapshot.mjs`. The compact snapshot includes only required/useful operational restore inputs: system interests, selection profiles, LLM prompt templates, all source channels split by provider, source runtime scheduling state, sequence definitions, active discovery targets, non-rejected/actionable discovery endpoints, source contracts, provider capabilities, discovery LLM task templates, manifest, restore order, and operational summary. It excludes discovery runs, repairs, debates, evidence items, coverage history, provider queries, article/content/final-selection state, and claim history as noise for clean-start restore. Proof: generated 16 JSON files / 40M, row counts include 9492 source channels, 129 website channels, 91 system interests, 14 LLM templates, 91 selection profiles, 78 sequences, 102 discovery targets, and 6796 operational endpoint rows; secret-pattern scan returned no matches for known local key patterns. No DB mutation or cleanup/delete/archive/reset was performed.
