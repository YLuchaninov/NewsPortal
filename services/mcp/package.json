{
  "name": "@signalops/mcp",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && pnpm build:runtime",
    "build:runtime": "node ../../infra/tooling/build-node-runtime.mjs --service=mcp",
    "start": "node dist/main.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@signalops/config": "workspace:*",
    "@signalops/contracts": "workspace:*",
    "@signalops/control-plane": "workspace:*",
    "@signalops/sdk": "workspace:*",
    "fastify": "5.8.5",
    "pg": "8.20.0"
  }
}
